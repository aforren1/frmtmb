# Review: lane wt-brmsport (item 2.6b), round 1

Reviewer brief: falsify the ledger and the defect claims. Worktree
`C:\Users\adf44\source\r\frmtmb-wt-brmsport`, base `e031e8c`, lane build
`C:/Users/adf44/source/r/brmsport-lib` (read only). No package edits, no
git operations, no `R CMD check`.

**Verdict: mergeable after punch.** The tier is green and gated, and it
changes no package code. The record is not yet right: the regression
claim is false, two silent claims are misdiagnosed, two passes are
hollow, the harness accepts seven hollow constructions of mine, and the
user's three decisions of 2026-09-17 move 45 rows.

## Scripts and logs

| script | log | what |
|---|---|---|
| `dev/brmsport-rev-verify.R` | stdout, quoted below | lane build against worktree source |
| `dev/brmsport-rev-silent.R` | `dev/brmsport-log/rev-silent.txt` | the eight silent claims, own constructions, brms beside |
| `dev/brmsport-rev-regress.R` | `dev/brmsport-log/rev-regress.txt` | the regression claim at five commits |
| `dev/brmsport-rev-guards.R` | `dev/brmsport-log/rev-guards.txt` | hollow passes the harness accepts |
| `dev/brmsport-rev-mutants.R` | `dev/brmsport-log/rev-mutants.txt` | passes against a broken frmtmb |
| `dev/brmsport-rev-totals.R` | `dev/brmsport-log/rev-totals.txt` | corrected ledger totals, generated |

Data seed 91 wherever I simulate data. The regression script loads each
commit with `pkgload::load_all()` from a `git archive` extract in the
session scratchpad; nothing was installed.

## The instrument

- The lane build matches the worktree. Every top-level function in `R/`
  deparses identically to the installed namespace: 1024 of 1024 in
  frmtmb 0.59.0, 197 of 197 in frmtmb.sample 0.7.0. StanHeaders 2.32.10,
  brms 2.23.0.
- `git diff --stat e031e8c` shows only `dev/release/run-gated.ps1`; the
  rest is untracked `dev/` and test files.
- The tier reproduces through a copy of the RELEASE runner
  (`dev/release/run-tests.R` with its library swapped to brmsport-lib),
  which does not attach frmtmb first: frmtmb.sample families 84 and
  methods 61, core methods 162 and standata 87, all with 0 fail, 0
  error, 0 skip.
- The record counts reproduce: 598 assertion records, 433 core and 165
  sample; 25 raw passes that the harness does not count.

## Findings

### MAJOR 1. The `default_prior()` regression claim is false

Ledger `priors:74` and findings L9 say the response check "arrived with
the 2.6c merge". Measured on brms's own construction (`y = rnorm(10)`
under `Beta()`), one process per commit:

| commit | what | Beta, `y = rnorm` | Beta, `y` in (0, 1) | binomial, no `trials()` |
|---|---|---|---|---|
| `df541e7` | base 0.58.0 (`get_prior()` only) | refused | 8 rows | refused |
| `d04b115` | famlink | refused | 8 rows | 3 rows |
| `4ef482f` | priorform | refused | 8 rows | refused |
| `2bf3074` | priorform merged onto famlink | refused | 8 rows | 3 rows |
| `e031e8c` | main | refused | 8 rows | 3 rows |

The refusal is at least as old as 0.58.0: base `get_prior()` called
`assemble_frame()`, which ran `valid_y` unconditionally. famlink's
`check_trials = FALSE` skips `valid_y` only for families that read
trials, which is why the binomial column changes and the Beta column
does not. Nothing regressed.

Why priorform's ledger showed a table: `dev/priorform-ledger.R`, block
P13, adds `dat$y <- stats::runif(10)` to brms's code. That substitution
changed brms's data to step past the refusal, and neither
`dev/priorform-findings.md` nor its review mentions it. File `priors:74`
as a PRE-EXISTING loud defect and name the substitution.

### MAJOR 2. S1 is mostly brms parity

brms 2.23.0 also accepts and ignores `not_an_argument = 2` on
`posterior_epred()`, `posterior_linpred()`, `posterior_predict()`,
`predictive_error()` and `log_lik()`: each returns 25 x 40 on
`brmsfit_example1`. Swallowing the dots is what brms does. The silent
defect that stands is narrower: `point_estimate` and
`ndraws_point_estimate` are brms arguments that frmtmb.sample does not
implement and ignores (25 x 40 where brms gives 2 x 40). Rewrite S1 and
drop "the 2.5e dots refusal does not reach these four" as a brms-parity
defect.

### MAJOR 3. S8 is a `$` partial match, not an empty data slot

`names(fit1)` has no `data`; it has `data2`, and there is no
`$.frmtmb_fit`. `fit$data` partial-matches `fit$data2`, and
`fit[["data", exact = TRUE]]` is `NULL`. So the answer depends on
`data2`: on a fit with `data2 = list(A = A)` for `gr(g, cov = A)`,
`fit$data` is `list(A = <6 x 6 matrix>)`, `nrow(fit$data)` is `NULL`.
The 12 `fit-data` rows and the hollow `brmsfit-methods:1035` are
unchanged in outcome, but the diagnosis in the findings and in their
reasons is wrong, and the fix is different (this is the `$` hazard of
`frm_hazard_reads()`, not a missing slot).

### MAJOR 4. Two recorded passes are hollow

- `brmsfit-methods:394`, `expect_true(is.numeric(hyp$hypothesis$Evid.Ratio[1]))`.
  frmtmb's value is `NA`, and `is.numeric(NA_real_)` is `TRUE`. brms's is
  0.1169631. D4 records that `Evid.Ratio` is `NA` on a fit, so this row
  is a no-draws divergence that the harness counts as a pass.
- `brmsfit-methods:698`, `expect_error(pp_check(fit2, "violin_grouped"), "group")`.
  frmtmb dies with R's `argument "group" is missing, with no default`;
  brms refuses by design ("Argument 'group' is required for ppc type
  'violin_grouped'"). The regex matches an internal error. Row 697 in
  the same block, an internal error on an unknown type, is filed as a
  defect of class `internal-error`; 698 must be too.

### MAJOR 5. The harness accepts hollow passes its rules do not name

Each case below is recorded as `pass`, is hollow, and is ACCEPTED
(failures 0). The two controls built from the existing rules fire.

| case | failures |
|---|---|
| H1 regex `expect_error` met by an argument-name refusal (`fitted(fit, bogus = 1)`, `"bogus"`) | 0 |
| H2 bare `expect_error` met by a "cannot honor" argument refusal (`fitted(fit, ndraws = 5)`) | 0 |
| H3 `expect_equal` on two NULLs through `fit$data` | 0 |
| H4 regex met by R's missing-argument error (the 698 pattern) | 0 |
| H5 stale object through a complex assignment `x$a <- stop()` | 0 |
| H6 a missing function reported as "object ... of mode 'function' was not found" | 0 |
| H7 `is.numeric()` on `NA` (the 394 pattern) | 0 |
| control: bare `expect_error` met by "has no argument" | 1 |
| control: plain stale object | 1 |

H4 and H7 are live in the ledger (MAJOR 4). I found no live H1, H2, H5
or H6 among the 167 passes by their caught messages. Suggested rules:
treat "cannot honor", "has no argument" and "unused argument" as vacuous
for any `expect_error`, not only bare ones; add "of mode 'function' was
not found" and "is missing, with no default" to the vacuous pattern;
mark `x$a <-`, `x[[i]] <-` and `names(x) <-` targets stale on failure.

### MAJOR 6. Citations that do not say what the ledger says

- D9 cites item 2.5b. Row 2.5b of `dev/extension-gaps-plan.md` is about
  registering methods on brms's six generics, not about class names.
  The user's decision of 2026-09-17 now covers it (below).
- D6 (`brmsfit-methods:107`, `as.mcmc(inc_warmup = TRUE)`) cites
  `dev/brms-api-diff.md` (c), whose heading is "Gaps worth closing later,
  not implemented here" and whose entry says "Blocked, not small". A
  recorded gap is a missing feature: cannot transfer, absent.
- `brmsfit-methods:217` (no "treated as continuous" warning on fit4)
  cites brmsnames "Found and NOT fixed" 9, which is about
  `conditional_effects()` on DRAWS. Item 11 of the same list says the
  FIT method was not audited. Defect, output.
- `brmsfit-methods:213`, `:215` (the default Wald band refused on a
  nonlinear predictor) cite `dev/brms-api-diff.md` (b), which records
  that a fit's band has Wald, profile and bootstrap choices. It does not
  record refusing the default on a nonlinear predictor. Defect,
  refuses-accepted.
- `priors:55`, `:59` (`s(x).fx1` against brms's `sx_1`) cite priorform
  P11 and P12, whose reason defers to lane wt-brmsnames; brmsnames "Found
  and NOT fixed" 5 defers prior naming back to priorform. No document
  decides it, and after brmsnames the same coefficient is `bs_sAge_1` in
  `variables(fit1)` and `s(Age).fx1` in the prior table. Defect, naming.

Citations that held are listed under "Claims that held".

### MAJOR 7. The user's decisions of 2026-09-17, applied

**Rule 1, wording.** I read brms's data and frmtmb's message for all 27
`wording` defects. 25 are the same refusal of the same case and become
passes: `brm:75, 77, 79, 83, 85, 88, 90, 92`;
`brmsfit-methods:203, 207, 330, 414, 719`; `standata:106, 109, 112,
121, 124, 127, 130, 179` (`s = -(1:9)`), `181`, `184` (`z = 1:9`
against `t = 9:1`), `726, 1118`. Two are not:

- `brm:81`: frmtmb stops on "object 'sei' not found" because it
  evaluates the data first; brms refuses `se()` for weibull from the
  formula alone. A different error. It also matches the harness's own
  vacuous pattern, so it could never be a pass. Defect,
  `different-error`.
- `families:102`: `mixture(poisson, binomial, order = "x")` is refused
  because `mixture()` has no `order`, where brms refuses the VALUE. An
  argument-name refusal. Defect, `argument`.

Two divergences are the same refusal in frmtmb's words and become
passes: `families:44` (a length-2 `link_shape`) and `brm:110`
(`ma(x)` on poisson needs `cov = TRUE`).

**Rule 2, class names.** The 15 `class` rows stand as divergences with
the user's decision as the citation. Two more rows fail only because of
a brms class and move there from cannot transfer:
`brmsfit-methods:373` (`is.brmsformula(formula(fit1))`) and `:270`
(`is(ms, "brms_conditional_effects")`). `priors:27`
(`expect_is(bprior, "brmsprior")`) was a `policy` divergence and is
relabeled `class`.

The package contradicts rule 2 today: `R/confint.R:2673` sets
`class(out) <- c("frmtmb_hypothesis", "brmshypothesis")`, as brmsnames
recorded. Not this lane's code; it needs filing. No bin-1 pass depends
on it.

**Rule 3, shapes and deprecated spellings.** The `shape`, `output`,
`nsamples()` and `posterior_samples()` rows are defects already. Four
shape assertions were filed as divergences because a refusal blocks
them first, while rows 888 to 897, blocked the same way by
`summary(priors = TRUE)`, were filed as shape defects. Past the refusal
each shape is still wrong (a vector, or no `$fixed`), so they are
defects: `brmsfit-methods:314`, `:317` (`fitted(ndraws =)`), `:825`
(`residuals(probs =)`), `:887` (`summary(priors =)`). Four more were
filed as missing features: `fitted()` and `residuals()` on the
multivariate fit6 refuse with "not supported yet" (`:358`, `:359`,
`:840`, `:841`). Both functions exist and brms answers, so these are
defects, refuses-accepted.

**What the tier needs for this.** A row flipped to `pass` under rule 1
will turn the tier RED, because brms's regexp still does not match
frmtmb's message. The port needs a verdict that asserts frmtmb's own
refusal pattern, so that a different error cannot satisfy it (the
`brm:81` trap). Row 394 needs the `hollow` treatment that row 1035 has.

### MAJOR 8. Features frmtmb has under another spelling

- `brmsfit-methods:995`, `parnames(fit3)`: brms's live spelling of
  `variables()`. `dev/brms-suite-audit.md` section 7 contract 14 lists
  it beside `nsamples()` and `posterior_samples()`, and frmtmb.sample
  already defines a `parnames()` that refuses. Defect, `spelling`.
- `brmsfit-methods:269`, `:275`, `:277`, `conditional_smooths()`:
  `R/conditional-effects.R` line 1121 documents that
  `conditional_effects()` "also covers what brms calls
  `conditional_smooths()`". Either that sentence is wrong or these are
  spelling defects; filed as `spelling`. `:273` (spaghetti) stays absent
  and `:270` is rule 2.

I checked the other `absent` rows against `R/`, `NAMESPACE` and
frmtmb.sample: `hurdle_negbinomial()`, `xbeta()`,
`zero_inflated_beta_binomial()`, `hurdle_cumulative()`,
`make_conditions()`, `update_adterms()`, `posterior_average()`,
`prior_draws()`, `threshold =`, `cmc`, `drop_unused_levels`, `subset()`,
`rate()`, `thres()`, `cat()`, `gr(by =)`, `mm(by =)` and
`0 + Intercept` have no other spelling. `cse` appears only in the
whole-term refusal list of `R/parse.R`.

### MINOR

1. S2 is wider than recorded. `ma(x + t, g, cov = TRUE)` fits
   (logLik -56.35949) and so does `ar(t - 10 * x, g, cov = TRUE)`
   (-56.58647, control `ar(t, g)` -52.40404); brms refuses both.
   `ar(2 * t, g)` gives the control's logLik with a warning, as it
   should. S3 reproduces (`gr = g1/g2` -52.15196); `g1 * g2` and
   `g1 + g2` were refused on my design only because groups collided.
2. S6 is every ordinal family, not sratio with `cs()`: a
   `cumulative()` fit lists `b_x` alone with 2 thresholds in
   `opt$par`. A categorical fit lists all 4. `variables(fit5)` lists 8
   names for 7 parameters, which is right (two mixing weights).
3. S5 is half silent. `"b_Age x 0"` is LOUD ("cannot be found:
   'b_b_Agex0'"). The silent half: `hypothesis(fit3, "Trt1 + Age")`
   returns exactly the row of `"Trt1 + Age = 0"`.
4. S7: the ordinal residual is the response code minus the expected
   score, `y - fitted(fit4) %*% 1:4`, equal at the printed digits. A
   defined number that brms refuses; the least harmful of the eight.
5. The gated glob fails open on its absent case. It matches exactly the
   14 files and ignores `.Rmd`, `.R~`, `.R.bak` and `.RData` decoys, but
   an empty match adds 0 jobs and the existence check then passes on
   zero jobs. Pin the counts (10 and 4) or throw on zero.
6. Fixture 1 does not converge (nlminb false convergence (8), NaN
   standard errors, the `mo()` simplex flat in 3 directions). No pass
   reads a value from it today (dimensions, row counts, positivity of
   an exp link), so no row is affected; a future value assertion would
   be.
7. The standata shim writes brms's zeros into `rcens`. A mutant with
   `y2 = 999` on non-interval rows still passes `standata:163`; a mutant
   that shifts the interval rows fails. The interval half is genuine.
8. The family link passes that use the default link (`families:4, 5, 6,
   12, 18, 23, 25, 32, 47, 49`) pass for a constructor that ignores its
   link. frmtmb honors non-defaults (`student(log)`, `bernoulli(probit)`,
   `exponential(identity)`), so the weakness is brms's test, not a
   defect.
9. frmtmb.sample's copy of the helper has no guard that it matches
   core's; today they differ only by the three-line header.
10. `emmeans:21`, `:24` (a `mo()` term) are defects and `emmeans:27`
    to `:50` (nonlinear, multivariate) are absent, for the same kind of
    refusal from the same support code. Pick one.
11. `dev/brmsport-log/defects.txt` numbers its sections differently
    from findings S1 to S8 (its S2 is hypothesis, its S3 is `ar()`).
12. `brmsfit-methods:399` to `:401` cite D4, which is about `scope`;
    the `r_visit[4,Intercept]` name is closer to brmsnames "Found and
    NOT fixed" 16. Kept as divergence.

### NIT

- `dev/release/run-gated.ps1`: the header still says "the two sampler
  files", and the two new lines pass 80 columns.
- `dev/release/run-tests.R` still reads `rellib-r3`, which predates
  `4e179c0`; the verdicts assume 0.59.0.
- `brmsfit-methods:203`: "pass the original data via data =" misleads
  for a misspelled effect name.
- `brmsfit-methods:719`: the refusal names the internal
  `mixture_probs()`.

## Silent wrong answers, ranked

1. S2, `ar()` and `ma()` with an expression as time: a different
   likelihood, no warning.
2. S3, `ar(gr = g1/g2)` on numeric codes: series merged by quotient.
3. S6, `variables()` omits thresholds and `cs()` coefficients on every
   ordinal family.
4. S5, a hypothesis with no relation is answered as `= 0`.
5. S8, `fit$data` is `fit$data2` by partial match.
6. S1, `point_estimate` and `ndraws_point_estimate` ignored (the dots
   swallow itself is brms parity).
7. S4, `fitted()` and `residuals()` on draws return `NULL`; downstream
   code usually fails loudly on it.
8. S7, an ordinal residual answered where brms refuses.

Missed by the worker: no new package-level silent answer beyond the
widenings in MINOR 1 to 3 and the `data2` content in MAJOR 3. In the
RECORD, two silent errors: the hollow passes of MAJOR 4. No defect or
divergence row returns a wrong number without an error other than the
four `silent` rows, 217 (output, not a wrong number) and the
`fit-data` rows.

## Corrected totals

Generated by `dev/brmsport-rev-totals.R` from the ledger plus the moves
above; `dev/brmsport-log/rev-totals.txt`:

| outcome | worker | corrected |
|---|---|---|
| pass | 167 | 192 |
| defect | 119 | 112 |
| divergence | 43 | 34 |
| pending 2.6d | 12 | 12 |
| cannot transfer | 153 | 144 |
| total | 494 | 494 |

Bin 1 passes 192 of 494 (38.9%). Divergences: class 18, no-draws 9,
policy 7. Cannot transfer: absent 106, stan 22, brms-internal 10, mcmc
4, fixture 1, no-draws 1. Defects: refuses-accepted 28, argument 23,
shape 22, fit-data 12, internal-error 5, output 5, silent 4, spelling
4, accepts-refused 3, naming 2, different-error 1, filed 1, hollow 1,
misparse 1. The frmtmb.sample half: 88 pass, 13 defect, 1 divergence,
63 cannot transfer, of 165.

Moves, by id: to pass, the 25 rule-1 rows plus `families:44` and
`brm:110`; `brm:81` to defect `different-error`; `families:102` to
defect `argument`; `brmsfit-methods:394` to divergence no-draws; `:698`
to defect internal-error; `:314, 317, 825, 887` to defect shape; `:217`
to defect output; `:213, 215, 358, 359, 840, 841` to defect
refuses-accepted; `priors:55, 59` to defect naming; `:107` to cannot
transfer absent; `:373, 270, priors:27` to divergence class; `:995,
269, 275, 277` to defect spelling.

## Claims that held

- No package code changed, and the lane build is the worktree source.
- The tier is green, 598 expectations in 14 files, and reproduces
  through the release runner's loading path.
- S2, S3, S4, S6 and S7 reproduce on my own constructions, with brms
  refusing (S2, S3, S7) or answering 40 x 4 (S4). S1's
  `point_estimate` half and S5's no-relation half reproduce.
- The draws design is brms's: all six `brmsfit_example` fits are one
  chain, `iter` 75, `warmup` 50, 25 draws, and the stand-ins give 25.
  No assertion reads draw values except 714, which is a defect.
- The fixture substitutions do not manufacture a pass or a defect that
  I could find. Fixture 3's 30 rows are brms's own count.
- The 25 raw passes the harness withholds, and the harness's own 12
  guard cases (log read, not re-run).
- The glob picks up the 14 files and nothing else in the tree.
- Citations that say what the ledger says: D3 (brmsnames section B);
  D4 on `Evid.Ratio` and the `scope`/`robust` refusals (brmsnames, "The
  hypothesis() object"); D5 on rows 978 and 984, where
  `variables(fit1)` and `variables(fit2)` contain every fit-side name
  the assertion lists and only draws quantities are absent; acat logit
  only (`?frmtmb-links`); `ranef(condVar =)` (`dev/brms-api-diff.md`
  (b)); priorform P1, P3, P15 and P26, which that lane's review read
  and accepted; 2.6d for the `predict()` rows.
- The ledger's reuse of famlink's and priorform's `absent` rows is
  faithful, apart from MAJOR 8.

## Recheck, round 1

Scope: the round's changes only. Record read: section 10 of
`dev/brmsport-findings.md`, `dev/brmsport-log/tier-final.txt`,
`guards.txt`, `ledger-summary.md`, `dev/brmsport-verdicts-own.tsv` and
the punched `tests/testthat/helper-brms-suite.R`.

**Verdict: mergeable.** Every MAJOR from round 0 is closed. What remains
is MINOR and none of it is live in the ledger today.

New scripts and logs:

| script | log |
|---|---|
| `dev/brmsport-rev1-guards.R` | `dev/brmsport-log/rev1-guards.txt` |
| release runner copy, all 14 files | `dev/brmsport-log/rev1-tier-release.txt` |

### The instrument

- The lane build still matches the worktree: 1024 of 1024 and 197 of
  197 functions identical. `git status` shows only
  `dev/release/run-gated.ps1` modified; no package code changed.
- Tier through the release runner's loading path (a copy of
  `dev/release/run-tests.R` with the library swapped, frmtmb not
  attached first), one process per file: 14 of 14 files, 598
  expectations, 0 fail, 0 error, 0 skip. Per file it matches
  `tier-final.txt` exactly (23, 3, 16, 5, 6, 11, 84, 162, 36, 87; 16, 4,
  84, 61).
- The ledger reads 192 pass, 117 defect, 34 divergence, 12 pending
  2.6d, 139 cannot transfer. The sample half is 88, 13, 1, 63. The 27
  own-words ids are the 27 of round 0's rule-1 list.
- The record has 28 own-words runs (families:44 runs in both packages),
  all held. No genuine pass was lost to the new rules: the only runs
  that held raw and are now withheld as passes are 394 and 698; the rest
  are rows that were never passes.
- The empty glob throws, and a directory with matches does not.

### Findings

**MINOR R1. The own-words verdict cannot see the REASON for a refusal.**
Construction O1: `brms_port_own()` with brm:110's pattern on
`frm(y ~ ma(x), d)` under gaussian is ACCEPTED. brms 2.23.0 fits that
model (`make_stancode()` returns code for gaussian and refuses poisson
with "Please set cov = TRUE when modeling MA structures for this
family"). frmtmb's message is its refusal of `cov = FALSE` on EVERY
family. So brm:110 passes on the same refusal that makes brm:106 and
:108 defects. For brm:110's own call (poisson) the outcome stands. The
mechanism also has no specificity check on the pattern: O3, pattern
`.`, is accepted on a designed refusal of an unrelated case. Specificity
rests on reading each row, so I read all 27 patterns against brms's call.
Each names the refusal of that case. Four use a message frmtmb also
gives on a case brms ACCEPTS: brm:110 (gaussian `ma()`), `standata:112`
(the same message refuses `standata:75`'s two-valued response),
`standata:179` (the same message refuses `se = 0`) and
`brmsfit-methods:203` (the same message refuses `:205`, where brms
warns). Those passes are right for the ported calls; state the caveat
beside the four rows.

**MINOR R2. The own-words verdict skips stale detection.** Construction
O2: an own-words row reading an object whose assignment failed is
ACCEPTED, and the same row as a plain `pass` is rejected.
`brms_port_own()` calls neither `brms_stale_reads()` nor `brms_mark()`.
Not live: the five own-words rows that read an object read fixtures,
which are never stale.

**MINOR R3. Under-reach: hollow shapes the rules still accept.** Each is
ACCEPTED as a pass:

| case | construction |
|---|---|
| U1 | `expect_null(fit$data$y)` |
| U2 | `expect_true(is.null(fit$data$y))` |
| U3 | `expect_type(NA_real_, "double")` |
| U4 | `expect_true(is.numeric(v) && length(v) == 1)` on NA |
| U5 | `expect_length(fit$data$y, 0)` |
| U6 | an argument refusal in other words, `bf(y ~ x, sigma1 = "sigma2")` refused as "Cannot interpret bf() argument" and met by `"sigma"` |
| U7 | a stale object through `assign("x", stop())` |

U8, a NULL compared to a variable holding NULL, is rejected. None of
U1 to U7 is live: no pass row uses `expect_null`, `is.null`,
`expect_type`, `expect_length`, a compound type check or `assign()`, and
no caught message of a pass is an argument refusal in other words.
brmsfit-methods:1035, the one `is.null` row, stays hand-marked hollow.

**MINOR R4. Over-reach: genuine passes the rules now reject.** Each is
REJECTED: V1 `expect_equal(names(c(1, 2)), names(c(3, 4)))`, two
correctly NULL names; V2 an assertion whose point is the argument
refusal, `expect_error(fitted(fit, bogus = 1), "has no argument")`; V3
`is.numeric()` on an NA that is the right answer. None is live: no
bin-1 assertion expects an argument-name refusal (grep of the ten files
for "unused argument", "has no argument", "is missing"), and the pass
count moved by exactly 394 and 698.

**MINOR R5. The five emmeans rows are a recorded limitation, not a
refusal of a supported model.** frmtmb's own compatibility matrix
declares them: `R/compat.R:1356` "Refused: emmeans support is
univariate-only for now" and `:1612` "Refused: emmeans support needs a
linear mu predictor", enforced by `emm_mu_linpred()` in
`R/interop.R:118` to `:126`. `emmeans:27` and `:50` ask for
`nlpar = "a"`, and the support reads only `mu`. By the lane's own
definition (absent: a feature frmtmb lacks, transferable if built)
these are cannot transfer. `emmeans:21` and `:24` are different: the
`mo()` refusal is declared nowhere and is a bug, so they are rightly
defects. The round-0 precedent, multivariate `fitted()` and
`residuals()` as defects, rested on the user's rule 3, which does not
name emmeans. The defect worth filing here is that emmeans swallows
frmtmb's reason into "Perhaps a 'data' or 'params' argument is needed".
If the coordinator keeps the worker's reading, the worker's totals
stand. Under this reading they are 192, 112, 34, 12, 144, as in round 0.

**MINOR R6. The helper-copy identity is asserted by a dev script, not a
test.** It lives in `dev/brmsport-guards.R`, and neither package's
suite checks it, so drift is caught only when someone runs that script.
Either move it into frmtmb.sample's tier file or say so in the record.

**NIT.** The reflowed header of `dev/release/run-gated.ps1` breaks
mid-sentence ("One R process per" / "file.").

### Claims that held

- Every round-0 MAJOR is closed as section 10 says: the regression claim
  is gone and priorform's substitution is named; S1 and S8 are
  rediagnosed; 394 and 698 are rejected by rules; the citations and the
  three user rules are applied row by row; the spellings are defects.
- The new rules fire on H1 to H7 and on the reviewer's controls, and
  their stated controls pass (`guards.txt`, 36 of 36).
- The own-words verdict rejects a different error (`brm:81`), no error,
  an argument refusal (O4) and a stale verdict where brms's own pattern
  holds.
- The tier is green on the release path with the claimed counts, and the
  gated glob throws on an empty match.

# Extension gaps: the plan

Date: 2026-09-08, re-read against 0.55.0 the same day. frmtmb 0.55.0;
frmtmb.eam 0.5.1, frmtmb.sample 0.3.2, frmtmb.latent 0.2.1,
frmtmb.learn 0.2.1, frmtmb.ode 0.1.1, frmtmb.spline 0.4.0,
frmtmb.coupling 0.2.0.

Source: a survey of `extensions/` against what a practitioner in each
field needs for a paper. The survey found that every extension is a
correct likelihood with a thin workflow around it, and that the same
four things block real use in every package: no evidence at realistic
scale, hierarchy exercised only at the one-intercept level, no path for
real data in, and a post-fit surface that refuses more than it answers.
This document orders the work to close that, phase by phase, with a
check and a size for each item.

Scope: the extensions. An item that cannot be built without a change
under core's `R/` is not built in an extension; it is listed once in
"Core seams" with the extension item that waits on it. No extension
reaches core with `:::`.

## Rules every item is held to

1. **Measure before building.** Phase 0 is a gate. Any item whose
   cost the survey could only guess at is re-ranked after Phase 0's
   numbers, and this document is edited to say so.
2. **The shipping bar for a family or a method** is the one the
   extensions already hold themselves to: an identity against an
   independent reference at the same estimates, a recovery table at
   the realistic scale in the table below, refusals by name for what
   it does not do, compat rows for what it composes with, and a timing
   taken in a fresh process with both arms warmed and the seed count
   stated (`extensions/frmtmb.eam/dev/dev-findings.md`, "How this
   package times things"). Since 0.55.0 it also includes the hazard-container guard: a new package or a new `R/` file is covered by `frm_hazard_reads()` in that package's own `test-bracket-access.R`, because a `$` read on a hazard container used to reach the release tally before anything caught it. And a timing is not a timing until the instrument has been checked: two claims in the 0.55.0 round measured below `proc.time()`'s 10 ms tick or outside the hot path, so interleave the arms in one process and carry a control that must report 1.0.
3. **A silent wrong answer outranks a refusal, and a refusal outranks
   a missing feature.** Where a phase has both, the silent one goes
   first.
4. **A data-shaping helper is not model machinery**, and that is a
   reason to write it, not to leave it out: it is where a user with
   real data stops.

### Realistic scale, per field

These are the designs Phase 0 times and Phase 2 recovers at. They are
what a typical paper in the field collects, not a stress test.

| package | design | rows |
|---|---|---|
| eam | 30 subjects x 400 trials, 2 conditions, random effects on drift, boundary and non-decision time | 12,000 |
| learn | 100 subjects x 200 trials, correlated random effects on every parameter | 20,000 |
| latent, hmm | 50 sequences x 500 steps, K = 3, random effect on a transition | 25,000 |
| latent, lca | n = 2000, 10 items, K = 4, two covariates on membership | 2,000 |
| ode | 100 subjects x 8 samples, a depot and a central state, twice-daily dosing for 7 days | 800; 14 dose events per subject and, with one `ss` row at the default `n_ss = 20`, about 35 solves per group per gradient |
| spline, curves | 40 subjects, per-subject `fs` curves, 200 points each | 8,000 |
| spline, RP | 2000 subjects, 40 percent censored, one frailty | 2,000 |
| coupling | 40 subjects x 2 conditions x 60 frequencies, smooth in frequency plus random effects | 4,800 |
| sample | a 2000-row GLMM with two crossed factors, 4 chains x 2000 | 2,000 |

## Phase 0. The measurement tier

DONE, 2026-09-08. One fit per package at the scale above, in a gated
test tier (`FRMTMB_SCALE_TESTS=true`) that lives in each package's
`tests/testthat/test-scale.R`, recording tape build, one gradient, the
whole `frm()` call including `sdreport()`, peak memory and the estimate
against the truth. The numbers are in `dev/scale-findings.md` as one
table, how the run was made is in `dev/scale-lane-notes.md`, and each
package's `dev/scale-row.md` points at its own row.

| package | fit | decided |
|---|---|---|
| eam | wiener with the three random effects; a second run with `variability = "sv"`; a third on the same data with the ndt bound lifted | MINUTES, not hours: 309 s over three passes. The bounded ndt link does NOT tolerate a random effect. 20 of the 30 subjects have a true `ndt` above `min(rt)`, which is the bound; the plain arm does not converge (gradient 1.25e11, seven NaN standard errors) and the `sv` arm converges to the link's edge, coming back PINNED 0.42 of a standard error below the bound with a standard error of 7.2e-06 on a population `ndt` that is wrong by ten percent, with nothing from `diagnose()`. Lifting the bound on the same data recovers everything and finds a log-likelihood 121.4 units higher at the same parameter count |
| learn | bandit2arm_delta with `(1 \| p \| id)` on both parameters, then rlddm the same way | the recursion scales: the delta learner is 7.0 s at 20,000 rows and recovers. rlddm is 12.3 minutes over two passes, 738 s against a 600 s rule, and fails the eam failure because it takes its diffusion from frmtmb.eam |
| hmm | K = 3 gaussian with `tr12 ~ (1 \| id)` | the post-fit passes are NOT a cost: over three interleaved rounds `hmm_probs()` is 0.10 s and `hmm_viterbi()` 0.06 s against a 118 s fit. The tape build is 0.84 s at 25,000 rows, near where `dev/hmm-feasibility.md` put it |
| lca | K = 4 | cheap, as expected: 0.54 s |
| ode | the design above with `ii`/`addl` and one `ss` row | NOT usable at population scale: 3948 s, 65.8 minutes, on 800 rows, with the truth recovered. The closed-form path is a prerequisite, not an option |
| spline | `s(t) + s(t, subject, bs = "fs")` at 40 subjects, then `frm_curve()` and `frm_curve_feature()` on it | the curve surface is NOT the cost, and it decomposes into a memoized joint-precision solve paid ONCE per fit by whichever curve call comes first, 0.48 s, and cheap calls after it: the simultaneous band 0.071 s, the feature search 0.0098 s, the pointwise band 0.0031 s, against a 6.0 s fit. The feature search's own cold cost is 0.40 s when it is the call that pays the solve. The number to carry forward is memory: 1.32 GB of process working set |
| coupling | five models on one 4,800-row design | seconds, not minutes, across the whole ladder. The survey's benchmarks ARE recoverable, from its own session transcript: its 52.5 s and 28.9 s fits are the ladder's top two rungs, so the promotion this row asked for is done, though the survey ran 0.53.0 under `load_all()` and this runs 0.55.0 installed so no speedup is claimed. Both rungs recover the contrast, 0.486 (0.451, 0.520) and 0.488 (0.415, 0.561) against 0.5, so the survey's 0.77 was noise. What the cheap rungs get wrong is the interval WIDTH, and adding `(1 \| id)` does not fix it: the contrast is within-subject, so only `(1 \| id:cond)` widens it, and without that term the interval is 53 percent too narrow |
| sample | 4 x 2000 on the GLMM, then `posterior_epred()` and `loo()` on the draws | the per-draw loops are NOT the cost: `posterior_epred()` over 4000 draws and 2000 rows is 1.01 s against 120 s of sampling, and the three calls item 4.6 rewrites are 2.2 percent of the time it took to produce the draws. Phase 4's caching is not needed before anything else |

Decision rule: a fit past ten minutes on its design moves that
package's cost item to the front of Phase 1. TWO packages breached:
frmtmb.ode at 3948 s, comfortably, and frmtmb.learn at 738 s through `rlddm()`, by only 1.23x once that row was reproduced as the procedure requires.
What moved is in Phase 1 below, and why is in
`dev/scale-findings.md`, "The decision rule, applied". Actual size: one
day of work and about four hours of runs.

## Phase 1. Small, high leverage

Each of these is under two days and removes a wall a user hits on day
one, EXCEPT the three Phase 0 moved here, which are larger and are at
the top because nothing behind them is worth doing first.

| item | what | where | check | days |
|---|---|---|---|---|
| 1.0a | DONE, and it beat its target. `ndt_group()` names the grouping and the bound becomes that group's fastest response; without the term the scalar bound stays in the LINK exactly where 0.6.0 put it, so an ungrouped model is bitwise unchanged, 0 ulp over 50 fixed-parameter probes across all five families. At the eam scale row, seed 20260908: log-likelihood -7003.01 against a target of -7027.4 and a baseline of -7148.8, convergence 0 at a maximum gradient of 9.87e-04, positive definite Hessian, 0 of 7 NaN standard errors, `sd(ndt)` 0.02543 against a truth of 0.02629, and 30 of 30 subjects below their own fastest response with 27.3 ms to spare. The bound-lifted arm reproduces Phase 0's -7027.37 and measures the row's own "22 ms" claim at 21.9 ms. `ndt_time()` reads the value in seconds. `gddm()` keeps the scalar bound and refuses `ndt_group()` by name, on SCOPE: it reads every dpar at the first row of its condition | `frmtmb.eam/R/ddm-shared.R`, `wiener-family.R` | met | 1.5 |
| 1.0a-note | The acceptance criterion "recovers `sd(ndt)`" is met IN APPEARANCE and Phase 2 should not lean on it. An estimator with the random effect on `ndt` switched off returns 0.02748 against a truth of 0.02629 where the shipped model returns 0.02543, and at 100 trials per subject the two agree in every digit: the observed floors carry the spread. What DOES separate the model from its floors is the per-subject error, and it is the argument for this item: the per-group arm halves with more data, 20.91 to 14.08 to 7.67 ms at 100, 200 and 400 trials, while the global bound's does not, 27.23 to 20.51 to 30.37. The global bound does not converge on the truth as data accumulates. Phase 2 should assert the per-subject error and the log-likelihood, not `sd(ndt)` | | | |
| 1.0b | DONE. `rlddm()` takes `ndt_group()` through a six-function seam exported from frmtmb.eam, whose contract stops at the bound: `ndt_bound()`, `ndt_bound_pending()`, `ndt_bound_attach()`, `ndt_bound_of()`, `ndt_bound_key()` and `ndt_apply()`. `ddm_ndt_install()` stays internal deliberately, because it rewrites a family's slots by name. The learn-rlddm scale row goes from convergence code 1 at a maximum gradient of 6.36e+09 with a non positive definite Hessian and four NaN standard errors, to code 0 at 3.12e-03 with a positive definite Hessian, no NaN, and 100 of 100 learners below their own fastest response with 20.9 ms to spare. The floors do not hand it that: per-learner error is 13.996 ms at a correlation of 0.940, against an analytic bound of 21.075 ms and 0.848 for the best constant fraction of each learner's own floor, and 22.107 ms for the same model with the component off. BREAKING: a constant `ndt` outside the link's range is refused at parse rather than reaching the optimizer as a NaN; the capability is respelled, not lost. Two measurements matter more than the feature. `sd(ndt)` is WORSE than doing nothing, 0.0334 against 0.0380 with the component off and a truth of 0.0393, so `?rlddm` says to read the per-learner error instead. And the tier's `cor_true` was 0 when the truth in the fitted parameterization is 0.776847 at that seed, computable per replicate with no fit; see the warning added to item 2.2 | `frmtmb.learn/R/rlddm.R`, `frmtmb.eam/R/ndt-seam.R` | met | 0.5 |
| 1.0c | DONE. `frm_lincmt()` is the analytic one- to three-compartment solution with the same `events` grammar, refusing by name what superposition cannot carry. On the Phase 0 design, whose `frm_ode()` arm reproduced at 3716.6 s against this row's recorded 3948 s, the closed form fits in 55 s at its default and 119 s with the run-in written out: at least 67x and at least 31x, taking each arm's slowest measurement over two passes against the other's fastest, which is what the design supports because its arms run one after another rather than interleaved. At 5 and 25 subjects, where `nlminb` takes identical iteration counts in every arm, the factors are 95x and 47x, and 89x and 39x. The load-independent count is 33 `RTMBode::ode()` calls per subject per objective evaluation against 0. Every estimate matches `dev/scale-findings.md` to seven significant digits, and `nlminb` returns code 0 at 4.7e-04 where the solver arm returns code 1 at 6.1e-03. The identity holds at 2.6e-12 of the trajectory's scale over 118 schedules against `frm_ode()` at `atol = rtol = 1e-12`, with the review's own 104 random schedules and 18 corners agreeing at worst 1.8e-11; against a 240-bit reference the error relative to the trajectory's maximum is 3.6e-10 on a seven-decade rate box, every coalescence swept to exact equality. It found no defect in `frm_ode()`'s arithmetic and one in its DEFAULT, which is item 1.0d. It also unblocks sampling: `frm_sample()` refuses `frm_ode()` and runs on `frm_lincmt()` | `frmtmb.ode/R/lincmt.R` | met | 3 |
| 1.0d | DONE, and not by raising the default. `frm_ode(ss_extrapolate = TRUE)` sums the geometric tail from the run-in's own last differences, which is arithmetic on tape variables and so runs DURING a fit at whatever parameters the fit has reached, at no extra solve. Worst error over a dosing interval against the exact limit, at the shipped `n_ss = 20`: 3.9e-03 to 9.9e-09 at a 23 h terminal half-life, 1.2e-02 to 2.6e-09 at 107 h, 2.1e-01 to 1.4e-08 at 670 h. The reason this was a wrong ANSWER and not a slow one is the gradient: at the 107 h design `d/dlog(k21)` from the truncated tape is +10.37 where the exact limit is -1.30, the wrong SIGN, and truncation manufactures precision on `lk21` by shrinking its standard error up to 41.6x while inflating `lke`'s up to 8.3x. The reviewer's `"auto"` proposal was measured and REJECTED: at the point `frm()` actually starts a nonlinear model it picks 7 cycles where the worst subject needs 169, and floored at 20 it is still dominated, reaching the integrator's own floor in 135 solves where this reaches it in 21 and truncation needs 201. The correction stands down smoothly because a fit walks there; both junctions read the curvature `2 / (1 - r)^3` to five figures, the same law the shape reads where there is no junction at all. It costs 258 tape nodes; `ss_extrapolate = FALSE` is 0.3.0 node for node. The warning now reports a distance rather than the cycle-to-cycle movement, and `?frm_ode` says plainly that it DETECTS rather than measures. Found on the way: past about `n_ss` = 1000, chained-solve error at `atol = 1e-8` contributes more than the extra cycles remove, so the docs say to tighten tolerances alongside it | `frmtmb.ode/R/ode.R` | met | 1 |
| 1.0e | DONE. `gddm()` refuses a distributional parameter that varies inside its condition. `gd_densities()` read every dpar at the FIRST ROW of its condition, so adding 100 to a covariate on 118 of 120 rows left the objective BITWISE identical for `mu`, `ndt`, `bs` and `bias`. It reached the random effects too: with a subject factor crossing the index, 2 of 4 subject deviations came back bitwise 0 and the fit reported `sd(s) = 7.6e-11` with no error and no warning, which a user reads as a finding about their data. Refusing rather than reading, because making the density read it is one solve per row instead of one per condition, and the count needs no clock since solves per evaluation IS the condition count: 60x. 0 false alarms over 21 designs the field writes, 26 of 26 on the paired drop arm, each naming the parameter, the variable and the user's own condition label. The tolerance is per pair plus a floor, because an exact comparison false-alarms on `poly()`, which returns rows 391 ulp apart for bitwise identical inputs; scaling the whole tolerance by the column maximum was tried and rejected in review because it made the answer depend on whether the user had centered. Residual band, stated as measured: a difference `s` on a column of maximum `M` is missed once `M / s` passes about 1e11, which neither search reached from a real covariate. Applying the floor only to computed columns closes that band and was built, measured and DECLINED: it false-alarms on 3 of 21 correct designs, because a precomputed `poly()` stored under a bare name carries exactly the noise the floor exists for. `gddm_simulate()` had the same defect and now refuses too, 5 of 5 caught where the previous build caught 0 | `frmtmb.eam/R/gddm.R` | met | 1 |
| 1.1 | DONE, and not as written. `frm_sample(control = list(adapt_delta =, max_treedepth =))` takes brms's spelling AND brms's meaning, and the fit-time options move to `fit_control =`. The `stan_control` of the original row was rejected: brms compatibility is a locked goal (SPEC.md section 5, "brms code ports mechanically"), `control` IS the Stan control in brms, and a second name would have left `vignette("brms-posterior")`'s "no route" row standing with a footnote instead of removing it. Putting the sampler options in `frmtmb_control()` was also considered and rejected: core has no Stan dependency by design (SPEC.md section 3) and `frmtmb_control()` validates eagerly, so core would validate options for a sampler it does not know, and `frm()` would accept `adapt_delta` and ignore it. The rename is breaking; the old shape is refused by name, keyed on the `frmtmb_control()` field names, which share none of rstan's fifteen | `frmtmb.sample/R/sample.R` | divergence count on a centered funnel falls between 0.8 and 0.99; `nuts_params()` reads the tightened step size back | 0.5 |
| 1.2 | DONE as written, except that the patches are prepared for filing rather than filed. `frm_sample()` reads the registry rows that name it with status `refused` and stops before taping; `frmtmb.ode` registers `frm_ode()` as a feature and `frm_ode() x frm_sample` as `refused`. The breakage was re-confirmed on 0.55.0 against RTMBode at commit 5242257, which is still the only build available. What the pre-flight can match against a model is a formula call and a family name; a refused row it cannot match warns rather than passing. `?frmtmb_register_compat` now promises that a `refused` row is consulted, which is a documentation change in core. The three patches are current and unapplied; the filing text is in `dev/stanctl-findings.md` | `frmtmb.sample/R/sample.R`, `frmtmb.ode/R/zzz.R` | an ODE fit refuses in under a second with no Stan call; the eam and latent rows stay `conditional` | 1 |
| 1.3 | DONE. `frm_curve(object, newdata, contrast = newdata2)` returns the difference with pointwise and simultaneous bands, and `frm_curve_feature()` finds its zero crossing. The gratia identity holds and the estimate gap is provably the fit rather than the arithmetic (5.55e-16); simultaneous coverage 0.970 over 200 seeds against a binomial mcse of 0.0154. An exact `gp()` is answered where the kriging residual is the same random variable, decided on the DESIGN rather than the numbers, and refused otherwise. It uncovered a pre-existing one-grid defect: `sp_sim_crit()` standardizes by a variance it did not draw from, so a simultaneous band over an exact `gp()` off the observed positions is at least 17 percent too narrow. That waits on the same core seam as the general cross-covariance | `frmtmb.spline/R/curve.R`, `curve-cov.R`, `curve-feature.R` | done | 1.5 |
| 1.4 | DONE. `valid_y` warns above 20 s across all five eam families and names milliseconds. 0 false alarms over 192 designs a two-choice task produces. Where it CAN fire on a correct model the rate is reported with the fastest response beside it, because the rate is not a function of the median: at about 50 s it spans 0.185 to 0.935 depending on drift | `frmtmb.eam/R/ddm-shared.R` | done | 0.25 |
| 1.5 | HALF DONE, and the half that is missing is a core seam. `reward(pay1)` is refused by CORE: `frmtmb_register_aterm(arity =)` holds one integer and the parser compares for equality, so registering at 1 would refuse the two-column spelling every example writes. Filed as a min-and-max arity, whose shape is settled by a measurement: under a plain range the derivation makes one group of three and the check skips for the TWO-column spelling as well, undoing this item's own fix. What WAS buildable closed a silent wrong answer: a duplicated schedule fits identically and simulates a different experiment, and is now refused across all eight families from a derivation rather than a declaration | `frmtmb.learn` | done | 0.5 |
| 1.6 | DONE. `frm_cross_spectrum()` splits at NA runs, so an artifact-rejected record is one call, and takes `window = "hann"`, measured to cost no degrees of freedom (8.021 against 8.008 at nominal 8) and identical to `stats::spec.pgram()` to 4.8e-16. Hann with `smooth` is refused, measured, because it delivers 4.67 to 5.66 where it claims 8. Recorded on the way: the cross-row correlation the docs understated is 0.397 at the default and 0.753 at `tapers = 4`, one independent frequency in four, though shipped `n` itself is right | `frmtmb.coupling/R/cross-spectrum.R` | done | 1 |

**Phase 1 is complete.** About 13 days of work against an estimate of
10, the overrun being the three items Phase 1 did not start with: 1.0d
and 1.0e were found by 1.0c and 1.0a rather than planned, and 1.0b's
export seam turned out to be a design decision rather than a rename.

Rule 3 ordered this phase from beginning to end, and it kept being
right. Every one of the three unplanned items was a SILENT WRONG
ANSWER found while building something else, and each outranked the
feature work that was already queued:

- 1.0a's `sv` arm converged, reported no warning, and gave a population
  non-decision time wrong by ten percent with a standard error of
  7.2e-06 on it. That arm now converges to 0.2479 against a truth of
  0.25 with 30 of 30 subjects inside their own floor.
- 1.0d's steady-state truncation was never reported to a fit at all,
  and biased every group in the same direction. Its gradient had the
  wrong SIGN, and it manufactured precision on the parameter a
  pharmacokineticist cares most about.
- 1.0e's first-row read reached every dpar of `gddm()`, including the
  random effects, where it could collapse a variance component to
  7.6e-11 with nothing printed.

None of the three was in the survey that produced this plan. All three
were found by measuring something adjacent.

The 0.55.1 disclosure in `frmtmb.eam/NEWS.md` telling users not to put
a random effect on `ndt` is discharged, and so is the `gddm()` note the
Phase 1 prose used to carry as an unnumbered item: it is 1.0e.

## Phase 2. Hierarchy: validate what is claimed

No new features. Each row is a recovery table and a third-party
identity at the realistic scale, and the result is written into the
family's help page whatever it says.

| item | what | reference | expected trouble | days |
|---|---|---|---|---|
| 2.1 | DONE. The hierarchical Wiener recovers at 30 x 400. Over 60 replicates every fixed effect and both variance components recover with no Wilson interval excluding 95: `mu` intercept 0.40191 at 90.0 percent, `mu` condition 0.90037 at 95.0, `bs` 1.38996 at 93.3, `sd(mu\|s)` 0.34743 at 91.7, `sd(log bs\|s)` 0.18978 at 90.0, all 60 converged with a positive definite Hessian and every subject inside its own floor. What the count resolves is stated with it, ONE-sample against a nominal rate known exactly: power 0.394 at a true 0.90, 0.591 at 0.88, 0.815 at 0.85, so it detects ten points and not seven. 80 percent power is first reached at n = 180 and sustained only from n = 202, because the rejection region advances in whole counts, and that sawtooth is paid for in SIZE, 0.0374 against 0.0259. Neither component is separately biased: one common ML shrinkage of 0.974716 covers both. The three carry-ins all settled and two went against expectation. `sd(ndt)` is the floors multiplied and it is an IDENTITY, not a measurement over 30 replicates. The constant-`ndt` design's 23.6 ms bias reproduces at -24.3 ms and is NOT the bound's, the paired difference between parameterizations being 0.238 plus or minus 0.066 ms. And the `sv` thread came back NEGATIVE: an early `se/sd` of 0.694 at 14 replicates is 0.913 and 0.984 at 60, and 0.694 sits at the 0.3rd percentile of 20,000 random 14-subsets, so it was an unlucky prefix rather than a noisy statistic. THREE DEFECTS in shipped code, all filed or fixed: the tier recorded a non-decision time times its own bound; the tier's own assertion failed on 1 seed in 8 by scoring against a median; and `sv` collapses to the log link's floor on 1 fit in 60 with `diagnose()` silent | `frmtmb.eam` tests and `?wiener` | met, and it found more than it was sent for | 1.5 |
| 2.2 | DONE, and it answered a question the row did not ask. `(1 \| p \| id)` recovers: bandit2arm_delta returns 0.5322 against a true correlation of 0.5 and 0.0214 against a true 0, with 120 of 120 converging; rlddm over 60 replicates has 0 fits below the objective at their own realized truths, 0 NaN standard errors, and NINE OF NINE components with a population truth covering at 0.90 to 0.98. Exactly two fail, `sd(ndt)` and `cor(bs, ndt)`, and the cause is an EXACT IDENTITY rather than an estimator fault: `floor_i = ndt_i + m_i`, so `qlogis(ndt_i/floor_i) = log(ndt_i) - log(m_i)`, checked at 1.9e-16. The fitted deviation is the log non-decision time minus the log of an OBSERVED MINIMUM. `sd(log ndt)` is 0.1494 against a drawn 0.15, recovered exactly, while `sd(log m)` is 0.4404, so the link-scale component is about 94 percent a property of the data. The same term correlates +0.872 with each learner's `bs` deviation, which is what pins `cor(bs, ndt)` near -1; strip it and `cor(bs, log ndt)` is +0.2989 against a drawn 0.3. More trials do not repair it: the nuisance share is 107.9, 106.7 and 108.2 percent at 100, 200 and 400 trials, paired difference +0.0023 at p = 0.895. ONE CAUSE, TWO SYMPTOMS: the same term takes the covariance toward singularity, which is why the positive-definite check fires on the five fits with the HIGHEST log-likelihood surplus over their own oracles, 23.741 against 17.193. Three candidate explanations were measured and refuted on the way and the retractions are on the page. The contrast worth carrying: the Stan identity holds at 4.9e-15 relative at the full design while the same model's link-scale component is 94 percent nuisance, so the estimator is exact to machine precision at computing a thing that is not what a reader assumes | `frmtmb.learn` tests, `?rlddm`, Stan programs | met, and the row's own warnings 1 and 2 now have algebra instead of observation | 1.5 |
| 2.3 | DONE. hmm recovers on both arms. K = 3 gaussian over 40 replicates: coverage 95.6 percent at 459 of 480, state means within 0.021, transition matrix within 0.014, identity against depmixS4 over 3 replicates at 3.1e-14 to 4.9e-12 relative. `tr12 ~ (1 \| id)` over 15 replicates: coverage 94.4 percent at 184 of 195, `sd(tr12\|id)` 0.615 plus or minus 0.067 against 0.6, identity against hmmTMB over 6 replicates at 6.1e-12 to 6.9e-11. The expected trouble was real: the cold start sits 8.099 units under the optimum with convergence 0, a positive definite Hessian, and `diagnose()` printing "No convergence problems detected" verbatim, and `frm_allfit()` reaches that same wrong optimum on all four optimizers with a spread of 7.8e-07, so agreement between optimizers is not evidence here. `hmm_starts()` ships and recovers from it on 5 of 5 seeds at jitter 1 and above, 1 of 5 at 0.5. It costs 175.7 s per refit at this scale, so `?hmm` recommends no default `n` and says why. A comparison trap worth carrying: hmmTMB's `initial_state = "estimated"` fits one initial distribution PER SEQUENCE where frmtmb fits one shared, 38 extra parameters and 19.678 units on 20 sequences, with nothing saying so | depmixS4, hmmTMB | discharged, and the remedy ships | 2 |
| 2.4 | DONE, and the row's expectation was WRONG. "None expected" was false: on 8 of 200 replicates at K = 4, n = 2000, the 0.2.2 starting rule reached a local optimum 243 to 284 log-likelihood units below `poLCA(nrep = 10)`, and SEVEN of the eight passed the pair this plan's own scale tier asserts, a positive definite Hessian and a relative gradient under 1e-3. The cause was the START and not the surface on at least 2 of the 8: there all 10 of 10 poLCA single random starts found the global optimum and none ever visited frmtmb's mode. The old rule cut subjects on the mean of their item codes, and on this design every class endorses three of ten items at 0.85 and seven at 0.2, so that score explains 0.000814 of its own variance between the true classes: blind by construction. `lca()` now clusters on the response pattern and shrinks each class profile toward the pooled one, reaching poLCA's optimum on 200 of 200 of the block its one constant was tuned on AND on 200 of 200 of a second block of fresh seeds. The shrink weight sits at 0.9 rather than the first value that worked, 0.5, which loses a seed out of sample by 256 units; the usable interval runs about 0.5 to 0.99 and 0.9 is its interior. Identity against poLCA at 4.3e-14 relative on the log-likelihood, 5.6e-07 on profiles, 2.2e-06 on gating coefficients. Two coefficients UNDER-COVER and there is no remedy yet: pooled over 400 replicates `class3:x2` covers 91.75 percent and `class4:x2` 90.75 against a nominal 95, both binary gating slopes, while the third and the other six contain 95. A profile interval was measured and does not help, 103 of 118 both ways, because the standardized error has no heavy tail and no curvature: the shortfall is in the MAGNITUDE of the standard error rather than the shape of the likelihood, and `?lca` says so rather than recommending something unmeasured | poLCA | measured, and it found a defect | 0.5 |
| 2.5 | DONE. `(1 \| centre)` agrees with rstpm2, and the spline spaces were proved to coincide BEFORE anything was compared: rstpm2's fitted eta regressed on the Royston-Parmar basis leaves at most 2.96e-14 relative over 200 replicates, and 1.59e-02 when a knot is moved. Recovery over 200 replicates at 2000 subjects in 40 centres: `beta` 0.5984 against 0.6 at 93.0 percent, `sd` 0.4905 against 0.5 at 92.0, and NEITHER is under-coverage, since at 800 replicates they are 0.9450 and 0.9425 with the 600 unseen seeds giving exactly 0.9500 each. The `sd` point estimate IS low at 4.8 mcse and it is ordinary ML bias, `0.5*sqrt(38/40) = 0.4873` against 0.4889. Agreement with rstpm2: `beta` to 3.2e-04 of the run's own standard error, `se(beta)` to 2.6e-06. The 0.064 log-likelihood gap between the packages is ENTIRELY the integration rule, adjudicated by a third likelihood belonging to neither: frmtmb's Laplace is 0.058 low, rstpm2's 9-node quadrature 4.9e-06 low, and the offset CHANGES SIGN at 4 per centre, reaching +1.38. `gamma1 ~ (1 \| centre)` recovers at 0.1994 against 0.2 and is anchored at `t = 1`, which is a UNIT: a 365x rescale moves the slope-only fit 8.19 log units and drops its `sd` from 0.2201 to 0.0370, where the paired block moves 4.26e-08. DEFECT FILED, NOT FIXED: `rp_floored()` tests only uncensored rows, so a centre with NO events can be driven to a decreasing hazard with nothing reporting it, and one seed gives a fitted survival RISING from 4.16e-50 to 0.774 with convergence 0 and no warning | rstpm2 | met, and the smoke test is replaced by a measurement | 1 |
| 2.6 | DONE, and the claim held with one correction. Over 148 replicates at the realistic design the correct model covers 141 of 148, 0.953 (0.906, 0.977), with a standardized error of 1.062 where 1.0 is an honest interval, and recovers both components, `sd(id)` 0.341 plus or minus 0.049 and `sd(id:cond)` 0.197 plus or minus 0.027. The survey's model covers 78 of 148, 0.527 (0.447, 0.606), and it is the WIDTH: paired on the same data its estimate sits 0.0036 from the correct model's, its interval is 0.37 of the correct one's, and on 63 of 148 replicates it missed where the correct model covered while never covering where the correct model missed. Coverage predicted from location and width alone reproduces every rung to within Monte Carlo error. The correction, which the review had to make twice: the two rungs with NO RANDOM EFFECTS are not a width failure at all. They estimate the MARGINAL contrast and estimate it correctly, sitting -0.0197 and -0.0133 from the correct model's paired, against -0.0196 and -0.0135 predicted by the logit non-collapsibility factor `1 / sqrt(1 + 0.346 V)` from the generator's constants with nothing fitted; `dev/scale-findings.md` recorded that attenuation at 0.016 across the ladder before this row ran, and a wider interval is not its remedy. The addition: dropping `(1 \| id)` and keeping `(1 \| id:cond)` goes the OTHER way, 1.90 times too wide at 148 of 148, so a within-subject contrast needs BOTH terms and neither "more random effects" nor "fewer" is the rule. A null arm of 36 replicates with `sd(id:cond)` set to 0 is the control the width comparison needs: the two models then agree to a median paired width ratio of 1.000 and the extra component is estimated at 0.015 plus or minus 0.018. Phase 0's "53 percent narrower" reproduces exactly at its own seed, 0.068695 / 0.145668 = 0.47159 from that document's own ladder table, and understates the typical case, which is 62.6 percent. The suite gains a small-design version of the comparison, a PAIRED width quotient that fired on 0 of 40 seeds, which is what bounds its false-alarm rate: below 0.072 a seed and below 0.26 for the minimum of four the test takes, at 95 percent confidence assuming no shape. A lognormal fit to those same 40 reads far rarer, about 1 in 2,200, and a normal fit to them reads 1 in 90, so the small number is the fit's and not the run's. Its unpaired first draft was rejected because it passed on data with the effect absent. The scale tier now records `coh_cond_se` | the simulator's truth | discharged; `dev/coh-findings.md` | 0.5 |

**Phase 2 is complete.** About 7.5 days against an estimate of 7, run as
four parallel lanes over four packages. Every row's claim held, and
three of the six rows found something the row was not sent for: item
2.4's "none expected" was false, item 2.2's two failing components
turned out to be an exact identity rather than an estimator fault, and
item 2.1 found three defects in shipped code while proving a claim that
was true.

Two lessons the phase paid for and the next one should not relearn.
**An early signal at a small count is not a finding**: two dissolved,
a coverage of 3 of 6 that became 0.85 at 20, and an `se/sd` of 0.694 at
14 that became 0.984 at 60 and sat at the 0.3rd percentile of its own
subsets. **And a target can be the biased quantity**: item 2.2 spent
most of its mechanism hunt asking why an estimate sat below a target,
and the answer was that the target was 94 percent nuisance.

## Phase 2.5. frmtmb must not break brms

Filed 2026-09-14, from the user's own session. This is ahead of Phase 3
because it is not a compatibility gap: **attaching frmtmb damages brms
for objects that already exist**, whether or not the user ever calls
frmtmb. By the rule that has ordered this plan from the start, a silent
wrong answer in shipped code outranks feature work.

Reproduced: `library(brms); library(frmtmb)`, then on an existing
`brmsfit`, 15 of 17 generics tested return "no applicable method", and
`getS3method("conditional_effects", "brmsfit")` cannot find the method
at all. 27 exported names collide; brms has methods on 24 of them.

The mechanism is that frmtmb defines its OWN S3 generic for each name
and exports it. Attached last, frmtmb's generic wins the search path,
and `UseMethod` then consults frmtmb's method table, which has no
`brmsfit` entry. brms's method is still registered, in brms's table,
and is unreachable from frmtmb's generic.

| item | what | where | check | days |
|---|---|---|---|---|
| 2.5a | The mechanical half, and it is bigger and cheaper than first filed. frmtmb defines **30** exported S3 generics; 28 collide and 2 are its own (`latent_probs`, `par_template`, no action). Ownership audited with `parseNamespaceFile()` across every installed package, NOT with grep, because a one-name-per-line grep missed nlme's multi-name export block and produced a wrong answer first time. **`nlme` owns `fixef`, `ranef` and `VarCorr` and is a RECOMMENDED package, so importing it costs nothing**, and this is exactly what lme4 does: `importFrom("nlme", fixef, ranef, VarCorr, ...)` then `export()` then `S3method()`. **`generics` owns `refit`** and is near-weightless. For the other 17, `posterior` owns 11 (`as_draws*`, `variables`, `nchains`, `ndraws`, `niterations`, `nvariables`), `loo` owns 3, `rstantools` owns 2 and `bayesplot` owns `pp_check`: register onto those by DELAYED registration in `.onLoad` rather than importing, both because they are meaningless without their owner and because bayesplot pulls ggplot2, which is too much weight for a hard Import. Vendor the public-domain `s3_register()` helper rather than depending on rlang | core `NAMESPACE`, `R/zzz.R`, `DESCRIPTION`; audit which package defines each name, since several are sampling concepts that may belong in frmtmb.sample | with brms attached FIRST and frmtmb second, every brms method on every listed generic still dispatches; with each owner absent, frmtmb still loads and the rest still work | 1 |
| 2.5b | The six brms owns alone: `conditional_effects`, `hypothesis`, `expose_functions`, `posterior_summary`, `LOO`, `WAIC`. frmtmb cannot depend on brms, which has `rstan` in hard Imports and is the founding constraint of this package. Try conditional registration: when brms is loaded, register frmtmb's method onto ITS generic and do not export a rival; when it is not, frmtmb provides its own. The hard part is that export lists are static, so this needs care and a test in both directions. If it cannot be made to work cleanly, the fallback is a distinct spelling in frmtmb, which is honest but breaks the mechanical-port promise on the functions users reach for first, and that trade is the user's to make rather than a lane's | core `R/zzz.R`, `NAMESPACE` | both load orders tested, in separate processes, with an existing `brmsfit` and an existing `frmtmb_fit`; the test must FAIL against today's code, seen failing | 1 |
| 2.5c | RESOLVED, and it was never the decision it looked like. The first filing said `fixef`, `ranef` and `VarCorr` had only lme4 as an owner and put a choice to the user between importing lme4 with Rcpp, RcppEigen, minqa, nloptr and boot behind it, or keeping the collision. **That was wrong: it rested on a grep that could not see nlme's multi-name export block.** nlme owns all three, is Recommended, ships with R, and is what lme4 itself imports them from. So all three become `importFrom(nlme, ...)` at zero weight, and the fix is strictly better than either option offered, because frmtmb, lme4 and brms then share ONE generic rather than frmtmb merely getting out of brms's way. The residue is a single function: **`ngrps`**, whose only owner is lme4. Decide it in the lane by measurement rather than in advance, and note it is one low-traffic name rather than three core accessors | core | the decision recorded with its reason | 0 |
| 2.5d | The scale contract, which is a separate defect with the same root: nothing says which scale a method reports. Measured on one lognormal fit: `predict()` returns 7.98 where brms returns 3155, because frmtmb gives the linear predictor and brms gives the predictive mean; the ratio confirms it at `3155.683 / exp(7.979832) = 1.079`, which is `exp(sigma^2/2)` at sigma about 0.386. `summary()` reports `sigma` on its log link where brms back-transforms. Neither is wrong internally and both silently contradict the ported-code promise. Write the contract down per method, then TEST THROUGH THE GENERICS rather than through internals, asserting the scale each one returns | core `R/methods-fit.R`, `?predict.frmtmb_fit` | a test per generic that pins the scale, each seen failing against a deliberately wrong scale; a lognormal fit compared against brms on the response scale to the tolerance the design supports | 1.5 |

| 2.5e | DONE at frmtmb 0.58.0. brms's spellings are adopted and the lme4 aliases are removed outright. Every S3 method taking `...` calls `frm_check_dots()`, which tolerates only the arguments the GENERIC documents. That table is derived from R's own package sources rather than written by hand. `fitted(fit, re_formula = NA)` now returns the population-level fit. One regression surfaced outside the lane, in frmtmb.learn: `simulate()` had silently dropped `newdata`, and a test asserted the swallow. See `dev/suite-baseline.md`. The original row follows. **A silent wrong answer on the brms porting path, and a naming policy to prevent the class.** The user set the rule on 2026-09-15: where lme4 or glmmTMB and brms disagree, **brms is the tiebreaker**, and frmtmb.sample should match brms even more closely, down to output. The measured defect: `fitted.frmtmb_fit` takes only `...`, so a ported `fitted(fit, re_formula = NA)` is SWALLOWED and returns the CONDITIONAL fit, 1.31023 at the first row, identical to `fitted(fit)`, where the population-level answer the user asked for is 1.74231, with no error and no warning; a misspelled argument is swallowed the same way. It is the "match both" drift made concrete: `predict()` pairs `re.form`, lme4's spelling, with `allow_new_levels`, brms's, in one signature, while `conditional_effects()` already uses `re_formula`, and `frmtmb.sample`'s `posterior_predict()` accepts BOTH, exactly as brms does. Audit every user-facing method's formals against brms's; adopt brms's spelling and drop the lme4 one outright, since nobody uses this package and whether a name already shipped is irrelevant; accept two spellings ONLY where brms itself does, as its `posterior_predict()` takes both `re_formula` and `re.form`, which is matching brms rather than preserving history; and REFUSE unrecognized arguments that fall into `...` rather than swallowing them. The refusal is the part that prevents the class: a method that swallows its dots will silently accept every future brms argument too | `R/predict.R` and every S3 method taking `...`; `frmtmb.sample/R/methods-draws.R` | `fitted(fit, re_formula = NA)` equals `predict(fit, re_formula = NA)` and differs from `fitted(fit)`, seen failing against today's code; a misspelled argument errors; a formals diff against brms per method, recorded | 1.5 |
| 2.5f | DONE at frmtmb.sample 0.6.0. `rhat()` and `neff_ratio()` are `identical()` to brms's on the same draws, and return frmtmb names. 60 calls through the generics differ from brms in 0 cases, and the 46 draws-side calls also differ in 0 cases. Twelve signatures moved. Still open: six more positional divergences, and `summary = FALSE` on `fixef`/`ranef`/`coef`/`VarCorr`, which return a summary where brms returns raw draws. Both are filed under 2.6c. The original row follows. **frmtmb.sample matches brms, decided by the user 2026-09-15.** Three findings, all measured, all to be FIXED toward brms. (1) `rhat()` and `neff_ratio()` ANSWER A DIFFERENT QUESTION. brms's `rhat.brmsfit` is `posterior::rhat`, rank-normalized split-Rhat; this package's is `bayesplot::rhat(stanfit)`, rstan's classic split-Rhat, matching rstan to the bit. Max relative difference 0.00563108 against a whole signal of `max|posterior - 1| = 0.0100318`, so **1.15x the entire excess over 1 the diagnostic reports**. `neff_ratio()` is the same defect and **73x larger, 0.412677**: brms uses `min(ess_bulk, ess_tail)/ndraws`, this uses rstan's `n_eff/total`, and it is the number a user reads to decide whether to sample longer. `nuts_params()` and `log_posterior()` are CLEAN BY CONSTRUCTION, since brms's own methods also delegate to bayesplot on the `stanfit`, so the blast radius is exactly two names. (2) Those same two return RAW STAN NAMES, `beta[1] beta[2] betad theta`, where every other draws accessor returns frmtmb's, so 4 of 11 entries disagree with `variables(ds)` and `rhat(ds)["x"]` is `NA`, against this package's own DESCRIPTION. **This is expected to fall out of (1)**: computing through posterior on the frmtmb-named draws produces frmtmb names, so verify it rather than doing separate work. (3) 10 of 28 methods diverge from brms at a POSITIONAL slot: `as.mcmc`(2), `log_lik`(2), `mcmc_plot`(2), `posterior_epred`(3), `posterior_interval`(2), `posterior_linpred`(4), `posterior_predict`(3), `pp_mixture`(2), `predictive_error`(2), `psis`(2). Eight error; **two answer SILENTLY**, `as.mcmc(ds, TRUE)` and `posterior_interval(ds, 0.9)`. Note the trade (1) makes and state it in NEWS: following brms moves two published diagnostics, where keeping rstan's keeps them consistent with `ds$stanfit` | `frmtmb.sample/R/methods-draws.R`, `R/loo.R` | the two diagnostics equal brms's on the same draws to the tolerance the design supports, seen failing against today's code; `rhat(ds)["x"]` is not `NA`; every one of the ten signatures accepts brms's positional call and gives brms's answer; and the two that answered silently now agree rather than merely erroring | 2 |

Phase 2.5 total: about 7 days, items 2.5e and 2.5f having been added
after the user set brms as the tiebreaker.

## Phase 2.6. Pass brms's own test suite

Proposed by the user on 2026-09-15. frmtmb should approximately pass
**brms's own test suite** nearly as-is, and frmtmb.sample even more
closely. This is a different instrument from what the repository has.
`test-brms-methods.R`, `test-brms-agreement.R`, `test-brms-likelihood.R`
and `test-brms-port.R` are frmtmb-authored comparisons against brms, so
they test what this project thought to check. brms's suite encodes
brms's contract as brms's authors understand it, which is exactly what
catches an argument named `re.form` where brms says `re_formula`, and
item 2.5e's silent swallow is the kind of defect it would have found.

| item | what | where | check | days |
|---|---|---|---|---|
| 2.6a | Get the suite and SIZE the job before building anything. It is not installed: CRAN's Windows binaries drop `tests/`, so it needs the brms 2.23.0 source. Then classify every test file into three bins, because only the first is cheap. (1) Transfers as-is: formula grammar, priors, family and data validation, argument names and return shapes of the post-processing interface. (2) Transfers with an ML adaptation: anything comparing numeric output, where brms reports a posterior and frmtmb an estimate with a standard error, so the assertion must be restated against a tolerance the design supports rather than copied. (3) Does not transfer: Stan code generation and anything that asserts on MCMC diagnostics. Report the counts per bin before any porting starts | `dev/brms-suite-audit.md` | a per-file classification with counts, and an estimate for bins 1 and 2 grounded in those counts | 1 |
| 2.6b | Port bin 1 as a gated tier, run as-is with the minimum of adaptation, and record every failure as a frmtmb defect, a deliberate divergence with its reason, or a test that cannot transfer. brms is the tiebreaker for the first two; frmtmb.sample is held to the stricter standard. **Sized by 2.6a: 494 assertions in 95 blocks, of which 254 NEED NO FIT and can ship first.** The fixtures are the hidden cost: five files run on `brms:::brmsfit_example1..6`, absent from the tarball, and frmtmb refuses four of the 24 terms they need, each killing a fixture: `me()` does not exist, `gr(g, by = )`, `arma()` without `cov = TRUE`, and `thres()`. **Quote any "passes brms's suite" fraction against 823, bins 1 and 2, never against 2,011**, since 59 percent is Stan code generation that cannot transfer at all. **Do NOT port `tests.log_lik.R`**, 65 of bin 2P: `test-brms-likelihood.R` already asserts the stronger claim that frmtmb's objective equals brms's Stan `log_prob` with a gradient check | `tests/testthat/test-brms-suite-*.R`, gated | the failure list, each classified, nothing left unclassified | 4 for bin 1; bin 2P is a further 2.75 and is NOT recommended, see `dev/brms-suite-audit.md` section 9 |

| 2.6c | DONE at frmtmb 0.59.0 and frmtmb.sample 0.7.0, as three lanes (famlink, priorform, brmsnames); the records are `dev/famlink-findings.md`, `dev/priorform-findings.md` and `dev/brmsnames-findings.md` with their reviews. Items filed and not fixed are listed in `dev/round-handoff.md`. The original row follows. **The defects brms's suite found, which is the payoff of 2.6a and outranks the port.** 15 are filed with constructions in `dev/brms-suite-audit.md` section 8. **Two are silent wrong answers and were verified independently before filing.** (a) **`family$link` returns a LIST.** frmtmb's family object has no `link`; it has `links`, a per-dpar list, and `$` PARTIAL-MATCHES: `student()$link` is a list of length 3 where brms gives the string `"identity"`, and `link_sigma` is `NULL`. `[[` is safe. This is the partial-matching hazard this repository keeps `test-bracket-access.R` for, on a PUBLIC object that brms-shaped code reads directly. (b) **An invalid `mu` link is not validated and FITS.** On one binary dataset `bernoulli()` gives (0.2461319, -0.6200684) and `bernoulli("sqrt")` gives (0.7395393, -0.0705001): the slope is destroyed and the only signal is three optimizer NA/NaN warnings. The check EXISTS for secondary dpars, since `weibull(link_shape = "logit")` is refused BY NAME, and is missing for `mu`. The remaining 13 include `bf()` not being idempotent where brms asserts `identical(form, bf(form))`; `mixture()` accepting incompatible support; `default_prior()`, `validate_prior()`, `brmsfamily()`, `empty_prior()` and `as.brmsprior()` not existing; `VarCorr(fit)` naming its element `"1 | patient"`, matching neither brms nor lme4; and `variables(fit)` dropping brms's `b_` prefix while keeping `sd_patient__Intercept`, half of each convention, **on a row `dev/brms-api-diff.md` records as matching brms exactly, which is true of its FORMALS and not of its ANSWER**. That last one is the lesson: an API diff that compares signatures does not compare answers. Also filed, a contract nothing here had written down: every brms error is a CLASSED condition, `brms:::stop2` raising `c("brms_error", "rlang_error", ...)`, so `tryCatch(brms_error = )` catches any refusal, while no frmtmb error is classed anywhere in `R/`. Filed at the 0.58.0 release, carried over from 2.5f: six more positional divergences from brms in frmtmb.sample, and the `summary = FALSE` family, where `fixef`, `ranef`, `coef` and `VarCorr` return a summary and brms returns the raw draws. Also filed: frmtmb.learn's `reward()` refusal says newdata "cannot supply them". That wording implies that `simulate()` reads newdata, but `simulate()` now refuses it by name. And `test-prior-compat.R` emits a pre-existing "singular convergence (7)" warning, which the suite does not assert | core `R/families.R`, `R/links.R`, `R/bf.R`; `dev/brms-api-diff.md` | each defect seen failing before its fix; the two silent ones refuse rather than fit, with a false-alarm rate measured on links the field actually uses | 2.5 |

**2.6c runs as three lanes started 2026-09-16, split by the files they touch.** `wt-famlink` holds findings 1, 2, 3, 5, 11, 15 and 16 of `dev/brms-suite-audit.md` section 8, plus section 7 contract 10: family objects, links and family validation. `wt-priorform` holds findings 4, 6, 7, 12, 13 and 14 and contract 7: `bf()`, formula grammar and the prior surface. It also carries the unasserted "singular convergence (7)" warning at `test-prior-compat.R:505`. `wt-brmsnames` holds findings 8, 9 and 10, the `hypothesis()` return object, the six positional divergences and the `summary = FALSE` family from `dev/brmsmatch-findings.md`, and the frmtmb.learn `reward()` message. brms is the tiebreaker throughout: finding 11 becomes an error, and finding 12 is refused.

| item | what | where | check | days |
|---|---|---|---|---|
| 2.6d | **`predict()` becomes brms's `predict()`, decided by the user 2026-09-16.** Today it carries glmmTMB's `type` vocabulary (`link`, `response`, `conditional`, `zprob`, `zlink`, `disp`) and defaults to `link`. On a lognormal fit the user compared it against brms and got a different scale with nothing said. Changing only the default was rejected: a response-scale mean matches neither brms, whose `predict()` summarizes the posterior PREDICTIVE distribution with observation noise in `Estimate, Est.Error, Q2.5, Q97.5`, nor glmmTMB, whose default is `link`. brms's mean on the response scale is `fitted()`, which already matches. The frequentist construction of a predictive summary has to be designed, not assumed: simulation from the plug-in fit, with or without parameter uncertainty drawn from the joint precision. Before changing anything, audit every internal caller of `predict(type = )`, which includes `conditional_effects()`, and every caller in emmeans, insight and marginaleffects | core `R/predict.R`, `R/conditional-effects.R`, interop | a lognormal and a Poisson fit's `predict()` has brms's shape and column names, and its interval covers at the nominal rate by simulation; no interop entry point breaks, measured by running them | 2 |
| 2.6f | **Return shapes follow brms, decided by the user 2026-09-17.** The 2.6b port (`dev/brmsport-findings.md` in `wt-brmsport`) found that `fitted()`, `residuals()`, `fixef()`, `ngrps()`, `vcov()` and `summary()` still return lme4/glmmTMB shapes (for example `vcov` 10x10 against brms's 9x9), and that frmtmb.sample refuses `nsamples()` and `posterior_samples()` by a design that predates the brms-tiebreaker rule. Move each to brms's shape. On a frequentist fit, a draws-only argument is refused by name, as 2.6c did for `summary = FALSE`. Audit every internal and interop caller first (emmeans, insight, marginaleffects, broom.mixed-style `as.data.frame`), because this breaks every one of them. Also recorded at the same time: error message WORDING need not match brms (the behavior and the condition class must), and frmtmb objects must NOT carry brms's class names | core `R/methods-fit.R`, `R/confint.R`; `frmtmb.sample/R/methods-draws.R`; interop | each ported brms assertion on these methods passes or is a cited divergence; no interop entry point breaks, measured by running it | 2 |
| 2.6e | **Classed conditions, brms contract 1.** Every brms error is `c("brms_error", "rlang_error", ...)`, and no frmtmb error is classed. This is cross-cutting across `R/`, so it runs AFTER the three 2.6c lanes merge, never beside them | core `R/`, then each extension | `tryCatch(frmtmb_error = )` catches every refusal the suite asserts; the count is generated | 1 |

## Phase 3. Real data in

| item | what | where | check | days |
|---|---|---|---|---|
| 3.1 | DONE in lane `wt-phase3a`, pending review (`dev/phase3a-findings.md`). `frm_ode_records(d, id, time, evid, amt, cmt, rate, dur, ii, addl, ss)` splits one NONMEM-shaped table into `list(data, events)` in `frm_ode()`'s grammar, doing no arithmetic on a dose; `dur` was added because rxode2 writes infusions that way. Against `rxode2::et()` its doses equal `etExpand()`'s on 126 of 126 schedules etExpand() can expand (it errors on every schedule carrying `evid = 3`, 74 of 200). Against `rxode2::rxSolve()` on 190 random schedules, `frm_ode()` on its output agrees to 7.1e-13 of the trajectory's scale and `frm_lincmt()` to 3.1e-12; on `nlmixr2data::warfarin` and `theo_md` to 4.2e-15 and 2.2e-13. Two refusals came from the validation: an observation listed after an event at the same time (rxode2 reads any such sample post-dose, `frm_ode()` pre-dose), and a reset inside a running infusion, where `frm_ode()` runs the infusion on, `frm_lincmt()` refuses and rxode2 goes negative on 10 of 10. Punch round 1 found that round 0 never put two events at one time: a reset, steady state, replace or multiply sharing a time with another event of the same id (`addl` repeats included) is read three ways by NONMEM, `frm_ode()` and rxode2 (a bolus beside a steady-state dose: 112.03 against 32.58), and is now refused by name, with non-contiguous ids, time running backwards, items on records that do not read them, non-finite items, non-integer `cmt` and dose-modifier column names (`ALAGn`, `Fn`, `Rn`, `Dn`, `lag`, `tinf`). On 300 grid-time schedules 132 are accepted and agree with rxSolve to 1.0e-12; the new tests fail 66 assertions with 0 errors on the round-0 function. The vignette's Theoph example is rewritten through it; at the default tolerance the records spelling ends at `nlminb` code 1 at a log likelihood 6.3e-09 from the initial-condition spelling, so the vignette uses `atol = rtol = 1e-10` | `frmtmb.ode/R/records.R` | met | 1.5 |
| 3.2 | DONE in lane `wt-phase3a`, pending review (`dev/phase3a-findings.md`). `frm_cross_spectrum()` takes two lists of epochs of unequal length with `group` naming each epoch's unit, read as the clean spans of one record so no segment crosses an epoch boundary, and a vector pair with a per-sample `group` as `frm_periodogram()` does. The epoch list equals the concatenated-and-split call bit for bit when the lengths agree, and equals the single-record call with an `NA` between epochs at any lengths. `frm_cross_pairs(X, pairs, ...)` stacks channel pairs with `pair`, `ch1` and `ch2`; the vignette has the `(1 | pair)` and `s(freq, by = pair)` section. A 4-pair frame fits with `coh ~ 1 + (1 | pair)` within 0.29 standard errors of the four two-channel fits with the order kept, and with every parameter by pair it equals them to 4.5e-04 of their own standard errors. The dependence between two pairs that share a channel is not modeled, and the help page says so | `frmtmb.coupling/R/cross-spectrum.R`, `pairs.R` | met | 2.5 |
| 3.3 | DONE in lane `wt-phase3b`, unmerged; `dev/phase3b-findings.md`. All eight families take `session =`; a two-session objective equals the sum of the two single-session objectives at the same parameters, relative difference 0; single-session fits bit-identical to 0.6.0; over 202 replicates at 40 x 2 x 100 no Wilson interval excludes 95, where ignoring the boundary covers `tau` on 74.8 percent. A label reused in two runs that are not adjacent (a b a b) is refused by name; interleaved-context designs, one value store per context kept across its runs, are filed as a possible later extension. WAS: learn: `session =` on every family, so `init` runs again at each session boundary and the importance block stays the subject. The per-subject non-decision bound that used to be the second half of this item is now item 1.0b, moved by Phase 0 | `frmtmb.learn/R/engine.R`, `family.R` | a two-session dataset equals two single-session fits with shared parameters | 1 |
| 3.4 | PARTLY DONE in lane `wt-phase3b`, unmerged, after punch round 2; `dev/phase3b-findings.md`. PUNCH ROUND 2: an interval's mass is a difference of logs in every regime, 4.9e-11 worst on the log against 751 Rmpfr intervals (the round-1 repair was 25 units out on the review's); the survival's accuracy figures hold on the 700-bit grid only, and the worst measured anywhere is 1.6e-9 (|v| a = 250, w = 0.9999). The drift-intercept attribution below is WITHDRAWN: it was a seed artifact (arms share seeds; an oracle covers 197 of 231 on the same seeds; fresh seeds 2001 to 2070 give 65 of 70). PUNCH ROUND 1: the response-time distribution rewritten in log space (the survival at large |v| a was floored); against a 700-bit reference to |v| a = 120, worst log error 8.0e-11 on the survival (rising with |v| a), 4.3e-14 on F, 1.2e-12 on each defective function. Decision of 2026-09-24: left and interval rows scored at their dec() boundary; with trunc() refused by name. Recovery at 30 x 400: right censoring 40 replicates and left plus interval censoring 60; every quantity covers except the drift intercept, 85 to 87 percent with its Wilson interval excluding 95, which is equally low in the arms with no censoring (WITHDRAWN in round 2 as a finding: the arms share seeds, and the low seeds are low for any estimator). ROUND 0 TEXT FOLLOWS, where its accuracy figure is superseded: `wiener()` only: `lcdf` and `lccdf` over both boundaries, 4.0e-14 (F) and 2.4e-11 (S) against a 400-bit reference, 4.5e-11 absolute against `RWiener::pwiener()` (RWiener's own error), all four codes and `trunc()` against the hand-written likelihood, and at 30 x 400 with 16 percent right-censored over 100 replicates no Wilson interval excludes 95. Refused under `variability =`. `gddm()` and `lba()` NOT done. WAS: eam censoring, and censoring ONLY: the per-subject non-decision bound that item 2.1 said this item would gain is now item 1.0a, moved by Phase 0. `wiener()` gains `lcdf` as the log-sum of the lower-boundary distribution function in `wiener-cdf.R` and its reflection for the upper, blended the way the density is; `gddm()` reads the absorbed mass off its own grid; `lba()` uses the race identity, one minus the product of survivors. Then `cens()` and `trunc()` work on all three, and a deadline design is `rt \| dec(r) + cens(c)` | `frmtmb.eam/R/wiener-family.R`, `wiener-cdf.R`, `gddm.R`, `lba.R` | `RWiener::pwiener()` to 1e-10 over the same grid the density is pinned on; right-censored log-likelihood against the likelihood written by hand, as `test-rdm-gng.R` already does for rdm | 3 |
| 3.5 | PARTLY DONE in lane `wt-phase3b`, unmerged, after punch round 2; `dev/phase3b-findings.md`. PUNCH ROUND 2: a response outside `contaminant_range` is refused by name, class `frmtmb_eam_contaminant_range_error`. PUNCH ROUND 1: defect B1 (a staircase in lambda, the round-0 false convergence) fixed: code 0 on 40 of 40 with the window given, lambda 0.0499 covering 39 of 40. Decision of 2026-09-24: the window is contaminant_range =, or under trunc(ub =) the fastest response to the deadline (at 30 x 400 with a 3 s deadline lambda 0.0306 against a share of 0.0307, 35 of 36), and otherwise refused, classed. ROUND 0 TEXT FOLLOWS: `wiener()` only; `lba()` and `rdm()` NOT done. The observed-range default FAILS this row's own recovery check: at 30 x 400 `lambda` came back 0.0451 against 0.05 and covered on 57 of 99, because the slowest diffusion trial sets the range; `contaminant_range =` (new) set to the contaminants' range gives 0.0499 and 58 of 60. The collapse case: on clean data at 30 x 400 `lambda` reached its link edge on 49 of 60 fits and `diagnose()` named all 49. At `lambda = 0` the density is the plain one, `identical()` where the diffusion density is at least the contaminant's and within 64 ulp elsewhere. Whether to change the default is the user's call. WAS: eam contaminant. `wiener(contaminant = TRUE)` mixes the density with a uniform over the observed response range at a mixing dpar `lambda` on a logit link, inside the family, so it needs no core mixture and no uniform family. Same option on `lba()` and `rdm()`. The same construction covers Modrak's contaminated shifted-lognormal, which reaches this design from the other direction: brms needs a `custom_family` there because every brms family must have at least one estimated parameter and a uniform contaminant has none, an obstacle that does not exist here. Two things carry over from the Phase 2 round and belong in the acceptance criteria. His central fix, reparameterizing `shift` as `shiftprop = shift / max_shift` on a logit, IS this package's `ndt_bound()`, already shipped, and it removes his three-case Stan function entirely because the bound makes `y - shift > 0` structural, which matters because branching on a parameter is forbidden here. And his `beta(1, 5)` prior on the mixing proportion is load-bearing, giving 0.13 plus or minus 0.05 at n = 100; under ML there is no prior, so a weakly identified `lambda` can run to its link edge exactly as item 2.1's `sv` did, and the row must assert the COLLAPSE case and not only recovery at 5 percent | `frmtmb.eam/R/wiener-family.R` | recovery of `lambda` at 5 percent contamination, 30 x 400; the log-likelihood at `lambda = 0` equals the plain family's | 1.5 |

| 3.6 | DONE in lane `wt-phase3a`, pending review (`dev/phase3a-findings.md`). The charter question is settled with a second COUNT, not a second function: `n_nonmonotone` keeps its meaning (event rows, whose density is floored, so `logLik()` is a pseudo-likelihood), and `n_nonmonotone_censored` counts censored rows with `d(eta)/d(log t) <= 0` (an interval row at both ends), whose `log S` is exact, so the model rather than `logLik()` is wrong: its survival function rises there. `action = "error"` refuses on the first and, since punch round 2, warns on the second, so `frm_curve()` and its companions stay gated through one call, and the fit warns at its end. Reproduced on the released build first: on 4 of 6 seeds of `frailty-floor3.R`'s design five no-death centres land at slopes of -0.18 to -0.31 and every gate is silent; on the lane build the new count is 50 on all four, exactly those centres' rows, and 0 on the six controls. False alarms on ordinary right-censored designs at `df` 1 to 5, proportional hazards and `gamma1 ~ x`: 0 of 1600 simulated fits (95 percent upper bound 0.0023) and 0 of 270 fits on nine designs from eight real datasets on all three scales. The inverse test item 2.5 wrote failed on the base build and is flipped. Punch round 1: on a cure-fraction design with `gamma1 ~ arm` it fires on 27 of 80 fits at `df` 3 to 6, every flagged row past its arm's last event, the fitted survival rising by 5.2e-04 to 0.13; decided by the user 2026-09-24 in punch round 2: REFUSE on event rows, WARN on censored rows with the size of the rise (`max_survival_rise`), flexsurv's line said aloud; the 27 now warn, 53 are silent, none refuses, and the round-0 designs are 1870 of 1870 silent. The row test is exact at `df = 1`; at `df >= 2` a dip between two observed times of one group is not searched for | `frmtmb.spline/R/rp-check.R` | met | 1 |
Phase 3 total: about 10.5 days, one half-day having moved to Phase 1
and item 3.6 having been filed by item 2.5.
Item 3.4 keeps its three days: item 2.1 said 3.4 would GAIN a
per-subject bound rather than already carry one, and that gain became
item 1.0a's day and a half.

## Phase 4. The post-fit surface

| item | what | where | check | days |
|---|---|---|---|---|
| 4.1 | RP predictions. `rp_predict(fit, newdata, times, type = c("survival", "hazard", "cumhaz", "rmst", "median"), level, simultaneous)`, on the basis rebuilt from an exported `rp_knots(fit)`, with delta-method bands through the same covariance path `frm_curve()` uses; delayed entry and interval censoring get tests against flexsurvspline's counting-process and `interval2` forms | `frmtmb.spline/R/rp-predict.R` | point estimates identical to `flexsurv::summary()` at the same parameters; bands compared loosely, since flexsurv bootstraps; delayed-entry log-likelihood identical to flexsurv's | 3 |
| 4.2 | hmm on new data. `hmm_probs(fit, newdata)` and `hmm_viterbi(fit, newdata)` under the same group and time contract; `residuals(fit, type = "pseudo")`, the forward pseudo-residuals of Zucchini and others, chapter 6 | `frmtmb.latent/R/hmm.R` | decoding on the training frame passed as newdata equals the fit's own; pseudo-residuals against hmmTMB's on the same fit. Phase 0 measured the existing passes at 0.25 s and 0.09 s on 25,000 rows, so this item is about the newdata contract and not about speed | 2.5 |
| 4.3 | lca tooling. `lca_profiles(se = TRUE)` by the delta method; `lca_enumerate(formula, data, K = 2:6)` returning one row per K with log-likelihood, AIC, BIC, entropy and the smallest class share; `lca_blrt(small, large, nsim)` by simulate-and-refit, gated | `frmtmb.latent/R/lca-tools.R` | profile SEs against poLCA's `probs.se`; the enumeration table's BIC minimum on carcinoma agrees with the literature | 2.5 |
| 4.4 | learn's two missing pieces. frmtmb.eam exports the conditional mean of a Wiener first-passage time, the one export `dev/learn2-findings.md` asked for, and rlddm gains `fitted()`; `learn_family(init, update, choose, ...)` becomes a public constructor with the rule contract on its help page and `check_learn_family()` beside it, mirroring core's `check_custom_family()` | `frmtmb.eam/R/wiener-post.R`, `frmtmb.learn/R/family.R` | a user-written delta learner through `learn_family()` equals `bandit2arm_delta()` to 1e-12; `check_learn_family()` passes it | 3 |
| 4.5 | eam model checking. `ddm_qp(fit, newdata, quantiles)`: the quantile-probability table per condition and boundary from `simulate()`, with a plot method, which is the figure every DDM paper shows | `frmtmb.eam/R/wiener-post.R` | observed and simulated tables agree on data the fit simulated | 1 |
| 4.6 | sample throughput and vocabulary. Build the design once and multiply per draw in `posterior_epred()`, `posterior_linpred()`, `posterior_predict()`, `ranef()` and `coef()`; bulk and tail ESS from `posterior::ess_bulk()` in `summary()`; `constant()` as a prior spelling that maps the parameter; a `class = "Intercept"` route to ordinal thresholds; `laplace = TRUE` draws refused by `posterior_epred()`, `ranef()` and `hypothesis()` the way `loo()` already refuses them | `frmtmb.sample/R/methods-draws.R`, `sample.R` | Phase 0's `posterior_epred()` time falls by the factor the tier measures; identical values before and after. Phase 0 measured that time at 1.59 s for 4000 draws over 2000 rows against 213 s of sampling, so this item is worth doing for the vocabulary half and NOT for the throughput half, and it stays in Phase 4 | 3 |

Phase 4 total: about 15 days.

## Phase 5. The model menu

These are new families and measures. Each ships with the full bar in
"Rules".

| item | what | where | check | days |
|---|---|---|---|---|
| 5.1 | learn: `bandit_delta(n_option = K, lapse = FALSE, perseveration = FALSE, decay = FALSE, q0 = FALSE)`, one family covering hBayesDM's `bandit4arm_lapse`, `bandit4arm_2par_lapse`, `bandit4arm_4par`, `bandit4arm_lapse_decay` and `banditNarm_*` by option, with those names as aliases in `frm_learn_families()`; the engine already vectorizes over K for the Kalman filter | `frmtmb.learn/R/bandit-delta.R` | a Stan program per option set, identity to 1e-12; recovery at 100 x 200 per option | 4 |
| 5.2 | hmm multi-stream emissions. A matrix response, following `lca()`'s precedent rather than `mvbf()`, with `family = list(gamma(), von_mises())` one per column, per-state dpars per stream, and the product of stream densities in the forward pass; circular quantile starts for angles | `frmtmb.latent/R/hmm.R`, `hmm-streams.R` | step and angle on the elk tracks moveHMM ships, against moveHMM and hmmTMB to 1e-6 in log-likelihood | 5 |
| 5.3 | coupling inference. `frm_imag_coherence(fit)`, the volume-conduction-robust quantity `sqrt(C) sin(phase)`, with a delta-method interval; `frm_coherence_test(fit)`, a likelihood-ratio test of zero coherence against two independent Whittle fits, with the boundary correction, and a max-over-frequency version from the simultaneous band on the coherence curve | `frmtmb.coupling/R/coupling.R`, `test.R` | size of the test at the null over 500 simulated pairs; imaginary coherence against FieldTrip's on one exported dataset | 2.5 |

| 5.4 | core: the **bridge** random-effect distribution for logit links, Wang and Louis (2003). A Gaussian random intercept makes the marginal model NOT logistic, so conditional and marginal coefficients differ by a factor that is only approximate; with a bridge-distributed intercept they are an EXACT scalar multiple, `beta_marginal = phi * beta_conditional`. The density is elementary and AD-safe, `f(x) = sin(phi*pi) / (2*pi*(cosh(phi*x) + cos(phi*pi)))`, with variance `pi^2 (phi^-2 - 1)/3`, so the work is not the density. The work is a SEAM: `R/covstruct.R` assumes the random effects are Gaussian, and this is a non-Gaussian distribution on `b`. RTMB integrates whatever density it is given, so `random=` still applies, but the Laplace approximation's adequacy is a separate question on a heavier-tailed latent and `check_laplace()` is the instrument. Item 2.6 is why this is worth having rather than a curiosity: it measured the Gaussian attenuation factor `1/sqrt(1 + 0.346 V)` predicting -0.01962 against -0.01969 observed, an approximation that happens to be good at that design; bridge makes the same relation exact and known | core `R/covstruct.R`, a random-effect DISTRIBUTION seam that does not exist yet | `phi` recovered at a known truth; the marginal fit of the same data returns `phi * beta` to the tolerance the design supports; `check_laplace()` on the heavier tail; identity against `bridgedist::dbridge()` | 3 |
Phase 5 total: about 15 days, item 5.4 having been added after the
Phase 2 round. Item 5.4, the ode closed forms, is no
longer here: Phase 0 measured the design at 3948 s and moved it to
Phase 1 as item 1.0c, which is what this line used to say Phase 0 would
decide.

## Core seams

Filed for core, in the order the phases above need them. Each names the
extension item that is waiting.

| seam | needed by | what |
|---|---|---|
| `frm_sample()` consults compat refusals | 1.2, DONE | the sampler is in an extension, so this was really a frmtmb.sample change; the core half was the contract, and `?frmtmb_register_compat` now promises that a `refused` row is enforced wherever the enforcing code can see the feature, with that code saying what it can see |
| `frm_features_used(spec)`, or the same answer from a `bf()` plus data | 1.2, for three kinds of the four | The pre-flight has to decide whether the model in front of it uses a refused feature, and a name cannot decide it: what identifies a feature in a model is WHERE its call sits in the formula. `se` inside the bar on the response's left is the `se()` addition term; `se` in a nonlinear body is a user's own helper, and frmtmb fits that model. ONE position is available without core: an addition term is written inside that bar and nowhere else, and the `bf()` object carries it, so frmtmb.sample matches the `aterm` kind by position in twelve lines and covers 9 of the 20 call-route rows. The seam is what the other three kinds need, `special`, `autocor` and `grammar`, where the parser's knowledge does not leave core: those are matched by name, which is justifiable but not sound, so a refusal registered on `mo()`, `ma()`, `cosy()` or `mm()` would fire on a user's function of that name. A core function returning the vocabulary rows a spec actually uses would let every consulting caller refuse exactly and warn never. Needed before a second package registers a refused row on one of those kinds. `dev/stanctl-findings.md` section 2.4 has the audit |
| A resolved-answer cache for `frm_compat()`, beside the vocabulary cache already in `compat_cache_store()` | 1.2, filed not built | `frm_compat(feature, status =)` re-resolves the whole pair table on every call, and `frmtmb.sample::frm_sample()` now calls it once per sampling call. Measured, interleaved, five rounds of twenty calls with a control arm: 0.117 s per call with two extensions loaded (111 vocabulary rows, 447 rules), 0.324 s with seven (138, 991), 0.581 s at a synthesized fifteen-extension scale (178, 1191), and twenty identical calls each pay it. The cost tracks the RULE count and the pair table it resolves, not the package count. Core already has the discipline this needs and uses it for the VOCABULARY: `compat_cache_store()` validates against `frmtmb_compat_contrib$features` rather than a dirty flag, so a caller that restores the list cannot leave a stale flag behind. Caching the resolved per-feature answer under that same key keeps ONE code path, keeps the call-time semantics that make a package's refusal appear the moment it loads, and takes the cost to near zero after the first call in a session. The alternative the sampler considered and declined, a cheap superset scan of `frm_compat_rules()` to skip the resolved read, is the wrong fix for a refusal: it is a second code path that can disagree with the first. `dev/stanctl-findings.md` section 2.6 has the table |
| `frm_multistart(fit, n, jitter)` | 2.3 | the general form of `hmm_starts()`: refit from jittered starts, return the best, report the spread. Every mixture-shaped model in core wants it too |
| log-scale left and interval censoring, and the truncation normalizer | 4.1 | `dev/spline-core-findings.md`, "What it does not close". Delayed entry in a survival model is a left truncation, and it round-trips through a probability today |
| `required_aterms` as a disjunction | closes eam dev-findings #2 | "one of `dec()`, `vint()`" cannot be declared; the check is hand-rolled in `valid_y` |
| a `constraints` slot on `frmtmb_family()` | closes eam dev-findings #9 | functions of the whole dpar vector, checked at the start and reported at the optimum; the joint constraints on `sz`/`bias` and `st`/`ndt` have no other home |
| a prior-only objective | 4.6, deferred | `sample_prior = "only"` needs the likelihood term dropped at tape build, which is a core control, not a sampler option |
| `diagnose()`'s `unbounded_dpar` inverts on a BOUNDED link | 1.0a, 1.0b | filed by Phase 0. The check's evidence pair is `abs(est) > 10` and a standard error LARGER than the estimate, calibrated on `student()`'s `nu` where running off an unbounded link explodes the standard error. On a scaled logit the derivative vanishes at the edge, so the standard error collapses and the linear predictor saturates below 10. Measured on the Phase 0 eam rows: `ndt: (Intercept)` at 11.20 with a link-scale se of 2.37, which is 0.99999 of the bound and does not fire; and 7.17 in the non-convergent arm, below the magnitude threshold entirely at 0.9992 of the bound. `extreme_theta` does look at random-effect log standard deviations but its threshold of 8 is link-agnostic and the `ndt` component stands at 2.05. Four bounded `ndt` links ship today (`wiener()`, `lba()`, `rdm()`, `gddm()`), so the check needs to know whether a link is bounded and, if it is, to test proximity to the bound rather than magnitude plus a large standard error |
| `diagnose()` needs a FOURTH `unbounded_dpar` mode | 2.1, filed | The seam below describes a BOUNDED link whose standard error collapses. Item 2.1 found the opposite on a LOG link: `sv` running to the link's floor on 1 fit in 60 with `confint()` at (-1163, 1147), convergence 0, a positive definite Hessian, and `diagnose()` printing "No convergence problems detected". The estimate, 7.7306, fails the magnitude half while the standard error, 589.25, passes it, and "test proximity to the bound" cannot reach a link that has no bound. The signature a user can read off ONE fit is the standard error RATIO against the largest other coefficient in the same fit: 589.25 against 0.050094, a ratio of 11,763. The likelihood really is flat, profiled monotone to the floor over 11 grid points, so this is not a failed optimization and a restart does not fix it |
| An ORACLE check: the objective at known truths | 2.2, proposed | Three lanes in the Phase 2 round found a fit-health instrument misreading a fit, in three different directions: `unbounded_dpar` inverting on a bounded link, `sv` collapsing on a log link with `diagnose()` silent, and the positive-definite-Hessian check firing on the five fits with the HIGHEST log-likelihood surplus in their cohort, three of which the optimizer itself declares converged at a gradient under 2e-3. The common shape is that **each instrument tests a PROXY for health that a boundary or a bounded link breaks, and none of them tests the likelihood value against anything.** Item 2.2's oracle comparison, the objective evaluated at each replicate's realized truths, costs a few seconds per fit and separated those five cleanly where both the convergence code and the Hessian check failed to. It is only available in simulation, where the truths are known, so the seam is a supported way for a recovery study to ask it rather than a general diagnostic |
| `imp_stalled()` is `@noRd` | 2.2, filed | Item 2.2's own wording asks an extension to count the importance correction's success rate "with 0.55.0's `imp_stalled()`", which the scope rule forbids because it is not exported. Either export it with a contract or restate the row against something that is |
| `frmtmb_control(vectorize = FALSE)`, an opt-in RTMB tape option | DEFERRED by the user 2026-09-17: too much complexity for the benefit. MEASURED and BUILT in `wt-vectorize`, not merged. A follow-up census (`wt-vecshape`, `dev/vecshape-findings.md`) found that no reshaping of the objective helps a Laplace fit, since the plain tapes come out identical. Three exact reshapings would make vectorize pay on the SAMPLER tape only, for gaussian-like models with an intercept-only dpar: the joint gradient is about 2x faster at 1e3 to 1e4 rows, and a Poisson model gains at most about 1.3x. The first of the three passes an intercept-only dpar as a scalar, which can break any lpdf, custom family or extension that indexes a dpar by row, so it would need an audit and an opt-in family flag. Revisit if sampling speed becomes a priority. `TapeConfig(vectorize = "enable")` is SLOWER on every random-effects model, 1.36x (`s(x)`) to 1,116x (`ar1()`) per full fit, and faster on none of 17 models, because it breaks the sparsity of the Laplace sparse Hessian tape (2,039 to 66M nodes on a 1,000-group model). The tmbstan vignette's 2.27x REPLICATES on its own hand-written ChickWeight tape (2.30x), but it is `timer()`'s forward pass only: that tape's joint gradient gains 1.41x and a sampling step 1.30x, identical under wall and CPU time on one thread. On frmtmb's objective for the same model the vectorized forward pass is 3x SLOWER. The record is `dev/vectorize-findings.md` in that worktree. The original design follows. | kaskr recommends `TapeConfig(vectorize = "enable")` in the RTMB tips and the tmbstan vignette, where it gives a 2.3x speedup on a random regression. The user's design: an `frmtmb_control()` option, OFF by default, that sets the tape config before a tape is built and RESTORES the previous state on exit. **A last-bit change in the objective is expected and is not a defect**: a vectorized tape evaluates the same sums in a different order, and IEEE addition is not associative; a first probe saw 1.54e-16 relative on a Poisson GLMM and bitwise identity on a gaussian and a wiener fit. Off by default keeps every bitwise pin in the suite unaffected, since those run with the default. Mechanics, verified against RTMB 1.9: `TapeConfig()` with no arguments RETURNS the whole state as a list of seven fields (`matmul`, `ops`, `math`, `sparse`, `sum`, `mvnorm`, `compare`), and passing that list back restores all seven exactly, so `old <- RTMB::TapeConfig(); on.exit(RTMB::TapeConfig(old), add = TRUE)` is the whole of the restore. `vectorize = "enable"` is a convenience that sets four of those fields at once, which is why an earlier probe that read a field named `vectorize` got nothing back: there is no such field. **The setting is baked into a tape when it is built**, so every place that builds one must apply it: there are TEN `MakeADFun` call sites, four in `R/fit.R`, two in `R/importance.R`, one each in `R/sandwich.R` and `R/interop.R`, and two in `frmtmb.sample`'s `R/sample.R`. Use ONE internal helper at all ten, and have the downstream sites read the option STORED ON THE FIT rather than a fresh default, or a vectorized fit gets its importance correction and its sandwich taped plain and the two disagree about the objective they share. The two sampler sites are where it matters most; once measured, whether `frmtmb.sample` should default it ON for sampling is a separate decision | core `R/fit.R`, `R/importance.R`, `R/sandwich.R`, `R/interop.R`, `frmtmb_control()`; `frmtmb.sample/R/sample.R` | the tape config after `frm()` is `identical()` to the config before it, INCLUDING when `frm()` errors partway, which is the case `on.exit()` exists for and must be seen failing against a version without it; every one of the ten sites honours the stored option, asserted by reading the config each site actually taped under rather than by trusting the helper is called; and the speedup measured with `GetTape()$timer()` per model family against an interleaved control | 1 |
| tmbstan 1.2.1: verify the sampling fix, then raise the floor | filed 2026-09-15, waiting on a free machine | The user's own upstream report, kaskr/tmbstan#33, "Sampling issue with StanHeaders 2.39.1?": with StanHeaders 2.39.1, tmbstan 1.2.0 SAMPLES A STANDARD NORMAL instead of the model, silently. It is closed by commit `089bc18`, two lines in `tmbstan/tools/autogen.R`, the generator that writes `model.hpp`, which is exactly the file `frmtmb.sample`'s `tmbstan_build_broken()` inspects for the unpatched overload, so the fix and the guard agree on the mechanism. Released as 1.2.1 on 2026-09-11. **Verify rather than trust**: in a PRIVATE library, never the shared ones, install tmbstan 1.2.1 against StanHeaders 2.39.1, the version that TRIGGERS the defect, and run #33's own reproduction, `TMB:::runExample("simple")` then `tmbstan(obj)`, asserting `beta[1]` is not distributed as N(0, 1), plus the guard's own tests, which construct a broken and a patched `model.hpp`. Then raise `frmtmb.sample`'s Suggests to `tmbstan (>= 1.2.1)`. **Keep the guard**: it costs nothing and still catches a partial or older install. **Do NOT drop the StanHeaders pin on the strength of this**, because the pin has TWO reasons and 1.2.1 resolves one: tmbstan sampling a standard normal is fixed, but rstan 2.32.7 still cannot compile any fresh Stan program against StanHeaders 2.39.1, which is rstan's defect and untouched by tmbstan. The verification may therefore hit rstan's compile failure before it reaches tmbstan's; if it does, that is the finding, and the pin stays | `frmtmb.sample` DESCRIPTION after the samplegen lane merges, to avoid a conflict; a private library for the check | #33's reproduction passes on 1.2.1 with StanHeaders 2.39.1, or the rstan compile failure is recorded as what blocks it; the guard's tests pass on 1.2.1 | 0.5 |
| `mixture()` over families whose primary dpar is not `mu` | eam races | `mixture(rdm(), ...)` is refused because the dpars are `v1..vn`; a structure-level opt-in would unblock it |
| EM-seeded starts for the latent-discrete families (`mixture()`, `hmm()`, `lca()`) | DEFERRED by the user 2026-09-21: lower value than the ongoing correctness work | EM as the ESTIMATOR is not proposed: with AD gradients, quasi-Newton on the exact marginal converges faster, a random-effects M-step needs Laplace inside it, and EM gives no standard errors. EM as an INITIALIZER targets a known weak point, the start at the exchangeable point (SEAM 4 in `dev/bcm-findings.md`, and the `mixture(poisson, poisson)` `init` bug): a few crude EM passes, fixed effects only, from randomized responsibilities break the symmetry, and nlminb and `sdreport` then run on the exact likelihood as now. Gate it on a measurement first: on the mixture, HMM and LCA fixtures, how often does the current start end below the best of 20 random starts, or fail to converge. If rarely, a hand-written start (a quantile split) is enough | core `R/families.R`, `R/fit.R`; `frmtmb.latent` | the local-optimum rate before and after on the same fixtures; estimates and standard errors unchanged on every fit whose current start already reaches the best optimum | 2 |

## Not doing, and why

- **More than two choices in `gddm()`.** `dev/gddm-nchoice-feasibility.md`:
  the only route that keeps state-dependent drift costs 12x the tape
  and 72x the gradient for an answer an order of magnitude worse.
  `lba()` and `rdm()` cover the choice count.
- **An hBayesDM cross-check.** Blocked on its toolchain, as
  `dev/rl-findings.md` records, and it would be an
  estimate-against-estimate comparison where a purpose-written Stan
  program gives an identity.
- **Estimated lag times, event times and dose intervals in ODE.** They
  decide where the solve is split, which is settled before the tape
  is built.
- **Time-frequency coherence and directed measures.** Different
  models, said so in the coupling vignette; the per-frequency Wishart
  cannot give them.
- **Horseshoe and R2D2 priors.** They carry auxiliary parameters the
  tape would have to hold; a real design, not a vocabulary entry.
- **A functional-response object like `refund::pffr`.** A scope
  difference, recorded in `dev/feature-gaps.md`.
- **Semi-Markov and continuous-time hidden Markov models.** Separate
  models; the discrete-time chain is the one the protocol fits.

## Sequence and size

| phase | days | runs in parallel with | gate |
|---|---|---|---|
| 0, measurement | 1, done | nothing; it was first | none |
| 1, small and high leverage | 13, DONE | 2 | none |
| 2, hierarchy validation | 7.5, DONE | 1 | none |
| 2.5, do not break brms | 3.5 | nothing; it is a defect | none; 2.5c resolved to nlme |
| 2.6, pass brms's own suite | 1, then sized by 2.6a | 2.5 | 2.6a sizes 2.6b |
| 3, real data in | 10.5 | 4 | none left: 2.1 decided 1.0a and it moved |
| 4, post-fit surface | 15 | 3 | 1.3 lands before 4.1 |
| 5, model menu | 15 | nothing | none; item 5.4 is now the bridge distribution, the ode closed forms having moved to 1.0c |
| total | about 65.5, with Phases 0, 1 and 2 done |  |  |

Days are single-lane estimates at the sizing convention of
`dev/hmm-feasibility.md`, which came in on the low side of its own
estimate. Phase 0 came in at one day of work against an estimate of
three, with about four hours of runs on top. Phases 1 and 2 are
per-package lanes and can run as seven lanes at once; the sequence
within a package is what the gate column constrains.

## What this document is not

It is not a promise of order beyond Phase 0. The survey's ranking rested
on doc figures and one afternoon of benchmarks, and Phase 0 existed to
replace it with measurements. It has: three items moved, two phase
totals changed, and two of the survey's cost worries turned out to be
nothing while a defect it had not looked for turned out to be the
worst thing in the table. The items moved, and the
table above is edited rather than appended to, so that the plan reads as
one document and not as a changelog.

dev/reunc-fdsmooth.R against the build of dev/reunc-log/install-p2-1.log,
which is punch round 1 BEFORE re_row_support() was given the smooth
parts. Kept because the file it was written to (fdsmooth.txt) is
regenerated by the fix and this is the only record of the defect.

frmtmb from C:/Users/adf44/source/r/reunc2-lib/frmtmb

factor smooth, 6 levels: kept 30, loaded in sample 30, by 3 newdata rows 15
  batches in sample: 30; on newdata: refused
  in sample                    identical FALSE max rel 4.06e-16  max ulp 1.8
  3 newdata rows               identical TRUE  max rel 0  max ulp 0.0

smooth plus (1 | h): kept 35, loaded by 3 rows 17
  in sample                    identical FALSE max rel 5.46e-16  max ulp 2.5
  3 newdata rows               identical FALSE max rel 0.676  max ulp 3044671088152878.0

The last line is the defect: fitted(newdata = ) on a cumulative fit
with s(x, g, bs = "fs") AND (1 | h) reported an Est.Error 0.676 of the
way wrong, silently. The smooth-only fit above it is correct by
accident: with no ordinary group term the row support was NULL, the
batch refused, and the route fell back to one coefficient at a time.

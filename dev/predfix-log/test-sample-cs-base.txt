LIB C:/Users/adf44/source/r/rellib-r3 0.10.0 
FAILED [an unseen level without the flag gets no refused hint] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match regexp "^posterior_predict[(][)] on draws". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level posterior_predict
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level posterior_predict
FAILED [an unseen level without the flag gets no refused hint] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match regexp "^posterior_epred[(][)] on draws". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level posterior_epred
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level posterior_epred
FAILED [an unseen level without the flag gets no refused hint] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match regexp "^posterior_linpred[(][)] on draws". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level posterior_linpred
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level posterior_linpred
FAILED [an unseen level without the flag gets no refused hint] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match regexp "^predict[(][)] on draws". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level predict
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level predict
FAILED [an unseen level without the flag gets no refused hint] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match regexp "^fitted[(][)] on draws". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level fitted
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level fitted
FAILED [an unseen level without the flag gets no refused hint] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match regexp "^residuals[(][)] on draws". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level residuals
FAILED [an unseen level without the flag gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level residuals
FAILED [sample_new_levels alone is the call without the flag] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [sample_new_levels alone is the call without the flag] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level posterior_predict
FAILED [sample_new_levels alone is the call without the flag] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [sample_new_levels alone is the call without the flag] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level posterior_epred
FAILED [sample_new_levels alone is the call without the flag] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [sample_new_levels alone is the call without the flag] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level posterior_linpred
FAILED [sample_new_levels alone is the call without the flag] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [sample_new_levels alone is the call without the flag] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level predict
FAILED [sample_new_levels alone is the call without the flag] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [sample_new_levels alone is the call without the flag] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level fitted
FAILED [sample_new_levels alone is the call without the flag] Expected `m` not to match string "Use allow_new_levels = TRUE". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level
FAILED [sample_new_levels alone is the call without the flag] Expected `m` to match string "refused here as well". Actual text: x | New levels in grouping factor `g`: new. Use allow_new_levels = TRUE to predict them at the population level residuals
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` not to match string "allow_new_levels = TRUE treats". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that block's varia
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` not to match string "allow_new_levels = TRUE treats". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that block's varia
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` not to match string "allow_new_levels = TRUE treats". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that block's varia
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` not to match string "allow_new_levels = TRUE treats". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that block's varia
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` not to match string "allow_new_levels = TRUE treats". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that block's varia
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` not to match string "allow_new_levels = TRUE treats". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that
FAILED [a newdata without the grouping column gets no refused hint] Expected `m` to match string "refused here as well". Actual text: x | newdata has no column `g`, the grouping factor of a random effect. Add it, or say what the prediction should do without it: allow_new_levels = TRUE treats every row as an unseen level (the population value, plus that block's varia
FAILED [re_formula = NA answers an unseen level, flag or not] Expected `pf_msg(fn, ds, newdata = unseen, re_formula = NA, allow_new_levels = TRUE)` to be identical to "answered". Differences: actual vs expected - "posterior_predict() on draws cannot predict a grouping level the fit did not see: brms draws that level's effect from each posterior draw or resampl
FAILED [re_formula = NA answers an unseen level, flag or not] Expected `pf_msg(fn, ds, newdata = unseen, re_formula = NA, allow_new_levels = TRUE)` to be identical to "answered". Differences: actual vs expected - "posterior_epred() on draws cannot predict a grouping level the fit did not see: brms draws that level's effect from each posterior draw or resamples
FAILED [re_formula = NA answers an unseen level, flag or not] Expected `pf_msg(fn, ds, newdata = unseen, re_formula = NA, allow_new_levels = TRUE)` to be identical to "answered". Differences: actual vs expected - "posterior_linpred() on draws cannot predict a grouping level the fit did not see: brms draws that level's effect from each posterior draw or resampl
FAILED [re_formula = NA answers an unseen level, flag or not] Expected `pf_msg(fn, ds, newdata = unseen, re_formula = NA, allow_new_levels = TRUE)` to be identical to "answered". Differences: actual vs expected - "predict() on draws cannot predict a grouping level the fit did not see: brms draws that level's effect from each posterior draw or resamples the lev
FAILED [re_formula = NA answers an unseen level, flag or not] Expected `pf_msg(fn, ds, newdata = unseen, re_formula = NA, allow_new_levels = TRUE)` to be identical to "answered". Differences: actual vs expected - "fitted() on draws cannot predict a grouping level the fit did not see: brms draws that level's effect from each posterior draw or resamples the leve
FAILED [re_formula = NA answers an unseen level, flag or not] Expected `pf_msg(fn, ds, newdata = unseen, re_formula = NA, allow_new_levels = TRUE)` to be identical to "answered". Differences: actual vs expected - "residuals() on draws cannot predict a grouping level the fit did not see: brms draws that level's effect from each posterior draw or resamples the l
FAILED [posterior_predict() on draws carries cs()] Expected at x = 3 < 6. Actual comparison: 384.2 >= 6.0 Difference: 378.2 >= 0
FAILED [posterior_predict() on draws carries cs()] Expected in sample, x > 1.5 < 6. Actual comparison: 99.0 >= 6.0 Difference: 93.0 >= 0
RESULT test-predfix-new-levels.R pass=12 fail=50 err=0 skip=0

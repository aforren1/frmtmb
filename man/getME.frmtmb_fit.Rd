% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop.R
\name{getME.frmtmb_fit}
\alias{getME.frmtmb_fit}
\title{Extract components of a fit, lme4 style}
\usage{
\method{getME}{frmtmb_fit}(object, name, resp = NULL, ...)
}
\arguments{
\item{object}{A \code{frmtmb_fit}.}

\item{name}{One or more names from the vocabulary above. A vector
returns a named list.}

\item{resp}{Response name, for the design extractors on a
multivariate fit.}

\item{...}{Unused.}
}
\value{
The requested component, or a named list when \code{name} names
several.
}
\description{
A small \code{\link[lme4:getME]{lme4::getME()}} vocabulary, for downstream code written
against merMod objects. Registered on lme4's generic, so call it as
\code{lme4::getME(fit, "X")} or load lme4 first.
}
\details{
Supported names:
\describe{
\item{\code{"X"}}{Fixed-effect design matrix of the \code{mu} predictor.}
\item{\code{"Z"}, \code{"Zt"}}{The sparse random-effect design of the \code{mu}
predictor and its transpose. Columns (rows of \code{Zt}) span the
whole random-effect coefficient vector, so a block belonging to
another distributional parameter contributes zero columns here.}
\item{\code{"beta"}, \code{"fixef"}}{The primary (\code{mu}-family) fixed-effect
coefficients, named. Coefficients of auxiliary distributional
parameters are a separate vector; use \code{\link[=fixef]{fixef()}} for all of them.}
\item{\code{"b"}}{Conditional modes in coefficient space, aligned with
the columns of \code{Z}. Reduced-rank (\code{rr()}) blocks are expanded
through their loadings, so this is not the internal parameter
vector.}
\item{\code{"theta"}}{Covariance parameters on the internal
(unconstrained) scale, as in \code{confint()}. These are not lme4's
relative-covariance-factor entries.}
\item{\code{"lower"}}{Lower bounds on \code{theta}. The internal
parameterization is unbounded, so this is a vector of \code{-Inf}, not
lme4's mixture of \code{0} and \code{-Inf}. Code that tests
\code{theta == lower} to detect a singular fit will never fire; use
\code{\link[=diagnose]{diagnose()}} or \code{\link[=VarCorr]{VarCorr()}} instead.}
\item{\code{"sigma"}}{Residual standard deviation
(\code{\link[=sigma.frmtmb_fit]{sigma.frmtmb_fit()}}).}
\item{\code{"flist"}}{The grouping factors, one per distinct grouping
variable. Smooth and Gaussian-process blocks are excluded: their
levels are basis functions, not groups. There is no \code{"assign"}
attribute.}
\item{\code{"n_rtrms"}, \code{"n_rfacs"}}{Number of random-effect terms and
of distinct grouping factors.}
}

Multivariate fits have one design per response, so \code{"X"}, \code{"Z"} and
\code{"Zt"} need \code{resp}; the other names answer without it.
}
\examples{
if (requireNamespace("lme4", quietly = TRUE)) {
  set.seed(1)
  dd <- data.frame(x = rnorm(100), g = factor(rep(1:10, 10)))
  dd$y <- rnorm(100, 1 + 0.5 * dd$x + rnorm(10, 0, 0.8)[dd$g], 1)
  fit <- frm(bf(y ~ x + (1 | g)) + gaussian(), data = dd)

  # the designs, for downstream code written against merMod objects
  dim(lme4::getME(fit, "X"))
  dim(lme4::getME(fit, "Zt"))

  # a vector of names returns a named list
  str(lme4::getME(fit, c("n_rtrms", "n_rfacs", "sigma")))

  # the conditional modes in coefficient space, aligned with Z
  head(lme4::getME(fit, "b"))
  # note: "lower" is all -Inf here, because the internal covariance
  # parameterization is unbounded. Use diagnose() to spot a singular
  # fit, not theta == lower.
  lme4::getME(fit, "lower")
}
}

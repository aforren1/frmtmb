% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-draws.R
\name{draws-structure}
\alias{draws-structure}
\alias{nobs.frmtmb_draws}
\alias{formula.frmtmb_draws}
\alias{family.frmtmb_draws}
\alias{getCall.frmtmb_draws}
\alias{coef.frmtmb_draws}
\title{Model structure behind a set of draws}
\usage{
\method{nobs}{frmtmb_draws}(object, ...)

\method{formula}{frmtmb_draws}(x, ...)

\method{family}{frmtmb_draws}(object, ...)

\method{getCall}{frmtmb_draws}(x, ...)

\method{coef}{frmtmb_draws}(object, ...)
}
\arguments{
\item{object, x}{A \code{frmtmb_draws} from \code{\link[=frm_sample]{frm_sample()}}.}

\item{...}{Unused.}
}
\value{
As for the corresponding \code{frmtmb_fit} method.
}
\description{
\code{nobs()}, \code{formula()}, \code{family()}, \code{getCall()} and \code{ngrps()} on a
\code{frmtmb_draws} report the model the sampler ran, by delegating to the
fit stored inside it. They read structure only, so they work on draws
from the formula route, which has no maximum-likelihood estimate.
}
\details{
\code{coef()} is a posterior quantity, not a structural one: it summarizes
the per-group coefficients (fixed effects plus that group's own
random effects) over the draws, in the same nested shape
\code{\link[=coef.frmtmb_fit]{coef.frmtmb_fit()}} returns, with a \verb{levels x statistics x coefficients} array in place of each data frame. That is brms's
\code{coef.brmsfit} layout.
}
\examples{
\donttest{
if (requireNamespace("tmbstan", quietly = TRUE) &&
    requireNamespace("rstan", quietly = TRUE)) {
  set.seed(9)
  dd <- data.frame(x = rnorm(60), g = factor(rep(1:6, 10)))
  dd$y <- rnorm(60, 1 + 0.5 * dd$x + rnorm(6, 0, 0.5)[dd$g], 1)
  ds <- frm_sample(bf(y ~ x + (1 | g)), family = gaussian(),
                   data = dd, chains = 1, iter = 500, refresh = 0)
  nobs(ds)
  ngrps(ds)
  coef(ds)$g[1:3, , "(Intercept)"]
}
}
}

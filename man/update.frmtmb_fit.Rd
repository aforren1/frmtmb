% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confint.R
\name{update.frmtmb_fit}
\alias{update.frmtmb_fit}
\title{Update and refit a model}
\usage{
\method{update}{frmtmb_fit}(object, formula., ..., evaluate = TRUE)
}
\arguments{
\item{object}{A \code{frmtmb_fit}.}

\item{formula.}{A complete formula or \code{\link[=bf]{bf()}}, or a delta such as
\code{~ . + z} or \code{. ~ . + z}.}

\item{...}{Arguments of \code{\link[=frm]{frm()}} to replace, e.g. \code{data}, \code{family},
\code{REML}. \code{newdata} is accepted for \code{data}.}

\item{evaluate}{If \code{FALSE}, return the updated call instead of the
refitted model.}
}
\value{
A \code{frmtmb_fit}, or the updated call when
\code{evaluate = FALSE}.
}
\description{
Re-evaluates the stored \code{\link[=frm]{frm()}} call with the given arguments
replaced. Any \code{frm()} argument can be updated by name.
}
\details{
The formula argument is \code{formula.}, as in \code{\link[stats:update]{stats::update()}} and in
brms; \verb{formula = } reaches it by partial matching. A formula
carrying a \code{.} is a delta applied to the stored \code{mu} formula with
\link[stats:update.formula]{stats::update.formula} semantics - one-sided \code{~ . + z}, dotted
\code{. ~ . + z}, or a changed response \code{z ~ . + x} - and keeps the dpar
formulas, the fixed dpar values and the family. A formula with no
\code{.} replaces the stored one. brms's \code{newdata} is accepted as a
synonym for \code{data}.
}
\examples{
set.seed(3)
dd <- data.frame(x = rnorm(60), z = rnorm(60))
dd$y <- rnorm(60, 1 + 0.5 * dd$x, 1)
fit <- frm(bf(y ~ x), family = gaussian(), data = dd)

# a delta on the stored formula, in either spelling
fit2 <- update(fit, ~ . + z)
formula(fit2)
formula(update(fit, . ~ . + z))
fit3 <- update(fit, formula. = ~ . - x, newdata = dd[1:40, ])
nobs(fit3)
}

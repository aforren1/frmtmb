% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{print.frmtmb_priorlist}
\alias{print.frmtmb_priorlist}
\title{Print a prior specification}
\usage{
\method{print}{frmtmb_priorlist}(x, show_df = NULL, ...)
}
\arguments{
\item{x}{A \code{frmtmb_priorlist}.}

\item{show_df}{Print as a table (\code{TRUE}) or as one line per
specification (\code{FALSE}). \code{NULL}, the default, prints a table when
there is more than one specification, as brms does.}

\item{...}{Must be empty.}
}
\value{
\code{x}, invisibly.
}
\description{
brms's layout: one specification prints as the parameter it
addresses followed by its density, \code{b_x ~ normal(0, 1)}, with any
bounds in front as \verb{<lower=0>}; several print as a table with one row
each, the table \code{as.data.frame()} returns, where a row with no density
of its own shows its class row's density as \code{"(vectorized)"}. An
empty prior prints nothing, as in brms.
}
\examples{
set_prior("normal(0,1)", coef = "x")
set_prior("cauchy(0,1)", class = "sd", group = "g")
set_prior("normal(0, 2)", class = c("b", "sd"))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral.R
\name{frm_periodogram}
\alias{frm_periodogram}
\title{Periodogram for a Whittle-likelihood fit}
\usage{
frm_periodogram(
  x,
  fs = NULL,
  taper = c("none", "hann", "split_cosine"),
  p = 0.1,
  detrend = c("mean", "none", "linear"),
  segments = 1L,
  group = NULL
)
}
\arguments{
\item{x}{A numeric vector (one series), a \code{ts}, or a matrix or data
frame whose \strong{columns} are series of equal length.}

\item{fs}{Sampling rate in Hz. Defaults to 1 (cycles per sample), or
to \code{stats::frequency(x)} when \code{x} is a \code{ts}.}

\item{taper}{\code{"none"}, \code{"hann"}, or \code{"split_cosine"} (the split
cosine bell of \code{\link[stats:spec.taper]{stats::spec.taper()}}, applied to a proportion \code{p}
of each end).}

\item{p}{Proportion tapered at each end for \code{"split_cosine"}.}

\item{detrend}{\code{"mean"} (the default), \code{"none"}, or \code{"linear"}.
\code{"mean"} and \code{"none"} return identical ordinates, because the
retained frequencies are exactly those where a constant contributes
nothing; \code{"linear"} does change them.}

\item{segments}{Number of non-overlapping blocks to average. See
Segment averaging.}

\item{group}{Optional grouping vector as long as \code{x} (a vector only),
splitting it into series that need not have equal lengths.}
}
\value{
A data frame with columns \code{freq} (Hz), \code{pgram} (the
ordinate), and, when the input names more than one series, \code{series}
(a factor). Rows are ordered by series and then by frequency.
}
\description{
Turns one or more time series into the data frame a spectral model is
fitted to: one row per retained Fourier frequency, with the ordinate
that \code{\link[=whittle]{whittle()}} treats as the response.
}
\details{
The zero frequency and the Nyquist frequency are \strong{dropped}. Their
ordinates are chi-square with one degree of freedom rather than two,
so they are not exponential and do not belong in the likelihood; a
fit that keeps them is wrong in a way nothing downstream reports. A
series of length \code{m} therefore returns \code{floor((m - 1) / 2)} rows.
}
\section{Scaling}{

The ordinate is \verb{|sum_t h_t (x_t - trend) exp(-2i pi f t / fs)|^2 / (fs sum_t h_t^2)} and \code{freq} is in cycles per unit time (Hz when \code{fs}
is in Hz), which is the scaling and the frequency axis of
\code{\link[stats:spec.pgram]{stats::spec.pgram()}}. So the ordinates of white noise average to
\code{var(x) / fs}, and a spectrum written for the model must be a density
per Hz. With \code{fs = 1} the frequency axis runs to just under \code{0.5},
in cycles per sample.
}

\section{What a taper does to the likelihood}{

A taper trades bias for dependence. Multiplying the series by a
window before the transform suppresses the leakage that makes a steep
spectrum's high frequencies too large, which is the main small-sample
bias of the Whittle likelihood; in exchange, neighboring ordinates
are no longer close to independent, so the likelihood is a working
one and standard errors from it are optimistic. The exponential
marginal survives: dividing by \code{sum(h^2)} keeps \verb{E I(f)} at the
spectral density smoothed by the window, so the mean model is
unchanged and only the correlation between rows is not.
}

\section{Segment averaging}{

\code{segments = k} splits the series into \code{k} non-overlapping blocks of
\code{floor(n / k)} points each (a remainder at the end is dropped),
periodograms each block, and averages them. The average of \code{k} independent
exponential ordinates is Gamma with shape \code{k} and the same mean, so
the matching family is \code{whittle(tapers = k)}: the two arguments are
one decision written twice, and this is the seam to get right. The
price is resolution, since the grid belongs to the block length
\code{floor(n / k)} rather than to \code{n}.
}

\examples{
set.seed(1)
y <- as.numeric(arima.sim(list(ar = 0.7), 512))
pg <- frm_periodogram(y, fs = 256)
str(pg)

# the ordinates average to the variance, in per-Hz units
c(mean(pg$pgram), var(y) / 256)

# one call for many series: columns are series
pg2 <- frm_periodogram(cbind(a = y, b = rev(y)), fs = 256)
table(pg2$series)
}
\seealso{
\code{\link[=whittle]{whittle()}} for the family, and \code{vignette("spectral")}.
}

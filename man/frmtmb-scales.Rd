% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{frmtmb-scales}
\alias{frmtmb-scales}
\title{Which scale each method reports}
\value{
This page documents a convention. It is not a function, so it
returns no value.
}
\description{
Every quantity a fitted model reports lives on one of three scales,
and nothing in the output says which. This page says it once, per
method, so that a ported script can be read without guessing.
}
\details{
The three scales are:
\itemize{
\item \strong{Link.} The linear predictor of one distributional parameter,
before its inverse link is applied. This is where the coefficients
live, where the standard errors are symmetric, and where a random
effect has its variance.
\item \strong{Response.} The units of the outcome itself.
\item \strong{Unitless.} A standardized quantity, such as a Pearson or
quantile residual, which has no units by construction.
}
}
\section{What each method returns}{
\tabular{lll}{
   method \tab returns \tab scale \cr
   \code{predict()} \tab a summary of the PREDICTIVE distribution \tab response \cr
   \code{frm_linpred()}, default \tab the \code{mu} linear predictor \tab link \cr
   \code{frm_linpred(type = "response")} \tab the conditional mean \tab response \cr
   \code{frm_linpred(dpar = "sigma")} \tab that predictor \tab link; \code{"response"} gives response \cr
   \code{fitted()} \tab a summary of the conditional mean \tab response \cr
   \code{conditional_effects()} \tab \code{estimate__} and its band \tab response \cr
   \code{residuals()}, default \tab observed minus \code{fitted()} \tab response \cr
   \code{residuals(type = "pearson")} \tab that, over the conditional SD \tab unitless \cr
   \code{residuals(type = "deviance")} \tab signed root unit deviance \tab unitless \cr
   \code{residuals(type = "osa")} \tab one-step-ahead quantile residual \tab unitless \cr
   \code{simulate()} \tab draws of the outcome \tab response \cr
   \code{coef()}, \code{fixef()}, \code{ranef()} \tab coefficients, modes \tab link, per dpar \cr
   \code{confint()}, \code{vcov()}, \code{summary()} \tab the same \tab link, per dpar \cr
   \code{VarCorr()} \tab SDs and correlations of a predictor \tab link, per dpar \cr
   \code{sigma()} \tab the residual SD, \code{NA} if it varies by row \tab response \cr
   \code{hypothesis()} \tab \code{Estimate} and its interval \tab link, per dpar \cr
   \code{posterior_summary()} \tab a summary of a draws MATRIX \tab that matrix's own \cr
   \code{pp_check()} \tab the outcome against replicates \tab response \cr
   \code{bayes_R2()} \tab refuses on a \code{frmtmb_fit} \tab neither \cr
   \code{logLik()}, \code{AIC()}, \code{BIC()} \tab the fitted likelihood \tab the data's own \cr
}
}

\section{Where this differs from brms}{


\code{predict()} used to be the first, and is no longer: since item 2.6d
it is brms's, a summary of the predictive distribution on the
response scale. The LINEAR PREDICTOR, which is what it returned
before and what the glmmTMB \code{type} vocabulary reaches, moved to
\code{\link[=frm_linpred]{frm_linpred()}} unchanged. On a lognormal fit the two differ by
about 8 against about 6,700, which is why they could not share a
name. Measured with \code{dev/generics-scale.R} and
\code{dev/generics-scale-brms.R}.

\code{sigma} is the one that remains. \code{summary()}'s
\verb{Regression Coefficients} block and \code{fixef()} report every
coefficient on its own link, so a \code{sigma} WITH A FORMULA and the
default log link is reported as \code{log(sigma)} and can be negative.
A \code{sigma} nobody wrote a formula for is not a coefficient at all: it
is in \code{summary()}'s \verb{Further Distributional Parameters} block on its
own response scale, as in brms, and \code{\link[=sigma]{sigma()}} returns that number.
}

\section{What agrees with brms without any conversion}{


\code{fitted()}, \code{residuals()}, \code{simulate()}, \code{ngrps()}, \code{ranef()},
\code{conditional_effects()} and the coefficients of a linear predictor
whose link is the identity.

For a lognormal fit with a CONSTANT sigma and no truncation,
\code{fitted()} is \code{exp(mu + sigma^2 / 2)}, the mean of the distribution
rather than its median \code{exp(mu)}; the ratio between the two is
\code{exp(sigma^2 / 2)}, which is the whole of the discrepancy a ported
script sees. \strong{Both conditions are load-bearing}, and neither is
obvious from the formula:
\itemize{
\item With a distributional sigma (\code{bf(y ~ x, sigma ~ x)}), \code{sigma()}
returns \code{NA} with a warning, because there is no one number to
return, and the formula gives \code{NA} with it. Use
\code{frm_linpred(dpar = "sigma", type = "response")}, which reproduces
\code{fitted(dpar = "sigma")[, "Estimate"]} exactly (\code{identical()} is
\code{TRUE}).
\item Under truncation, \code{fitted()} is the TRUNCATED mean and the
formula answers a different question. With a lower bound \code{lb},
the truncated mean is \code{exp(mu + sigma^2 / 2) * pnorm(sigma - a) / pnorm(-a)} with \code{a = (log(lb) - mu) / sigma}, so the naive
formula falls short by a relative \code{1 - pnorm(-a) / pnorm(sigma - a)} in each row. That is an identity, not an estimate, and there
is no single number for it: it depends on where the bound sits in
each row's distribution, near zero for a row far above the bound
and approaching one for a row whose mean is below it
(\code{dev/generics-trunc.R} sweeps three bounds on one design).
}

Measured on one 400-row lognormal fit against a two-chain brms fit
of the same model (\code{dev/generics-scale-brms.R}, seed 2026): the
largest relative disagreement in \code{fitted()} over the 400 rows is
0.0106, which is 0.113 of brms's own posterior standard deviation
for that row and 0.058 of it at the median. \code{predict()} disagrees
by roughly 440 at the median row and 765 at row 1, because it is
a different scale rather than a different answer. Those two are
given loosely on purpose: brms's \code{predict()} summarizes fresh
predictive draws, so it moves between calls on one fit, where
the \code{fitted()} figures above are deterministic and repeat to the
last digit.
}

\examples{
set.seed(2026)
dd <- data.frame(x = rnorm(200))
dd$y <- exp(rnorm(200, 8 + 0.4 * dd$x, 0.4))
fit <- frm(bf(y ~ x) + lognormal(), data = dd)

# predict() summarizes the predictive distribution; frm_linpred()
# is the linear predictor and fitted() the expected response
head(predict(fit, ndraws = 200))
head(frm_linpred(fit))
head(fitted(fit))

# the last two are related by the family's own mean
head(exp(frm_linpred(fit) + sigma(fit)^2 / 2) -
       fitted(fit)[, "Estimate"])

# a sigma nobody wrote a formula for is a distributional parameter
# on its own scale, not a coefficient on its link
summary(fit)$spec_pars
sigma(fit)
}
\seealso{
\code{\link[=predict.frmtmb_fit]{predict.frmtmb_fit()}}, \code{\link[=frm_linpred]{frm_linpred()}},
\code{\link[=fitted.frmtmb_fit]{fitted.frmtmb_fit()}},
\code{\link[=residuals.frmtmb_fit]{residuals.frmtmb_fit()}}, \code{\link[=sigma.frmtmb_fit]{sigma.frmtmb_fit()}}, \code{\link[=fixef]{fixef()}},
\code{\link[=VarCorr]{VarCorr()}}, \code{\link[=confint.frmtmb_fit]{confint.frmtmb_fit()}}
}

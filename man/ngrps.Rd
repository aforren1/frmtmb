% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-draws.R, R/sugar.R
\name{ngrps.frmtmb_draws}
\alias{ngrps.frmtmb_draws}
\alias{ngrps}
\alias{ngrps.frmtmb_fit}
\title{Number of levels per random-effect grouping factor}
\usage{
\method{ngrps}{frmtmb_draws}(object, ...)

ngrps(object, ...)

\method{ngrps}{frmtmb_fit}(object, ...)
}
\arguments{
\item{object}{A \code{frmtmb_fit}.}

\item{...}{Unused.}
}
\value{
A named integer vector (smooth terms are excluded).
}
\description{
Number of levels per random-effect grouping factor
}
\examples{
set.seed(1)
dd <- data.frame(x = rnorm(100),
                 g = factor(rep(1:10, 10)),
                 h = factor(rep(1:4, each = 25)))
dd$y <- rnorm(100, 1 + 0.5 * dd$x + rnorm(10, 0, 0.8)[dd$g], 1)
fit <- frm(bf(y ~ x + (1 | g) + (1 | h)) + gaussian(), data = dd)

# one count per distinct grouping factor
ngrps(fit)
# the count that decides whether a variance component is trustworthy,
# and the unit influence() deletes when given `groups`
ngrps(fit)[["h"]]
}

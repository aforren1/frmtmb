% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sugar.R
\name{refit}
\alias{refit}
\alias{refit.frmtmb_fit}
\title{Refit a model to a new response}
\usage{
refit(object, newresp, ...)

\method{refit}{frmtmb_fit}(object, newresp, start = NULL, ...)
}
\arguments{
\item{object}{A \code{frmtmb_fit} for a univariate model.}

\item{newresp}{Replacement response: a vector of the original length,
or a matrix of the original dimensions for matrix responses.}

\item{...}{Unused.}

\item{start}{Optional named start list (as in \code{\link[=frm]{frm()}}); when given it
replaces the warm start.}
}
\value{
A new \code{frmtmb_fit}.
}
\description{
Reuses the assembled design (no formula parsing, no frame assembly)
and warm-starts the optimizer at the previous estimates, so a refit
costs one re-tape plus the optimization. This is the engine for
parametric bootstrap: simulate responses with \code{\link[=simulate]{simulate()}}, refit to
each.
}
\examples{
set.seed(2)
dd <- data.frame(x = rnorm(80), g = factor(rep(1:8, 10)))
dd$y <- rnorm(80, 1 + 0.5 * dd$x + rnorm(8, 0, 0.5)[dd$g], 1)
fit <- frm(bf(y ~ x + (1 | g)) + gaussian(), data = dd)
# refit to a simulated response (the parametric-bootstrap step)
ysim <- simulate(fit, nsim = 1, re.form = NA)[[1]]
rf <- refit(fit, ysim)
fixef(rf)
}

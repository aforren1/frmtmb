% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{frmtmb_priorlist-columns}
\alias{frmtmb_priorlist-columns}
\alias{$.frmtmb_priorlist}
\alias{$<-.frmtmb_priorlist}
\title{Column access on a prior specification}
\usage{
\method{$}{frmtmb_priorlist}(x, name)

\method{$}{frmtmb_priorlist}(x, name) <- value
}
\arguments{
\item{x}{A \code{frmtmb_priorlist}.}

\item{name}{A column: \code{prior}, \code{class}, \code{coef}, \code{group}, \code{resp},
\code{dpar}, \code{nlpar}, \code{lb}, \code{ub} or \code{source}.}

\item{value}{The new column, recycled as a data frame column is.}
}
\value{
For \code{$}, a character vector with one element per
specification. For \verb{$<-}, the rebuilt \code{frmtmb_priorlist}.
}
\description{
A \code{frmtmb_priorlist} holds parsed specifications, and brms's prior
object is a data frame of strings. \code{$} reads the columns brms's
object has, from the table \code{as.data.frame()} builds, so
\code{set_prior("normal(0, 2)", class = c("b", "sd"))$class} is
\code{c("b", "sd")} in both packages. The name must match a column
exactly; anything else is \code{NULL}, as it is on a data frame.
Assigning a column rebuilds every specification from the edited
table with \code{\link[=set_prior]{set_prior()}}, so an edit is checked as a new call would
be.
}
\examples{
pr <- set_prior("normal(0, 2)", class = c("b", "sd"))
pr$class
pr$prior[2] <- "exponential(1)"
pr
}

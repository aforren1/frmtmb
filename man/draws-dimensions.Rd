% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-draws.R
\name{draws-dimensions}
\alias{draws-dimensions}
\alias{ndraws}
\alias{ndraws.frmtmb_draws}
\alias{nchains}
\alias{nchains.frmtmb_draws}
\alias{niterations}
\alias{niterations.frmtmb_draws}
\alias{nvariables}
\alias{nvariables.frmtmb_draws}
\title{Size of a draws object}
\usage{
ndraws(x)

\method{ndraws}{frmtmb_draws}(x)

nchains(x)

\method{nchains}{frmtmb_draws}(x)

niterations(x)

\method{niterations}{frmtmb_draws}(x)

nvariables(x)

\method{nvariables}{frmtmb_draws}(x)
}
\arguments{
\item{x}{A \code{frmtmb_draws} from \code{\link[=frm_sample]{frm_sample()}}.}
}
\value{
A single integer.
}
\description{
\code{ndraws()} counts the post-warmup draws (all chains pooled),
\code{niterations()} the draws per chain, \code{nchains()} the chains and
\code{nvariables()} the sampled parameters. The names and meanings are
posterior's; frmtmb registers methods with posterior so that the
generics work whether or not that package is attached.
}
\examples{
\donttest{
if (requireNamespace("tmbstan", quietly = TRUE) &&
    requireNamespace("rstan", quietly = TRUE)) {
  set.seed(9)
  dd <- data.frame(x = rnorm(60), g = factor(rep(1:6, 10)))
  dd$y <- rnorm(60, 1 + 0.5 * dd$x + rnorm(6, 0, 0.5)[dd$g], 1)
  ds <- frm_sample(bf(y ~ x + (1 | g)), family = gaussian(),
                   data = dd, chains = 1, iter = 500, refresh = 0)
  c(ndraws(ds), niterations(ds), nchains(ds), nvariables(ds))
}
}
}

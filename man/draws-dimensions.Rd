% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draws-generics.R
\name{draws-dimensions}
\alias{draws-dimensions}
\alias{ndraws}
\alias{nchains}
\alias{niterations}
\alias{nvariables}
\alias{ndraws.frmtmb_fit}
\alias{nchains.frmtmb_fit}
\alias{niterations.frmtmb_fit}
\alias{nvariables.frmtmb_fit}
\title{Size of a draws object}
\usage{
ndraws(x)

nchains(x)

niterations(x)

nvariables(x, ...)

\method{ndraws}{frmtmb_fit}(x)

\method{nchains}{frmtmb_fit}(x)

\method{niterations}{frmtmb_fit}(x)

\method{nvariables}{frmtmb_fit}(x, ...)
}
\arguments{
\item{x}{An object holding draws.}

\item{...}{Unused. Carried because posterior's \code{nvariables()}
generic has it and frmtmb hands these generics back to
posterior when posterior is loaded.}
}
\value{
A single integer.
}
\description{
\code{ndraws()} counts the post-warmup draws (all chains pooled),
\code{niterations()} the draws per chain, \code{nchains()} the chains and
\code{nvariables()} the sampled parameters. The names and meanings are
posterior's; frmtmb registers methods with posterior so that the
generics work whether or not that package is attached.
}
\details{
As with the \code{as_draws} family, the objects that answer these with a
number come from \code{frmtmb.sample::frm_sample()}.
}
\examples{
dd <- data.frame(y = rnorm(40), x = rnorm(40))
fits <- frm_multiple(bf(y ~ x) + gaussian(), data = list(dd, dd))
try(ndraws(fits))
}

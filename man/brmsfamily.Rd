% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{brmsfamily}
\alias{brmsfamily}
\title{Build a family by name, with a link for any of its parameters}
\usage{
brmsfamily(family, link = NULL, ...)
}
\arguments{
\item{family}{Family name, as a single string.}

\item{link}{The link for the mean, quoted or not. \code{NULL} gives the
family's default. See \link{frmtmb-links} for the set each family takes.}

\item{...}{Any \verb{link_<dpar>} argument the named family takes, and its
other constructor arguments. Every one has to be named.}
}
\value{
A \code{frmtmb_family} object, ready for \code{\link[=frm]{frm()}}.
}
\description{
\code{brmsfamily()} is brms's generic family constructor. It names the
family with a string, takes the link for the mean as the second
argument, and takes a \verb{link_<dpar>} argument for each of the
family's other distributional parameters.
}
\details{
The family constructors take the same arguments, so
\code{student(link_sigma = "softplus")} does not need it. They exist
for the families frmtmb has no constructor of its own for.
\code{gaussian()}, \code{poisson()}, \code{binomial()} and \code{Gamma()} come from
'stats', which knows nothing of a \code{link_sigma} and refuses several
links brms allows for the mean. frmtmb does NOT shadow them: a
\code{gaussian()} that answered a \code{frmtmb_family} would break every
\code{glm()} call in an attached session.

The name follows brms's spelling rules: case does not matter,
\code{"normal"} is \code{"gaussian"}, \code{"com_poisson"} is \code{"compois"}, and the
prefixes \code{"zi_"} and \code{"hu_"} stand for \code{"zero_inflated_"} and
\code{"hurdle_"}. The link may be unquoted, as in
\code{brmsfamily("gaussian", inverse)}.

The family and its link as one character vector,
\code{c("weibull", "log")}, is the form brms accepts for \verb{family =} in a
model call, and \code{\link[=frm]{frm()}} accepts it there. \code{brmsfamily()} refuses it,
as brms does.
}
\section{Differences from brms}{

An argument the family does not have is refused by name. brms
ignores it, so \code{brmsfamily("poisson", link_sigma = "log")} builds a
poisson family there. \code{threshold} and \code{refcat} are not arguments.
}

\examples{
# sigma on a softplus: gaussian() from 'stats' has no link_sigma
brmsfamily("gaussian", link_sigma = "softplus")

# a mean link stats::poisson() refuses, and brms's short names
brmsfamily("poisson", softplus)$link
brmsfamily("zi_poisson")$link_zi

set.seed(2)
d <- data.frame(x = rnorm(60))
d$y <- rnorm(60, 1 + d$x, exp(0.3 * d$x))
frm(bf(y ~ x, sigma ~ x),
    family = brmsfamily("gaussian", link_sigma = "softplus"), data = d)
}
\seealso{
\link{frmtmb-links} for the links and which parameter takes
which, \link{frmtmb-families} for the constructors.
}

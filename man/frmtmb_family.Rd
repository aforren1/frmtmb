% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{frmtmb_family}
\alias{frmtmb_family}
\alias{custom_family}
\title{Define a model family}
\usage{
frmtmb_family(
  family,
  dpars,
  links,
  lpdf,
  valid_y = NULL,
  init_dpars = list(),
  type = "continuous",
  post = list(),
  sim = NULL,
  primary_dpars = "mu",
  lcdf = NULL,
  extra_pars = NULL,
  drop_intercept = FALSE
)

custom_family(
  family,
  dpars,
  links,
  lpdf,
  valid_y = NULL,
  init_dpars = list(),
  type = "continuous",
  post = list(),
  sim = NULL,
  primary_dpars = "mu",
  lcdf = NULL,
  extra_pars = NULL,
  drop_intercept = FALSE
)
}
\arguments{
\item{family}{Character name of the family.}

\item{dpars}{Character vector of distributional parameter names. The
first entry must be \code{"mu"}.}

\item{links}{Named list mapping each dpar to a link name (see
\code{frmtmb:::frmtmb_links}) or a link object.}

\item{lpdf}{Function \verb{(y, dpars, aterms)} returning the vectorized
log-density. \code{dpars} is a named list of advector vectors; \code{aterms} is a
named list of numeric addition-term values (for example \code{trials}).}

\item{valid_y}{Optional function \verb{(y, aterms)} that signals an error for
invalid responses. Called once at assembly time.}

\item{init_dpars}{Optional named list of functions \verb{(y, aterms)} giving a
response-scale starting value per dpar (applied to the intercept
through the link).}

\item{type}{One of \code{"continuous"}, \code{"discrete"}, \code{"ordinal"},
\code{"categorical"}. The last two say the modelled response is a
distribution over \verb{1..K} categories rather than a number, so
\code{\link[=fitted]{fitted()}} returns an \verb{n x K} probability matrix; \code{"ordinal"}
shares one latent predictor across the categories and
\code{"categorical"} gives each non-reference category its own.}

\item{post}{Named list of numeric helper functions used by
\code{\link[=fitted]{fitted()}}, \code{\link[=predict]{predict()}} and \code{\link[=residuals]{residuals()}}: \code{mean_fn(dpars, aterms)} (the response mean), \code{var_fn(dpars, aterms)} (for pearson
residuals) and \code{dev_fn(y, dpars, aterms)} (the unit deviance
\verb{2 * (loglik of the saturated fit - loglik at the fitted value)},
for \code{residuals(type = "deviance")}). A family that omits one is
refused by the method that needs it.}

\item{sim}{Optional numeric simulator \verb{(dpars, aterms, n)} returning \code{n}
response draws; used by \code{\link[=simulate]{simulate()}}.}

\item{primary_dpars}{Which dpars receive the main model formula
(default \code{"mu"}). Families with several location predictors (for
example multinomial's per-category \code{mu2}, \code{mu3}, ...) list them all;
these live in the \code{beta} parameter vector and are integrated out
under REML.}

\item{lcdf}{Optional vectorized AD log-safe CDF \verb{(q, dpars, aterms)}
returning probabilities; enables \code{cens()} and \code{trunc()} addition
terms.}

\item{extra_pars}{Optional function \verb{(y, aterms)} returning a named
list of numeric starting vectors for family-level parameters outside
the dpar system (for example ordinal thresholds). They join the
parameter template under their own names and reach \code{lpdf} as its
fourth argument.}

\item{drop_intercept}{If \code{TRUE}, the intercept column is removed from
the main formula's design matrix (ordinal families: thresholds take
its place).}
}
\value{
An object of class \code{frmtmb_family}.
}
\description{
Constructs a family object for use with \code{\link[=frm]{frm()}}. The log-density
function must be vectorized and AD-compatible: it is evaluated on RTMB
'advector' objects during taping, so it must use RTMB-overloaded
operations (RTMB and RTMBdist \verb{d*} functions, plain arithmetic) and must
not branch on parameter values.
}
\examples{
# a custom family is a plain R log-density over taped parameters
dd <- data.frame(y = rbinom(100, 5, 0.4),
                 size = 5, x = rnorm(100))
fam <- custom_family(
  "vbinom", dpars = "mu", links = list(mu = "logit"),
  lpdf = function(y, dpars, aterms) {
    RTMB::dbinom(y, aterms$vint1, dpars$mu, log = TRUE)
  },
  type = "discrete"
)
fit <- frm(bf(y | vint(size) ~ x) + fam, data = dd)
fixef(fit)
}

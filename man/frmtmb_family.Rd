% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{frmtmb_family}
\alias{frmtmb_family}
\alias{custom_family}
\title{Define a model family}
\usage{
frmtmb_family(
  family,
  dpars,
  links,
  lpdf,
  valid_y = NULL,
  init_dpars = list(),
  type = "continuous",
  post = list(),
  sim = NULL,
  sim_ctx = NULL,
  sim_refusal = NULL,
  primary_dpars = "mu",
  lcdf = NULL,
  lccdf = NULL,
  required_aterms = character(0),
  accepts_aterms = NULL,
  family_finalize = NULL,
  extra_pars = NULL,
  drop_intercept = FALSE,
  structure = NULL
)

custom_family(
  family,
  dpars,
  links,
  lpdf,
  valid_y = NULL,
  init_dpars = list(),
  type = "continuous",
  post = list(),
  sim = NULL,
  sim_ctx = NULL,
  sim_refusal = NULL,
  primary_dpars = "mu",
  lcdf = NULL,
  lccdf = NULL,
  required_aterms = character(0),
  accepts_aterms = NULL,
  family_finalize = NULL,
  extra_pars = NULL,
  drop_intercept = FALSE,
  structure = NULL
)
}
\arguments{
\item{family}{Character name of the family.}

\item{dpars}{Character vector of distributional parameter names. The
first entry must be \code{"mu"}.}

\item{links}{Named list mapping each dpar to a link name or a link
object. \link{frmtmb-links} lists the names and says what a link object
has to carry.}

\item{lpdf}{Function \verb{(y, dpars, aterms)} returning the vectorized
log-density. \code{dpars} is a named list of advector vectors; \code{aterms} is a
named list of numeric addition-term values (for example \code{trials}).}

\item{valid_y}{Optional function \verb{(y, aterms)} that signals an error for
invalid responses. Called once at assembly time.}

\item{init_dpars}{Optional named list of functions \verb{(y, aterms)} giving a
response-scale starting value per dpar (applied to the intercept
through the link).}

\item{type}{One of \code{"continuous"}, \code{"discrete"}, \code{"ordinal"},
\code{"categorical"}. The last two say the modelled response is a
distribution over \verb{1..K} categories rather than a number, so
\code{\link[=fitted]{fitted()}} returns an \verb{n x K} probability matrix; \code{"ordinal"}
shares one latent predictor across the categories and
\code{"categorical"} gives each non-reference category its own.}

\item{post}{Named list of numeric helper functions used by
\code{\link[=fitted]{fitted()}}, \code{\link[=predict]{predict()}} and \code{\link[=residuals]{residuals()}}: \code{mean_fn(dpars, aterms)} (the response mean), \code{var_fn(dpars, aterms)} (for pearson
residuals) and \code{dev_fn(y, dpars, aterms)} (the unit deviance
\verb{2 * (loglik of the saturated fit - loglik at the fitted value)},
for \code{residuals(type = "deviance")}). A family that omits one is
refused by the method that needs it.

\code{post$fit_check(fit, resp)} is different in kind: it is run once,
when a fit FINISHES, and its return value is discarded. It is where
a family says something about where the optimizer landed, which
nothing else can: \code{logLik()} reads the optimizer's own value, so a
family whose likelihood is floored or degenerate in some region had
no way to report it. Warn from it rather than stopping; a hook that
throws is caught, reported as a warning naming the family, and the
fit is returned regardless.}

\item{sim}{Optional numeric simulator \verb{(dpars, aterms, n)} returning \code{n}
response draws; used by \code{\link[=simulate]{simulate()}}, \code{posterior_predict()} and
\code{\link[=frm_simulate]{frm_simulate()}}. It is stateless and rowwise: it sees the
distributional parameters and nothing else. A family whose extra
parameters (\code{extra_pars}) enter the draw declares a fourth
argument \code{extra} instead.}

\item{sim_ctx}{Optional structured simulator \code{(ctx)} for a family
whose draws are not rowwise (see Structured simulators). It takes
precedence over \code{sim}.}

\item{sim_refusal}{Optional one-sentence reason why the family has no
simulator, appended to the refusal each entry point raises. Use it
when the omission is a decision rather than a gap.}

\item{primary_dpars}{Which dpars receive the main model formula
(default \code{"mu"}). Families with several location predictors (for
example multinomial's per-category \code{mu2}, \code{mu3}, ...) list them all;
these live in the \code{beta} parameter vector and are integrated out
under REML.}

\item{lcdf}{Optional vectorized AD log-safe CDF \verb{(q, dpars, aterms)}
returning probabilities; enables \code{cens()} and \code{trunc()} addition
terms. It is the only thing either term asks of a family, whatever
the family's \code{type}: a discrete family that supplies one is
censored under the inclusive convention (see Censoring a discrete
response), which reads a lower bound as \code{F(q - 1)} and so calls
this at one below a recorded value.}

\item{lccdf}{Optional vectorized AD LOG SURVIVOR function
\verb{(q, dpars, aterms)} returning \code{log(1 - F(q))} directly. A family
that declares it scores a RIGHT-censored row from it instead of
from \code{log(1 - F)}, which cannot be accurate once \code{F} rounds to one
(see Right censoring and the representable tail). It is optional
and independent of \code{lcdf}: a family that supplies only \code{lccdf}
accepts right censoring and refuses left censoring, interval
censoring and \code{trunc()}, each by name.}

\item{required_aterms}{The addition-term values the density cannot do
without, named as they reach \code{aterms}: \code{"vint1"}, \code{"vreal2"},
\code{"trials"}. A character vector names the terms it needs ALL of. A
LIST adds the alternative: an element of length one is a term that
must be there, and an element of length more than one is a set of
spellings, ANY one of which will do, so
\code{required_aterms = list(c("dec", "vint1"), "vreal1")} reads as one
of \code{dec} or \code{vint1}, and \code{vreal1}. A family that reads the same
datum from either of two terms declares it that way instead of
hand-rolling the refusal.

Frame assembly refuses a model that leaves a requirement unmet,
naming the family, the terms and the spelling that supplies them;
for a set of alternatives it names all of them and writes the first
into the example formula. Without the declaration an absent term
reaches the density as \code{NULL}, arithmetic on it gives \code{numeric(0)},
and the log-likelihood becomes a sum over nothing: a fit that
returns, with a log-likelihood of zero. Declare every per-row datum
the density indexes.}

\item{accepts_aterms}{The addition terms this family reads or lets the
core act on, named as a formula writes them and without
parentheses: \code{c("weights", "trials", "cens")}. Frame assembly
refuses any other term on the response, by name, and lists the ones
the family takes. \code{character(0)} declares a family that takes none.
\code{NULL}, the default, accepts every registered term, which is what a
family written before this argument existed keeps.

\code{required_aterms} is a conjunction of what the density cannot do
without; this is the complementary allow-list, and the two are read
together, so a required term need not be repeated here. Without a
declaration an unread term is parsed, stored on the fit and
silently ignored: \code{wiener()} accepted a \code{vint()} it cannot use, and
\code{lba()} accepted a \code{dec()}, both giving a fit bit-identical to the
one without the term.

Naming \code{"se"} here is more than an allow-list entry: it is how a
family OPTS IN to \code{se()}. That term is the one whose entire effect
is inside the density - the core hands over \code{aterms[["se"]]} and
maps out the residual scale it replaces, and does nothing else with
it - so a family that does not declare it is refused the term
rather than given one it would ignore. \code{NULL}, which accepts every
other term, declares nothing and so does not open this one. Read
\code{aterms[["se"]]} as the known standard deviation, and
\code{aterms[["se_sigma"]]} to honor \code{se(x, sigma = TRUE)}, which asks
for the known and estimated scales in quadrature.}

\item{family_finalize}{Optional function \verb{(fam, y, aterms)} returning
a family. It runs once at frame assembly, after the response is
coerced and validated and before any link is used, and whatever it
returns is the family the rest of the fit sees. It is how a family
derives a link bound, a default, or an extra slot FROM the data
instead of asking the user for a quantity the framework already
holds (see Deriving a family from the data).}

\item{extra_pars}{Optional function \verb{(y, aterms)} returning a named
list of numeric starting vectors for family-level parameters outside
the dpar system (for example ordinal thresholds). They join the
parameter template under their own names and reach \code{lpdf} as its
fourth argument.}

\item{drop_intercept}{If \code{TRUE}, the intercept column is removed from
the main formula's design matrix (ordinal families: thresholds take
its place).}

\item{structure}{Optional \code{\link[=frmtmb_structure]{frmtmb_structure()}} for a family whose
likelihood does not factorize over rows (a group-level \code{\link[=mixture]{mixture()}},
a hidden Markov chain). It carries the non-rowwise log-likelihood,
the frame block that likelihood reads, and the capability flags
that say which post-fit methods the family can answer. \code{lpdf} stays
required even then, for the rowwise contract, and may be a stub
that refuses.}
}
\value{
An object of class \code{frmtmb_family}.
}
\description{
Constructs a family object for use with \code{\link[=frm]{frm()}}. The log-density
function must be vectorized and AD-compatible: it is evaluated on RTMB
'advector' objects during taping, so it must use RTMB-overloaded
operations (RTMB and RTMBdist \verb{d*} functions, plain arithmetic) and must
not branch on parameter values.
}
\section{Structured simulators}{

Some families cannot draw a response one row at a time: a group-level
\code{\link[=mixture]{mixture()}} draws one class per group, a \code{\link[=mixture_mvn]{mixture_mvn()}} draw needs
the class covariances, which are family-level extras rather than
dpars, and a hidden Markov family walks a chain per sequence. Those
families supply \code{sim_ctx(ctx)} instead of \code{sim(dpars, aterms, n)}.

\code{ctx} is a list with \code{fit} (any object carrying \code{spec}, \code{frame} and
\code{estimates} - a fitted model, one posterior draw, or the de novo
shim), \code{family}, \code{rspec}, \code{resp}, \code{dpars} (the evaluated numeric
distributional parameters), \code{aterms}, \code{n}, \code{extra} (the family-level
extra parameters) and the frame structures \code{autocor} and \code{block}
(the structured family's own data; see \code{\link[=frmtmb_structure]{frmtmb_structure()}}).
Read its fields with \verb{[[ ]]}.

The same \code{sim_ctx()} serves [simulate()], \code{posterior_predict()} and
[frm_simulate()]. Because a structured draw covers whole sequences or
groups, \code{trunc()} rejection and \code{newdata} cannot apply to it and are
refused.

[ ]: R:\%20
[simulate()]: R:simulate()
[frm_simulate()]: R:frm_simulate()
}

\section{Slot call order}{

The order the slots run in is part of the contract, because a family
that derives anything from the data depends on it. Measured on an
instrumented family, one fit of one response:
\enumerate{
\item \code{valid_y(y, aterms)}, once, at frame assembly, with the response
coerced and the addition terms evaluated.
\item \code{family_finalize(fam, y, aterms)}, once, immediately after, and
still before any link function is called.
\item \code{aterm_data()} and \code{extra_pars(y, aterms)}, once each, at
assembly.
\item \code{init_dpars[[dpar]](y, aterms)}, once per dpar, when the starting
values are built; each value goes straight through that dpar's
\code{linkfun}.
\item \code{linkinv} and \code{lpdf}, on the tape, from then on.
}

\code{post$mean_fn}, \code{post$var_fn}, \code{post$dev_fn} and \code{sim} are never
called by \code{\link[=frm]{frm()}}. They run only when a post-fit method asks for
them.

So a link may read anything \code{valid_y} or \code{family_finalize} computed,
and neither of those may read anything a link produced.
}

\section{Deriving a family from the data}{

Some families are not fully determined until the response is in hand.
A shifted family whose density is zero below a non-decision time
wants a link bounded above by \code{min(y)}, so that the constraint is
structural rather than left to the optimizer. The bound is a property
of the data, and the family object is built before \code{\link[=frm]{frm()}} sees any.

\code{family_finalize} closes that gap. It receives the family, the
coerced response and the evaluated addition terms, and returns the
family the fit will use:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{shifted <- frmtmb_family(
  "shifted", dpars = c("mu", "ndt"),
  links = list(mu = "log", ndt = "log"),
  lpdf = function(y, dpars, aterms) \{ ... \},
  family_finalize = function(fam, y, aterms) \{
    # a logit onto (0, min(y)): ndt cannot reach the smallest
    # observation, whatever the optimizer tries
    ub <- min(y)
    fam$links$ndt <- list(
      name    = paste0("scaled_logit(0, ", signif(ub, 4), ")"),
      linkfun = function(mu) log(mu / (ub - mu)),
      linkinv = function(eta) ub / (1 + exp(-eta)),
      mu_eta  = function(eta) \{
        p <- 1 / (1 + exp(-eta))
        ub * p * (1 - p)
      \}
    )
    fam
  \}
)
}\if{html}{\out{</div>}}

Replacing a link this way replaces it everywhere: the starting
values, the tape, and every post-fit method read the finalized
family. The alternative an extension reached for before this slot
existed was to have \code{valid_y} write the bound into an environment the
link closures read at call time, which works only for as long as the
call order happens to hold and leaves the family object lying about
what it is.
}

\section{Right censoring and the representable tail}{

Core forms a right-censored row's contribution as \code{log(Fub - F(y))},
which without truncation is \code{log(1 - F)}. A double cannot represent
the complement of a probability that has rounded to one, so past that
point the contribution is not merely inaccurate: it is CONSTANT, and
its gradient is exactly zero. An optimizer then prices such a row the
same however far it moves, and fits every other row as if the
survivor were free. That failure is silent - the fit converges, and
\code{logLik()} and \code{AIC()} report the floored number.

\code{lccdf} removes the class for right censoring by giving core the
quantity it actually needs. Measured on a standard normal tail, one
process:

\tabular{lrr}{
\strong{z} \tab \strong{log(1 - pnorm(z))} \tab
\strong{pnorm(z, lower.tail = FALSE, log.p = TRUE)} \cr
8.0 \tab -35.013 \tab -35.013 \cr
8.3 \tab -Inf \tab -37.494 \cr
37 \tab -Inf \tab -689.031 \cr
500 \tab -Inf \tab -125007.13
}

Both the value and the derivative are exact on the right, over the
whole range.

The built-in families that declare \code{lccdf} are \code{\link[=gaussian]{gaussian()}},
\code{\link[=lognormal]{lognormal()}}, \code{\link[=exponential]{exponential()}}, \code{\link[=weibull]{weibull()}} and \code{\link[=cox]{cox()}}. The last
three have \verb{log S} in closed form (\code{-q/mu}, \code{-(q/scale)^shape},
\code{-H0(t) * mu}); the first two use \code{pnorm()}'s own log upper tail.

Two families with a CDF do NOT declare one, for measured reasons.
\code{\link[=inverse.gaussian]{inverse.gaussian()}} gains nothing:
\code{RTMBdist::pinvgauss(lower.tail = FALSE, log.p = TRUE)} is computed
on the probability scale and reaches \code{-Inf} at the same
\verb{log S = -34} that \code{log(1 - F)} does. \code{\link[=poisson]{poisson()}} is censored (see
Censoring a discrete response) and would use the slot, but cannot
declare one: \code{RTMB::ppois(lower.tail = FALSE, log.p = TRUE)} is
exact in R (\code{-2773.28} at \code{q = 700}, \code{lambda = 5}, where
\code{log(1 - F)} is \code{-Inf})
and does not TAPE - inside \code{MakeADFun} it reaches
\code{stats::ppois} and errors with "Non-numeric argument to mathematical
function". An exact discrete log survivor has to be written out
before poisson can have one.

\code{lccdf} fixes RIGHT censoring and nothing else. Left censoring is
still \code{log(F(y) - Flb)}, interval censoring is still a difference of
CDFs, and the truncation normalizer is still \code{log(Fub - Flb)}, so a
LEFT-TRUNCATED survival model - delayed entry, which is routine -
meets the identical representability problem from the other side.
Closing that needs a windowed log-difference slot, and this is the
first step rather than the last one.
}

\section{Censoring a discrete response}{

A censoring bound on a discrete response NAMES a value the response
can take, and the value is INCLUDED in the event:

\tabular{lll}{
\strong{code} \tab \strong{means} \tab \strong{scored as} \cr
\code{0} "none" \tab \code{Y == y} \tab \code{f(y)} \cr
\code{-1} "left" \tab \code{Y <= y} \tab \code{F(y)} \cr
\code{1} "right" \tab \code{Y >= y} \tab \code{1 - F(y - 1)} \cr
\code{2} "interval" \tab \verb{y <= Y <= y2} \tab \code{F(y2) - F(y - 1)}
}

Equivalently: every LOWER edge enters the CDF as \code{F(edge - 1)}, and
upper edges are unchanged because \code{F} already includes its argument.
It is the rule \code{trunc(lb = )} has always followed on a discrete
response, so one number means one thing however a response is
bounded, and it is what a count recorded as "5 or more" means.

It DIFFERS from brms for RIGHT and INTERVAL censoring only, where
brms emits \code{poisson_lccdf(y | mu)}, that is \code{P(Y > y)}, and reads an
interval as \verb{(y, y2]}. LEFT censoring is \code{P(Y <= y)} in both packages
and agrees exactly. Migrating a right- or interval-censored count
model changes its log-likelihood; on 200 poisson draws at
\code{lambda = 4} right censored at 6, the two readings differ by 20.8 log
units and 3.7 percent of the estimate. Subtract one from every
right-censored and interval lower bound to reproduce a brms fit.

The divergence is deliberate, because \strong{brms is internally
inconsistent here and frmtmb cannot be both.} brms's own discrete
truncation emits \code{poisson_lccdf(lb - 1 | mu)}, an INCLUSIVE lower
bound, \code{P(Y >= lb)}. So in brms \code{trunc(lb = 6)} means \code{Y >= 6} while
a right-censored row recorded at 6 means \code{Y > 6}: one number, two
meanings, on one response. frmtmb's \code{trunc()} reproduces brms bit for
bit (poisson on \verb{y = 3,4,5,6} at \verb{b0 = log 4}, \code{trunc(lb = 2)} gives
6.9990718955 under both, against 6.2954805379 for the exclusive
reading), so its \code{cens()} had to choose between matching brms's
censoring and matching its own truncation. It matches its own.

It is also the only reading consistent with the censored SIMULATOR,
which predates all of this: \code{simulate(censored = TRUE)} caps a draw
with \code{pmin(pmax(y, lo), hi)}, recording the value \code{k} exactly when
the latent draw is \verb{>= k}. Measured on 4000 draws from a fit censored
at 7, the simulated mass at that point is 0.13250, against
\code{P(Y >= 7) = 0.12190} inclusive and \code{P(Y > 7) = 0.05763} exclusive.
The other reading would silently decouple the likelihood from the
simulator that \code{dharma_residuals()} rests on.

Two consequences worth knowing. A one-point interval (\code{y2 == y}) is
legal and is the exact observation \code{P(Y = y)}, where on a continuous
response it is refused as an event of probability zero. And the
shift assumes the support is the unit integer lattice, so a
non-integer censoring bound is refused rather than moved onto a
point the family has no mass at.

\code{residuals(type = "osa")} is refused on a censored discrete fit:
inclusive bounds make an uncensored row's support \verb{[lo + 1, hi - 1]}
rather than the \verb{[lo, hi]} the one-step window is built on.
}

\section{Tape-safe scope}{

\code{lpdf} and \code{lcdf} run with RTMB's tape-safe \code{c()}, \verb{[<-} and
\verb{diag<-} in scope automatically (the
\code{"c" <- RTMB::ADoverload("c")} boilerplate is spliced in unless the
function already binds it), so base spellings keep the
automatic-differentiation class. A helper the density CALLS still
needs its own bindings: lexical scope does not travel into other
functions.
}

\examples{
# a custom family is a plain R log-density over taped parameters
dd <- data.frame(y = rbinom(100, 5, 0.4),
                 size = 5, x = rnorm(100))
fam <- custom_family(
  "vbinom", dpars = "mu", links = list(mu = "logit"),
  lpdf = function(y, dpars, aterms) {
    RTMB::dbinom(y, aterms$vint1, dpars$mu, log = TRUE)
  },
  # the density indexes vint1, so a model without it is refused
  # rather than fitted against a zero-length log-likelihood
  required_aterms = "vint1",
  type = "discrete"
)
fit <- frm(bf(y | vint(size) ~ x) + fam, data = dd)
fixef(fit)
}
\seealso{
\code{\link[=frmtmb_structure]{frmtmb_structure()}} for a likelihood that does not
factorize over rows, \code{\link[=frmtmb_register_aterm]{frmtmb_register_aterm()}} for giving the
family's per-row data a name of its own instead of \code{vint()},
\code{\link[=frmtmb_register_compat]{frmtmb_register_compat()}} for telling \code{\link[=frm_compat]{frm_compat()}} what the
family does and does not combine with, and
\link{frmtmb-extension-api} for the accessors a family outside frmtmb
may use
}

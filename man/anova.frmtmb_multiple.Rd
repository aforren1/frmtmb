% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiple.R
\name{anova.frmtmb_multiple}
\alias{anova.frmtmb_multiple}
\title{Pooled model comparison across imputations (D1, D2, D3)}
\usage{
\method{anova}{frmtmb_multiple}(
  object,
  ...,
  method = c("D3", "D1", "D2"),
  use = c("likelihood", "wald"),
  constraint = NULL,
  dfcom = NULL
)
}
\arguments{
\item{object}{A \code{frmtmb_multiple} from \code{\link[=frm_multiple]{frm_multiple()}}.}

\item{...}{A second \code{frmtmb_multiple}, nested with \code{object} and fit
to the same imputed datasets.}

\item{method}{Combining rule: \code{"D3"}, \code{"D1"} or \code{"D2"}.}

\item{use}{For \code{method = "D2"}, whether the per-imputation
statistics are likelihood-ratio (\code{"likelihood"}) or Wald
(\code{"wald"}) chi-squares.}

\item{constraint}{Instead of a second model, the names of the
coefficients to test jointly against zero (\code{"D1"} and
\verb{method = "D2", use = "wald"} only).}

\item{dfcom}{Complete-data residual degrees of freedom for the
\code{"D1"} reference distribution. Defaults to \code{df.residual()} of the
larger model's first fit; \code{Inf} selects the unadjusted form.}
}
\value{
A one-row \code{frmtmb_pooled_anova} table: \code{statistic}, \code{df1},
\code{df2}, \code{p}, \code{riv} (the relative increase in variance).
}
\description{
The multiply-imputed counterpart of \code{\link[=anova.frmtmb_fit]{anova.frmtmb_fit()}}. A
likelihood-ratio test per imputation is not a test: the m statistics
have to be combined, and the three standard combining rules
(\code{mice::D1()}, \code{mice::D2()}, \code{mice::D3()}) each reference an F
distribution whose denominator degrees of freedom absorb the extra
between-imputation uncertainty.
}
\details{
\describe{
\item{\code{"D1"}}{Multivariate Wald (Li, Raghunathan and Rubin 1991).
Let \code{Qbar} be the pooled estimate of the \code{k} coefficients that
the larger model adds, \code{Ubar} their average within-imputation
covariance and \code{B} the between-imputation covariance. With the
average relative increase in variance r, which is
\verb{(1 + 1/m) tr(B Ubar^-1) / k}, the statistic is
\verb{Qbar' ((1 + r) Ubar)^-1 Qbar / k} on \code{k} and \code{v} degrees of
freedom. \code{v} uses the Reiter (2007) small-sample form when
\code{dfcom} is finite and the Li et al form otherwise.}
\item{\code{"D2"}}{Pooled test statistics (Li, Meng, Raghunathan and
Rubin 1991). From the \code{m} per-imputation statistics \code{d} - either
likelihood-ratio chi-squares (\code{use = "likelihood"}) or Wald
chi-squares (\code{use = "wald"}) - and r set to
\verb{(1 + 1/m) var(sqrt(d))}, the statistic is
\verb{(mean(d)/k - (m + 1)/(m - 1) r) / (1 + r)} on \code{k} and
\code{k^(-3/m) (m - 1) (1 + 1/r)^2} degrees of freedom. D2 needs no
covariance matrix, so it is the cheapest rule and the least
efficient.}
\item{\code{"D3"}}{Pooled likelihood ratio (Meng and Rubin 1992). The
average deviance difference is computed twice: at the
imputation-specific estimates (\code{dbar}) and at the pooled
estimates (\code{dtilde}), the second by evaluating each imputation's
own likelihood at one common parameter vector. With r set to
\code{(m + 1) / (k (m - 1)) (dbar - dtilde)}, the statistic is
\code{dtilde / (k (1 + r))}. D3 is the default: it needs no
covariance matrix either, and unlike D2 it stays valid when the
coefficients are far from normal.}
}

Pooled parameter values are the plain across-imputation means of the
optimizer's own parameter vector, so variance components are pooled
on the internal (log / Cholesky) scale, matching \verb{$pooled}. No model
is refit: each imputation's objective is re-evaluated at the pooled
vector, which costs one Laplace solve.

\code{mice::D3()} instead fixes the pooled coefficients as an offset and
re-estimates every remaining parameter, so it can differ by a few
percent; \code{mitml::testModels(method = "D3")} uses the plug-in form
implemented here. Note also that \code{df.residual()} counts the
dispersion parameter of a gaussian fit, which \code{lm()} does not, so
\code{dfcom} is one smaller than mice's default; pass \code{dfcom} explicitly
to reproduce mice exactly.
}
\examples{
set.seed(4)
n <- 60
imps <- lapply(1:4, function(i) {
  x <- rnorm(n)
  data.frame(y = rnorm(n, 1 + 0.5 * x), x = x, z = rnorm(n))
})
m1 <- frm_multiple(bf(y ~ x + z) + gaussian(), data = imps)
m0 <- frm_multiple(bf(y ~ x) + gaussian(), data = imps)
anova(m1, m0)
anova(m1, m0, method = "D1")
}
\references{
Li, K. H., Raghunathan, T. E. and Rubin, D. B. (1991).
Large-sample significance levels from multiply imputed data using
moment-based statistics and an F reference distribution.
\emph{JASA} 86, 1065-1073.

Li, K. H., Meng, X.-L., Raghunathan, T. E. and Rubin, D. B. (1991).
Significance levels from repeated p-values with multiply-imputed
data. \emph{Statistica Sinica} 1, 65-92.

Meng, X.-L. and Rubin, D. B. (1992). Performing likelihood ratio
tests with multiply-imputed data sets. \emph{Biometrika} 79, 103-111.
}
\seealso{
\code{\link[=frm_multiple]{frm_multiple()}}, \code{\link[=anova.frmtmb_fit]{anova.frmtmb_fit()}}.
}

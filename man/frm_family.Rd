% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{frm_family}
\alias{frm_family}
\title{Build a family by name, with a link for any of its parameters}
\usage{
frm_family(family, ...)
}
\arguments{
\item{family}{Family name, as a single string.}

\item{...}{\code{link}, and any \verb{link_<dpar>} argument the named family
takes. Every one has to be named.}
}
\value{
A \code{frmtmb_family} object, ready for \code{\link[=frm]{frm()}}.
}
\description{
The frmtmb family constructors take \code{link} and a \verb{link_<dpar>} for
each of their other distributional parameters, so
\code{student(link_sigma = "softplus")} needs nothing else and this is
not the usual way to reach a link.
}
\details{
It exists for the families frmtmb has no constructor of its own for.
\code{gaussian()}, \code{poisson()}, \code{binomial()} and \code{Gamma()} come from
'stats', which knows nothing of a \code{link_sigma} and refuses several
links brms allows for the mean. frmtmb does NOT shadow them: a
\code{gaussian()} that answered a \code{frmtmb_family} would break every
\code{glm()} call in an attached session. Naming the family instead
reaches every argument, which is what brms does with \code{brmsfamily()}.
}
\examples{
# sigma on a softplus: gaussian() from 'stats' has no link_sigma
frm_family("gaussian", link_sigma = "softplus")

# a mean link stats::poisson() refuses
frm_family("poisson", link = "softplus")

set.seed(2)
d <- data.frame(x = rnorm(60))
d$y <- rnorm(60, 1 + d$x, exp(0.3 * d$x))
frm(bf(y ~ x, sigma ~ x),
    family = frm_family("gaussian", link_sigma = "softplus"), data = d)
}
\seealso{
\link{frmtmb-links} for the links and which parameter takes
which, \link{frmtmb-families} for the constructors.
}

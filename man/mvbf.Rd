% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bf.R
\name{mvbf}
\alias{mvbf}
\alias{set_rescor}
\title{Combine formulas into a multivariate model}
\usage{
mvbf(..., rescor = FALSE)

set_rescor(rescor_value = TRUE)
}
\arguments{
\item{...}{\code{bf()} formulas, each with a family attached (or supply one
\code{family} to \code{\link[=frm]{frm()}} for all of them).}

\item{rescor}{Model residual correlation between the responses
(gaussian only).}

\item{rescor_value}{For \code{set_rescor()}: turn residual correlation on
or off.}
}
\value{
An object of class \code{frmtmb_mvformula}.
}
\description{
Each response keeps its own formula, family, dpar formulas, and
addition terms. Residual correlation between gaussian responses is
requested with \code{rescor = TRUE} or \code{\link[=set_rescor]{set_rescor()}}. Random-effect
correlation across responses uses the brms \verb{|ID|} syntax, e.g.
\code{(1 | p | g)} in several formulas correlates their \code{g} effects.
}
\details{
The linked terms merge into one covariance block, so they must all
name the same grouping specification. When they all write
\code{gr(g, cov = A)} (or all \code{gr(g, prec = Q)}) with the same matrix, the
merged block keeps it: its covariance is \verb{A (x) Sigma}, with \code{Sigma}
unstructured across the merged coefficients. A two-trait animal model
is therefore the same fit whether written across two responses with
\code{(1 | q | gr(id, cov = A))} or in long format as a single
\code{(0 + trait | gr(id, cov = A))}. Mixing structures under one key -
a plain \code{g} in one formula and \code{gr(g, cov = A)} in another, or
\verb{cov =} against \verb{prec =} - is refused, because a merged block has
room for one structure.
}
\examples{
set.seed(2)
n <- 160
dd <- data.frame(x = rnorm(n), g = factor(rep(1:16, 10)))
u <- cbind(rnorm(16, 0, 0.8), rnorm(16, 0, 0.8))
e <- rnorm(n)                      # a disturbance both responses see
dd$y1 <- 1 + 0.5 * dd$x + u[dd$g, 1] + e + rnorm(n, 0, 0.5)
dd$y2 <- 2 - 0.3 * dd$x + u[dd$g, 2] + e + rnorm(n, 0, 0.5)

# each response keeps its own formula and family
fit <- frm(mvbf(bf(y1 ~ x), bf(y2 ~ x)) + gaussian(), data = dd)
fixef(fit)

# rescor estimates the correlation of the residuals
fit_rc <- frm(mvbf(bf(y1 ~ x), bf(y2 ~ x), rescor = TRUE) + gaussian(),
              data = dd)
rescor_matrix(fit_rc)

# set_rescor() turns it on after the fact, and `+` also combines bf()s
mvbf(bf(y1 ~ x), bf(y2 ~ x)) + set_rescor(TRUE)
bf(y1 ~ x) + bf(y2 ~ x)

# |ID| correlates the random effects of the two responses
fit_id <- frm(mvbf(bf(y1 ~ x + (1 | p | g)), bf(y2 ~ x + (1 | p | g))) +
                gaussian(), data = dd)
VarCorr(fit_id)
}

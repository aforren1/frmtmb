% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draws-generics.R, R/multiple.R
\name{as_draws}
\alias{as_draws}
\alias{as_draws_matrix}
\alias{as_draws_array}
\alias{as_draws_df}
\alias{as_draws_list}
\alias{as_draws_rvars}
\alias{as_draws.frmtmb_fit}
\alias{as_draws_matrix.frmtmb_fit}
\alias{as_draws_array.frmtmb_fit}
\alias{as_draws_df.frmtmb_fit}
\alias{as_draws_list.frmtmb_fit}
\alias{as_draws_rvars.frmtmb_fit}
\alias{as_draws.frmtmb_multiple}
\title{Convert to a posterior draws object}
\usage{
as_draws(x, ...)

as_draws_matrix(x, ...)

as_draws_array(x, ...)

as_draws_df(x, ...)

as_draws_list(x, ...)

as_draws_rvars(x, ...)

\method{as_draws}{frmtmb_fit}(x, ...)

\method{as_draws_matrix}{frmtmb_fit}(x, ...)

\method{as_draws_array}{frmtmb_fit}(x, ...)

\method{as_draws_df}{frmtmb_fit}(x, ...)

\method{as_draws_list}{frmtmb_fit}(x, ...)

\method{as_draws_rvars}{frmtmb_fit}(x, ...)

\method{as_draws}{frmtmb_multiple}(x, ...)
}
\arguments{
\item{x}{An object holding draws.}

\item{...}{Passed to methods.}
}
\value{
A posterior draws object of the requested format.
}
\description{
The \code{as_draws} family converts an object holding posterior draws into
one of the posterior package's draws formats. frmtmb defines the
generics so that they work whether or not posterior is attached, and
registers methods with posterior so that its own spellings dispatch
too.
}
\details{
Core has no object that carries draws: \code{frm()} is maximum likelihood
and \code{frm_multiple()} pools point estimates, so its methods explain
that rather than inventing a draws matrix. Install \code{frmtmb.sample}
and sample with \code{frmtmb.sample::frm_sample()} to get an object these
convert.
}
\examples{
# a maximum-likelihood fit carries no draws, and says so by name
dd <- data.frame(y = rnorm(40), x = rnorm(40))
try(as_draws_df(frm(bf(y ~ x) + gaussian(), data = dd)))

# frm_multiple() pools estimates rather than carrying draws, so it
# answers with the reason rather than a matrix
fits <- frm_multiple(bf(y ~ x) + gaussian(), data = list(dd, dd))
try(as_draws(fits))
}

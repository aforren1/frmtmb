% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-draws.R
\name{frmtmb-draws-refusals}
\alias{frmtmb-draws-refusals}
\alias{stancode}
\alias{stancode.frmtmb_draws}
\alias{standata}
\alias{standata.frmtmb_draws}
\alias{expose_functions}
\alias{expose_functions.frmtmb_draws}
\alias{restructure}
\alias{restructure.frmtmb_draws}
\alias{posterior_samples}
\alias{posterior_samples.frmtmb_draws}
\alias{nsamples}
\alias{nsamples.frmtmb_draws}
\alias{parnames}
\alias{parnames.frmtmb_draws}
\title{Methods a ported brms script may call that frmtmb does not have}
\usage{
stancode(object, ...)

\method{stancode}{frmtmb_draws}(object, ...)

standata(object, ...)

\method{standata}{frmtmb_draws}(object, ...)

expose_functions(x, ...)

\method{expose_functions}{frmtmb_draws}(x, ...)

restructure(x, ...)

\method{restructure}{frmtmb_draws}(x, ...)

posterior_samples(x, ...)

\method{posterior_samples}{frmtmb_draws}(x, ...)

nsamples(object, ...)

\method{nsamples}{frmtmb_draws}(object, ...)

parnames(x, ...)

\method{parnames}{frmtmb_draws}(x, ...)
}
\arguments{
\item{object, x, ...}{Ignored; these functions always stop.}
}
\value{
These functions never return; they signal an error.
}
\description{
These \code{brmsfit} methods either describe machinery frmtmb does not use
(Stan code and Stan data) or are brms spellings that have been
renamed. They are defined so that a ported script gets the reason and
the replacement rather than "could not find function", which is what
the vignette-port audit measured most of its post-processing failures
as.
}
\examples{
\donttest{
if (requireNamespace("tmbstan", quietly = TRUE) &&
    requireNamespace("rstan", quietly = TRUE)) {
  set.seed(1)
  dd <- data.frame(x = rnorm(40))
  dd$y <- rnorm(40, 1 + 0.5 * dd$x, 1)
  fit <- frm(bf(y ~ x), family = gaussian(), data = dd)
  ds <- frm_sample(fit, chains = 1, iter = 400, refresh = 0)
  # each refusal names its reason and the replacement
  try(stancode(ds))
  try(nsamples(ds))
}
}
}

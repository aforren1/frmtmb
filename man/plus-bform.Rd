% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bf.R
\name{plus-bform}
\alias{plus-bform}
\alias{+.frmtmb_bform}
\title{Add to a model formula}
\usage{
\method{+}{frmtmb_bform}(e1, e2)
}
\arguments{
\item{e1}{A \code{bf()} or \code{mvbf()} object.}

\item{e2}{The object to add.}
}
\value{
A \code{frmtmb_formula} or a \code{frmtmb_mvformula}.
}
\description{
One \code{+} method serves \code{\link[=bf]{bf()}} and \code{\link[=mvbf]{mvbf()}} objects alike, so a sum of
any number of formulas reads left to right. The sum of \code{bf(y1 ~ x)},
\code{bf(y2 ~ x)} and \code{bf(y3 ~ x)} is
\code{mvbf(bf(y1 ~ x), bf(y2 ~ x), bf(y3 ~ x))}.
}
\details{
The right-hand side can be another formula, a family, \code{\link[=lf]{lf()}},
\code{\link[=nlf]{nlf()}} or \code{\link[=set_rescor]{set_rescor()}}. A family added to a single formula sets
its family. A family added to a multivariate formula goes to every
response that has no family yet, so in the sum of \code{bf(o ~ x)},
\code{cumulative()}, \code{bf(y ~ x)} and \code{gaussian()} the response \code{o} stays
ordinal. brms instead gives the last family to every response. An
\code{\link[=lf]{lf()}} or \code{\link[=nlf]{nlf()}} added to a multivariate formula names its response
with \verb{resp =}.
}
\examples{
# three responses, one family for all of them
bf(y1 ~ x) + bf(y2 ~ x) + bf(y3 ~ x) + gaussian()

# a family per response, and a dpar formula for the third response
bf(o ~ x) + cumulative() + bf(y1 ~ x) + gaussian() + bf(y2 ~ x) +
  lf(sigma ~ x, resp = "y2")
}

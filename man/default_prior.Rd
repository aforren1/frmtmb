% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{default_prior}
\alias{default_prior}
\alias{get_prior}
\title{Default priors: the slots a prior can target}
\usage{
default_prior(
  object,
  data = NULL,
  family = NULL,
  data2 = list(),
  route = c("fit", "sample")
)

get_prior(formula, ...)
}
\arguments{
\item{object}{A \code{bf()} formula (with family), a plain formula, or
an already fitted \code{frmtmb_fit}.}

\item{data}{A data frame of model data (ignored when \code{object} is a
fit).}

\item{family}{Family, when \code{object} does not carry one.}

\item{data2}{Structural objects, as in \code{\link[=frm]{frm()}} (ignored when
\code{object} is a fit, which carries its own).}

\item{route}{Which route's defaults the \code{prior} column reports.
\code{"fit"} (the default) reports the defaults \code{\link[=frm]{frm()}} applies, which
are flat in every slot; it consults no registry, so its answer
does not depend on which packages are attached. \code{"sample"} reports
the defaults \code{frmtmb.sample::frm_sample()} applies, and refuses
when no package has registered any. The returned object records
the route and \code{print()} names it on its first line.}

\item{formula}{The same as \code{object}, under the name brms's
\code{get_prior()} gives it.}

\item{...}{For \code{get_prior()}, the arguments of \code{default_prior()}.}
}
\value{
A data frame of class \code{frmtmb_prior_rows} with brms's columns
\code{prior}, \code{class}, \code{coef}, \code{group}, \code{resp}, \code{dpar}, \code{nlpar}, \code{lb},
\code{ub} and \code{source}, and a \code{route} attribute. \code{source} is
\code{"default"} on every row; \code{\link[=validate_prior]{validate_prior()}} marks the rows a user
set.
}
\description{
brms's \code{default_prior()}, and \code{get_prior()}, which brms has kept as
its alias since 2.20.14: one row per slot a prior can target, with
the class/coef/dpar/group values to pass to \code{\link[=set_prior]{set_prior()}}. Classes
\code{"sd"} and \code{"cor"} are targeted by \code{group} and \code{nlpar}; the
residual-correlation classes (\code{"ar"}, \code{"ma"}, \code{"cosy"}, \code{"cortime"},
\code{"rescor"}) by \code{resp}; and class \code{"theta"} rows name the raw internal
covariance parameters (escape hatch, including correlations one at a
time, across all three covariance components).
}
\details{
A nonlinear parameter's coefficients are listed under class \code{"b"}
with its name in the \code{nlpar} column, the intercept among them, which
is how brms lists them and what \code{\link[=set_prior]{set_prior()}} addresses (see its
Nonlinear parameters section).

A distributional parameter is listed under whichever of its two
spellings the model offers: its OWN class where the model gives it
no predictor, and class \code{"Intercept"} with its name in the \code{dpar}
column where the model gives it a formula. brms lists the same two,
the same way round. See A distributional parameter's own class in
\code{\link[=set_prior]{set_prior()}}.

An ordinal family has no intercept column, so its class \code{"Intercept"}
row names the THRESHOLD vector, which is what the same row means in
brms. See the Ordinal thresholds section of \code{\link[=set_prior]{set_prior()}}.
}
\section{Where the table differs from brms's}{

The class \code{"theta"} rows are frmtmb's own. They name the internal
covariance parameters, which are real parameters of the fit, and for
a \code{gp()} length-scale, a \code{car()} dependence parameter or one entry of
a structured covariance they are the only spelling that reaches one
parameter. brms has no such class, so a brms table for the same model
has fewer rows.

brms lists a class \code{"sd"} row per coefficient of each block
(\code{sd_patient__Intercept}). frmtmb's class \code{"sd"} addresses a whole
block and refuses a \code{coef}, so those rows are not listed; class
\code{"theta"} reaches one standard deviation on its own.

A flat slot reads \code{"(flat)"} in the \code{prior} column, where brms stores
an empty string and prints \code{(flat)}.
}

\section{Which route the defaults describe}{

Every column but \code{prior} is a property of the design, and the design
does not change with what is attached. The \code{prior} column is a
property of a ROUTE, and frmtmb has two of them with different
defaults: \code{\link[=frm]{frm()}} is maximum likelihood and is flat in every slot
until a prior is set, while \code{frmtmb.sample::frm_sample()} applies
brms's weakly-informative defaults on both of its routes. \code{route}
makes the caller say which one is being asked about, so that the
answer is a property of the question rather than of the search path.

\code{route = "fit"}, the default, reads no registry at all. Its table is
identical whatever extension packages are loaded.

\code{route = "sample"} reads the defaults \code{frm_sample()} would apply,
which only frmtmb.sample can state. Without that package loaded the
call is refused rather than answered \code{(flat)}, because a flat table
would be a wrong answer about the sampling route and not a missing
one.

brms's \code{default_prior()} describes what \code{brm()} would use, so the
brms reading of this function is \code{route = "sample"}. \code{route = "fit"}
has no brms counterpart: it describes \code{frm()}.

With frmtmb.sample attached, the example below with
\code{route = "sample"} added returns the same rows as the example below with brms's densities in
the \code{prior} column instead of \code{(flat)}: a Student-t on the intercept
centered on the response, a half-Student-t on \code{sigma} and on each
standard deviation, and \code{lkj(1)} on each correlation. Population-level
slopes stay \code{(flat)}, as they are in brms.
}

\examples{
dd <- data.frame(y = rnorm(60), x = rnorm(60),
                 g = factor(rep(1:6, 10)))
# what frm() applies: flat, whatever else is loaded. For what
# frm_sample() applies, see "Which route the defaults describe"
default_prior(bf(y ~ x + (1 | g)) + gaussian(), data = dd)
# the same table under brms's older name
get_prior(bf(y ~ x + (1 | g)) + gaussian(), data = dd)
}
\seealso{
\code{\link[=validate_prior]{validate_prior()}} for the table with a prior filled in.
}

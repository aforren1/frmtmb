% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmm.R
\name{hmm}
\alias{hmm}
\title{Hidden Markov models}
\usage{
hmm(
  K,
  family = stats::gaussian(),
  time = NULL,
  group = NULL,
  init = c("stationary", "estimated", "uniform"),
  trans = ~1
)
}
\arguments{
\item{K}{Number of hidden states (at least 2, at most 9 - beyond that
the \verb{tr\{i\}\{j\}} dpar names stop being unambiguous).}

\item{family}{The state-dependent (emission) family. Any univariate
family with a \code{mu} parameter works, plus \code{\link[=multinomial]{multinomial()}} for
categorical emissions. Ordinal families, mixtures and nested \code{hmm()}
components are refused.}

\item{time}{Variable giving the order of observations within a
sequence, as a bare name or a one-sided formula. Omit it to use each
row's position in the data.}

\item{group}{Variable identifying the sequences, as a bare name or a
one-sided formula. Omit it to treat the whole data set as one
sequence.}

\item{init}{Initial-state distribution: \code{"stationary"} (the default),
\code{"estimated"}, or \code{"uniform"}.}

\item{trans}{Default one-sided formula for every transition cell.}
}
\value{
A \code{frmtmb_family}.
}
\description{
\code{hmm(K, family)} fits a \code{K}-state hidden Markov model: the response at
each time point is drawn from one of \code{K} state-dependent copies of
\code{family}, and the unobserved state follows a first-order Markov chain
along \code{time} within \code{group}. The state sequence is summed out exactly
by the forward algorithm, which is evaluated on the same AD tape as
everything else, so random effects, smooths and distributional
predictors compose with it unchanged.
}
\section{Parameters}{

Each of the wrapped family's distributional parameters is copied once
per state and suffixed with the state index, exactly as \code{\link[=mixture]{mixture()}}
does: \code{hmm(2, gaussian())} has \code{mu1}, \code{mu2}, \code{sigma1}, \code{sigma2}. The
main model formula applies to every state's location parameter;
override one state with \code{bf(y ~ x, mu2 ~ x + (1 | id))}. Every dpar
takes the full formula grammar, random effects included.

Transition probabilities are a row-wise multinomial logit with
\strong{state 1 as the reference cell in every row}, named \verb{tr\{i\}\{j\}} for
the move from state \code{i} to state \code{j} (\code{j >= 2}): a two-state chain has
\code{tr12} and \code{tr22}. This is \code{depmixS4}'s parameterization, and the
coefficients agree with it to four decimals (\code{dev/hmm-feasibility.md},
probe B1). \code{hmmTMB} and \code{moveHMM} instead reference the diagonal;
the two span the same model and only the reported coefficients differ.
\code{trans} gives every transition cell the same default formula; a single
cell is overridden the ordinary way, \code{bf(y ~ 1, tr12 ~ x)}. The
covariate value at time \code{t} drives the transition from \code{t} to
\code{t + 1}, so a sequence's last row never contributes a transition -
again \code{depmixS4}'s convention.
}

\section{Initial distribution}{

\describe{
\item{\code{"stationary"}}{The stationary distribution of the transition
matrix, solved on the tape. Costs no parameters and is the right
default for long sequences and for many short ones. It needs a
CONSTANT transition matrix, so it is refused when any transition
dpar carries a predictor.}
\item{\code{"estimated"}}{\code{K - 1} free logits (state 1 the reference),
reported as \verb{hmm_ldel_1 ...} and counted in \code{df}.}
\item{\code{"uniform"}}{Fixed at \code{1 / K}.}
}
}

\section{Decoding and the fitted values}{

\code{E[y_t]} under an HMM is \verb{sum_k P(S_t = k | y) mu_k(x_t)}, which needs
the smoothed state probability and therefore a backward pass over the
whole sequence. \code{\link[=hmm_probs]{hmm_probs()}} returns those probabilities and
\code{\link[=hmm_viterbi]{hmm_viterbi()}} the maximum-a-posteriori state path; \code{\link[=fitted]{fitted()}},
\code{predict(type = "response")} and \code{\link[=residuals]{residuals()}} all route through
\code{\link[=hmm_probs]{hmm_probs()}}, so they report the occupancy-weighted mean rather than
any single state's.
}

\section{Label switching and local optima}{

The likelihood is invariant to permuting the states, so the state
means start at spread response quantiles (see \code{\link[=mixture]{mixture()}}); a start
with every state mean equal sits on the symmetry axis and the
optimizer never leaves it. Relabeling between runs is expected and is
not fought. Multimodality is real and is not signalled by any
convergence diagnostic: on a random-effect model the default cold
start has been measured converging 8.1 log-likelihood units below the
global optimum with \code{convergence == 0} and a positive-definite
Hessian. Compare several starts (\code{\link[=frm_allfit]{frm_allfit()}}) before reporting.
}

\section{Missing responses}{

An \code{NA} response is a time point the chain passes THROUGH without
emitting: the row is kept, its emission factor is masked out, and the
transition into and out of it is unchanged. This departs from the
package-wide \code{na.action}, deliberately - dropping the row would
shorten the chain and make one transition stand in for several, which
biases the transition matrix (measurably: with 3 of 20 points missing
the fitted off-diagonal moved from 0.115 to 0.128 against a true
0.10). \code{nobs()} therefore counts every row. \code{fitted()} and
\code{residuals()} are \code{NA} at a masked row, while \code{\link[=hmm_probs]{hmm_probs()}} is not:
the neighbouring observations still say where the chain was.
}

\section{Cost}{

Evaluating the likelihood is linear in the number of ROWS and free in
the number of sequences: a thousand sequences of five cost what one of
five thousand does. Both the tape and the decoding passes are
organized by time step, so what they actually cost is
\verb{length of the LONGEST sequence} times \code{K^2}. At 20 000 rows a fit
takes about 3 s cut into a thousand short sequences and about the same
as one long chain, but \code{\link[=hmm_probs]{hmm_probs()}} takes 0.06 s in the first shape
and 1.3 s in the second. Below 5 000 rows nothing is noticeable.
On a long chain the objective's own magnitude also makes the
optimizer's relative convergence test bite before its gradient test
does; judge \verb{max|grad|} per observation rather than in absolute terms.
}

\section{Boundaries}{

An unpenalized multinomial logit will send an emission or transition
probability to 0 whenever a category is rare inside a state, and the
optimizer then reports singular convergence at a perfectly good
optimum. \code{\link[=set_prior]{set_prior()}} on the affected logit is the remedy.
}

\section{Random effects and the Laplace approximation}{

A random effect in a state's linear predictor is integrated by the
Laplace approximation OUTSIDE the exact state sum. That integrand is a
mixture over state sequences and is not Gaussian, so the approximation
is genuinely approximate here even for a gaussian response. Measured
against adaptive Gauss-Hermite quadrature on a 40-sequence,
25-observation model with one scalar random intercept, the bias was
\strong{-0.126 in the log-likelihood} (8.9e-5 relative) and 4.4e-4 absolute
in the parameters (\code{dev/hmm-feasibility.md}, probe D1). \code{quadrature = TRUE} is refused: its rule integrates a random effect against a
PRODUCT of per-row densities, and the forward algorithm is not one.
}

\section{What is refused}{

\code{REML} (the restricted likelihood would integrate out only one state's
location coefficients, which matches no standard definition),
\code{quadrature}, \code{frmtmb_control(profile = TRUE)}, \code{weights()}, \code{cens()},
\code{trunc()}, \code{se()} and \code{mi()} on the response, multivariate models and
\code{rescor}, \code{residuals(type = "osa")}, \code{predict(se.fit = TRUE)} on the
response scale, and \code{conditional_effects()}. A grouping in which every
sequence has length 1 is refused too: the chain is then unidentified
and the model is a \code{\link[=mixture]{mixture()}}.
}

\examples{
set.seed(11)
n_seq <- 20; len <- 25
G <- matrix(c(0.9, 0.1, 0.2, 0.8), 2, 2, byrow = TRUE)
mu <- c(0, 3); sg <- c(0.6, 0.6)
dd <- do.call(rbind, lapply(seq_len(n_seq), function(id) {
  s <- integer(len); s[1] <- 1L
  for (t in 2:len) s[t] <- sample.int(2, 1, prob = G[s[t - 1], ])
  data.frame(id = id, t = seq_len(len),
             y = rnorm(len, mu[s], sg[s]), state = s)
}))
fit <- frm(bf(y ~ 1),
           family = hmm(K = 2, gaussian(), time = t, group = id),
           data = dd)
fixef(fit)

# smoothed state probabilities and the MAP path
head(hmm_probs(fit))
mean(hmm_viterbi(fit) == dd$state)

# fitted() is the occupancy-weighted mean, not state 1's
cor(fitted(fit), dd$y)

\donttest{
# one state's mean takes its own predictor, random effects included
frm(bf(y ~ 1, mu2 ~ 1 + (1 | id)),
    family = hmm(K = 2, gaussian(), time = t, group = id),
    data = dd)

# covariate-dependent transitions: trans = sets every cell's default
dd$x <- rnorm(nrow(dd))
frm(bf(y ~ 1),
    family = hmm(K = 2, gaussian(), time = t, group = id,
                 init = "estimated", trans = ~x),
    data = dd)
}
}
\seealso{
\code{\link[=hmm_probs]{hmm_probs()}}, \code{\link[=hmm_viterbi]{hmm_viterbi()}}, \code{\link[=mixture]{mixture()}}
}

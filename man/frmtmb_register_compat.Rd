% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat.R
\name{frmtmb_register_compat}
\alias{frmtmb_register_compat}
\alias{compat_rule_builder}
\title{Contribute to the compatibility matrix from another package}
\usage{
frmtmb_register_compat(features = NULL, rules = NULL)

compat_rule_builder()
}
\arguments{
\item{features}{Named character vector mapping a feature's DISPLAY
name to its kind: \code{c("hmm" = "structure")}, \code{c("dec()" = "aterm")}.
The display name is how the feature is written in a formula or a
call, parentheses included for a callable one; the kind groups it
in the printed matrix (\code{"family"}, \code{"covstruct"}, \code{"aterm"},
\code{"special"}, \code{"autocor"}, \code{"mode"}, \code{"structure"}).}

\item{rules}{A FUNCTION of no arguments returning a rule data frame,
not the data frame itself, so that contributed rules are built on
demand exactly as the core ones are. Build the frame with
\code{compat_rule_builder()}: it returns a list of two functions, \code{r(a, b, status, note, override = FALSE)} to record one rule and
\code{rules()} to return the accumulated frame. \code{a} and \code{b} are display
names, \code{"*"} matches every feature, and \code{override = TRUE} says the
rule beats a more specific one rather than losing to it.}
}
\value{
\code{NULL}, invisibly. Called for the registration.
\code{compat_rule_builder()} returns a list with elements \code{r} and
\code{rules}.
}
\description{
\code{\link[=frm_compat]{frm_compat()}} answers what one feature does in the presence of
another, and it answers in three states: the pair works, the pair is
refused, or the pair is untested. A feature that lives outside
frmtmb has to be able to say the same things about itself, or the
matrix reports on part of the package and reads as if it reported on
all of it.
}
\details{
\code{frmtmb_register_compat()} is that seam. Call it from the
contributing package's \code{.onLoad()}: by then every namespace is
sealed, so the collation-order question a top-level call would raise
does not arise. \code{compat_rule_builder()} is the accumulator the core's
own rules are written with, exported so that a contributed rule reads
the same as a core one.

Contributions are APPENDED, which is the only safe direction. Rules
of equal specificity resolve later-wins, so a contributed rule may
override a core default and a core default can never silently
override a contributed one.
}
\section{Status vocabulary}{

Every rule declares one of three states, and the third is the reason
the registry exists: a guard that does not exist looks exactly like a
guard that passed, so "no error" was never evidence of support.
\describe{
\item{\code{"works"}}{The pair is supported and exercised by a test.}
\item{\code{"refused"}}{The pair is rejected with a message. Say in the
note what the user should do instead.}
\item{\code{"untested"}}{Nothing is known. Neither a promise nor a
refusal.}
}
}

\examples{
# what a contributing package's .onLoad() does
contribute <- function() {
  b <- compat_rule_builder()
  b$r("wiener", "cens()", "refused",
      "The family supplies no lcdf, so there is no CDF to censor with.")
  b$r("wiener", "*", "untested",
      "Not exercised outside this package's own suite.")
  frmtmb_register_compat(features = c("wiener" = "family"),
                         rules = b$rules)
}
# the accumulator on its own, which is all a rule set is
b <- compat_rule_builder()
b$r("dec()", "trials()", "refused", "Different response shapes.")
b$rules()
}
\seealso{
\code{\link[=frm_compat]{frm_compat()}} for the matrix these fill,
\code{\link[=frmtmb_family]{frmtmb_family()}} and \code{\link[=frmtmb_structure]{frmtmb_structure()}} for the family-side
seams a contributor usually registers alongside these, and
\code{vignette("compatibility")}
}

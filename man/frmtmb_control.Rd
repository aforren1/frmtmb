% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{frmtmb_control}
\alias{frmtmb_control}
\title{Control parameters for frmtmb fits}
\usage{
frmtmb_control(
  optimizer = "nlminb",
  optCtrl = list(iter.max = 1000, eval.max = 1000),
  restarts = 1,
  grad_tol = 0.001,
  profile = FALSE,
  sparse_x = FALSE,
  autoscale = FALSE,
  check_nlev_1 = c("warning", "ignore", "stop"),
  check_olre = c("warning", "ignore", "stop"),
  importance_seed = 1L,
  importance_rounds = 5L,
  importance_ess = 0.25,
  verbose = NULL
)
}
\arguments{
\item{optimizer}{\code{"nlminb"} (default), \code{"optim"} (L-BFGS-B), or a
function with signature \verb{(par, fn, gr, lower, upper, control)}
returning a list with elements \code{par}, \code{objective}, \code{convergence}
(0 = success), and optionally \code{message} - the hook for optimx,
nloptr, and friends without frmtmb depending on them. Example:

\if{html}{\out{<div class="sourceCode">}}\preformatted{nlopt <- function(par, fn, gr, lower, upper, control) \{
  r <- nloptr::nloptr(par, fn, gr, lb = lower, ub = upper,
                      opts = list(algorithm = "NLOPT_LD_LBFGS",
                                  xtol_rel = 1e-10, maxeval = 2000))
  list(par = r$solution, objective = r$objective,
       convergence = as.integer(r$status < 0), message = r$message)
\}
frm(..., control = frmtmb_control(optimizer = nlopt))
}\if{html}{\out{</div>}}}

\item{optCtrl}{Control list passed to the built-in optimizers
(\code{\link[stats:nlminb]{stats::nlminb()}} / \code{\link[stats:optim]{stats::optim()}}).}

\item{restarts}{Number of times to restart the optimizer from the
current optimum while the gradient remains above \code{grad_tol}.}

\item{grad_tol}{Warn (and restart) if the maximum absolute gradient at
the optimum exceeds this value.}

\item{profile}{Experimental: move the primary (\code{beta}) coefficients
into the inner (Laplace) problem, TMB's \code{profile} argument - the
analog of \code{glmmTMBControl(profile = TRUE)} and \code{glmer(nAGQ = 0)}.
Speeds up models with many fixed effects, and like those it is an
approximation: estimates differ slightly from the exact fit.
Coefficient covariance comes from the joint precision. Not
compatible with \code{REML = TRUE} or \code{quadrature = TRUE};
profile/uniroot \code{confint()} and \code{hypothesis(method = "profile")}
need a non-profiled fit.}

\item{sparse_x}{Build the parametric fixed-effect design matrices as
sparse \code{\link[Matrix:sparse.model.matrix]{Matrix::sparse.model.matrix()}} objects, the analog of
\code{glmmTMB(sparseX =)}. Worth it when a many-level fixed factor makes
the dense design dominate memory; estimates are identical either
way. \code{model.matrix()} on the fit then returns a sparse matrix.}

\item{autoscale}{Standardize badly scaled continuous predictors
internally (the lme4 >= 1.1.37 feature): fit a copy of the model
with each qualifying fixed-effect column centered and scaled
(scaled only, in a linear predictor without an intercept), map
that optimum back to the original parameterization exactly, and
warm-start the ordinary fit there. Reported results are always on
the original scale, so every downstream method works unchanged;
the cost is a second (cheap) optimization. Columns qualify when
they are parametric and take more than two distinct values;
intercepts, factor contrasts, smooth bases, and mo()/mi() columns
are never touched, and the whole step is a silent no-op when
nothing qualifies. Compatible with \code{profile = TRUE}. Under
\code{prior} or bounds, the first stage applies them to the scaled
coefficients; the second stage is the fit that is reported.}

\item{check_nlev_1}{What to do about a scalar random-effect term
whose grouping factor has a single level: \code{"warning"} (default),
\code{"ignore"}, or \code{"stop"}, following lme4's \code{lmerControl()} check
vocabulary. Such a term has no variance to estimate - the single
level is absorbed by the intercept - and its standard deviation
collapses to zero. Structured blocks over several terms per level
(\code{ar1()}, \code{us()}, the spatial covariance structures) are never
flagged: one grouping level there is a single realization of a
field, which is the normal way to write them.}

\item{check_olre}{What to do about an observation-level random
effect - one level per row - on a gaussian, student or lognormal
response: \code{"warning"} (default), \code{"ignore"}, or \code{"stop"}. Its
variance is confounded with the residual standard deviation, so
only their sum is identified and the split between them is
arbitrary. The check is skipped when \code{sigma} is not free to absorb
it - a \code{se()} response or a constant \code{sigma} - which is the
random-effects meta-analysis, and for discrete families, where an
observation-level term is the usual overdispersion model. It is
also skipped for the known-structure blocks \code{gr(cov = )},
\code{gr(prec = )} and \code{equalto()}: their level covariance is not
proportional to the identity, so the block and the residual are
separately identified. That is the animal model with one
measurement per individual.}

\item{importance_seed}{Seed for the standard normal draws of
\code{frm(importance =)}. The draws are taken from a private random
stream, so the fit neither reads nor disturbs the session's random
state: the same seed gives the same answer to the last bit,
whatever else has drawn random numbers.}

\item{importance_rounds}{Cap on the number of times
\code{frm(importance =)} refreezes its proposal. Each round rebuilds
the proposal at the current estimate and optimizes again, and the
loop stops as soon as either the estimates move less than \code{1e-3}
or the freshly anchored objective is stationary at them to
\code{grad_tol}. Measured on this package's own designs that takes two
to four rounds, with the move falling about tenfold each time, so
the default leaves one in hand; an unused round costs nothing, and
each round actually taken costs one tape.}

\item{importance_ess}{Effective sample size, as a fraction of the
draw count, below which \code{frm(importance =)} warns and names the
groups. The default \code{0.25} is placed by measurement: the hardest
design in the test suite holds \code{0.43} at its own optimum, and a
proposal displaced far enough to matter falls below \code{0.01}.}

\item{verbose}{Report fit progress through \code{\link[=message]{message()}}, one terse
line per stage with its elapsed seconds, so a slow fit shows where
the time went. \code{FALSE} (default) is silent and costs nothing.
\code{TRUE} (or \code{1}) reports parsing, frame assembly, the autoscale
pre-fit, tape construction, each optimizer run and restart,
\code{sdreport()} when \code{se = TRUE}, and a closing line with the
objective, the maximum absolute gradient, and the number of
convergence warnings. The fit itself opens with a line naming the
family, the mode (ML or REML, plus profile, quadrature, autoscale,
prior), and the optimizer. \code{2} adds the optimizer's own trace, by
setting \code{optCtrl$trace} unless you set it yourself. That trace is
printed by \code{\link[stats:nlminb]{stats::nlminb()}} / \code{\link[stats:optim]{stats::optim()}} to standard
output, not through \code{message()}, and a custom optimizer function
receives \code{optCtrl} unchanged, so \code{verbose} does not reach it.

Use \code{suppressMessages()} to silence a verbose fit. The refit loops
in \code{\link[=frm_bootstrap]{frm_bootstrap()}}, \code{\link[=influence]{influence()}}, and \code{\link[=frm_allfit]{frm_allfit()}} force
\code{verbose} off, so a verbose fit does not make them print hundreds
of lines; \code{\link[=refit]{refit()}} and \code{\link[=frm_multiple]{frm_multiple()}} report normally.}
}
\value{
A list of control settings.
}
\description{
Control parameters for frmtmb fits
}
\examples{
set.seed(1)
n <- 200
dd <- data.frame(x = rnorm(n), g = factor(rep(1:10, 20)))
dd$y <- rnorm(n, 1 + 0.5 * dd$x + rnorm(10, 0, 0.8)[dd$g], 1)

# another optimizer, with its own control list
fit <- frm(bf(y ~ x + (1 | g)) + gaussian(), data = dd,
           control = frmtmb_control(optimizer = "optim",
                                    optCtrl = list(maxit = 500)))
fit$opt$convergence

# a tighter gradient criterion, with restarts from the current
# optimum until it is met
frm(bf(y ~ x + (1 | g)) + gaussian(), data = dd,
    control = frmtmb_control(grad_tol = 1e-4, restarts = 3))

# badly scaled predictors: fit an internally standardized copy first,
# then warm-start the reported fit from it
dd$xbig <- dd$x * 1e5
frm(bf(y ~ xbig + (1 | g)) + gaussian(), data = dd,
    control = frmtmb_control(autoscale = TRUE))

# the object is a plain list, so it can be built once and reused
ctrl <- frmtmb_control(check_nlev_1 = "ignore")
ctrl$optimizer
}

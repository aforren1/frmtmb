% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional-effects.R
\name{pp_check}
\alias{pp_check}
\alias{pp_check.frmtmb_fit}
\title{Predictive check against simulated responses}
\usage{
pp_check(object, ...)

\method{pp_check}{frmtmb_fit}(
  object,
  type = "dens_overlay",
  ndraws = 10,
  prefix = c("ppc", "ppd"),
  group = NULL,
  x = NULL,
  newdata = NULL,
  resp = NULL,
  ...,
  re_formula = NA
)
}
\arguments{
\item{object}{A \code{frmtmb_fit} for a univariate model.}

\item{...}{Passed to the \verb{ppc_*} function.}

\item{type}{The bayesplot check, i.e. the part after \code{ppc_}
(\code{"dens_overlay"}, \code{"hist"}, \code{"stat"}, \code{"stat_grouped"},
\code{"scatter_avg"}, ...). With \code{prefix = "ppc"} a name that
\code{bayesplot::available_ppc()} does not list is refused, as brms
refuses it.}

\item{ndraws}{Number of simulated response vectors.}

\item{prefix}{\code{"ppc"} (the default) plots the observed response
against the simulated ones; \code{"ppd"} plots the simulated ones alone,
through bayesplot's \verb{ppd_*} function of the same name.}

\item{group}{The name of a model variable to stratify by, for the
\verb{*_grouped} types, which need it. The name is looked up in the
model frame, as brms looks it up in the model's data, so a column
the model does not use is refused.}

\item{x}{The name of a model variable for the types that take an \code{x}
(\code{"intervals"}, \code{"ribbon"}, \code{"error_scatter_avg_vs_x"}, ...),
looked up as \code{group} is.}

\item{newdata, resp}{brms's arguments. With \code{newdata} the check is on
its rows: \code{\link[=simulate]{simulate()}} draws for them, the observed series is its
response column (needed for \code{prefix = "ppc"} only), and \code{group} and
\code{x} are read from it, as brms reads them. A row with a missing
response is dropped with brms's warning. \code{resp} is accepted and
ignored, which is what brms does with it on a model that has one
response; a multivariate fit is refused before \code{resp} could select
one.}

\item{re_formula}{The random-effect switch, in brms's spelling
(\code{pp_check()} is a brms function). On a fit it is passed to
\code{\link[=simulate]{simulate()}} and defaults to \code{NA}, which redraws every group-level
effect in each replicate; \code{~0} and \code{~1} mean the same, and a
one-sided formula keeps the terms it names and redraws the rest
(see \code{\link[=simulate.frmtmb_fit]{simulate.frmtmb_fit()}}). On draws it is passed to
\code{posterior_predict()} and
defaults to \code{NULL}, because a draw already carries its own. lme4's
\code{re.form} is refused. brms honors it on \code{pp_check()} and warns that
it ignored it, which is a leak through its dots rather than a
decision to copy.}
}
\value{
A ggplot object, as returned by the bayesplot \verb{ppc_*}
function that \code{type} selects.
}
\description{
The frequentist analog of brms's \code{pp_check()}: responses are
simulated from the fitted model (marginally over the random effects)
and handed to the corresponding bayesplot \verb{ppc_*} function
(bayesplot must be installed, but not necessarily attached).
}
\section{Types a fit cannot draw}{

The \verb{loo_*} types weight posterior draws by Pareto-smoothed importance
sampling, and a maximum-likelihood fit has no posterior draws, so
they are refused on a fit. \code{pp_check()} on the draws of
\code{frmtmb.sample::frm_sample()} is the route to them.
\code{"error_binned"} is refused on an ordinal, categorical or multinomial
fit, as brms refuses it.
}

\examples{
if (requireNamespace("bayesplot", quietly = TRUE)) {
  set.seed(1)
  dd <- data.frame(x = rnorm(100), g = factor(rep(1:10, 10)))
  dd$y <- rpois(100, exp(0.3 + 0.4 * dd$x + rnorm(10, 0, 0.6)[dd$g]))
  fit <- frm(bf(y ~ x + (1 | g)) + poisson(), data = dd)

  # the observed density against draws from the fit
  pp_check(fit, ndraws = 20)

  # any bayesplot ppc_* check, named by its suffix. A statistic the
  # model was not fitted to is the informative one: here, the share
  # of zeros, which is how zero inflation shows up.
  pp_check(fit, type = "stat", stat = function(y) mean(y == 0),
           ndraws = 50)
}
}

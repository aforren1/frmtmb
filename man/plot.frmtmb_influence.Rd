% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/influence.R
\name{plot.frmtmb_influence}
\alias{plot.frmtmb_influence}
\title{Plot case-deletion influence}
\usage{
\method{plot}{frmtmb_influence}(x, which = NULL, ask = NULL, labels = 3L, ...)
}
\arguments{
\item{x}{A \code{frmtmb_influence} object from \code{\link[=influence]{influence()}}.}

\item{which}{Panels to draw: \code{1} is the Cook's distance plot, \code{1 + j}
the \code{dfbetas} panel of the \code{j}th coefficient. Defaults to all of
them.}

\item{ask}{Pause between panels; defaults to \code{TRUE} when more than
one panel goes to an interactive device, the \code{\link[=plot.frmtmb_fit]{plot.frmtmb_fit()}}
rule.}

\item{labels}{Number of extreme cases to label in each panel.}

\item{...}{Passed to the underlying \code{\link[graphics:plot]{graphics::plot()}} calls.}
}
\value{
\code{x}, invisibly. Called for the plots it draws.
}
\description{
Draws the \code{\link[=influence]{influence()}} result as base-graphics index plots: Cook's
distances first, with the most influential cases labeled by name,
then one \code{dfbetas} panel per fixed-effect coefficient. Each \code{dfbetas}
panel carries the conventional \verb{+/- 2 / sqrt(n)} reference band, \code{n}
being the number of deleted units (Belsley, Kuh and Welsch 1980).
}
\details{
This is deliberately NOT a \code{car::influencePlot()} counterpart:
hatvalues and studentized residuals are OLS-geometry approximations
to case deletion that are ill-defined for a fit whose random effects
are marginalized by the Laplace approximation, and the refit-based
quantities plotted here are the exact version of what those
approximate.
}
\examples{
\donttest{
set.seed(7)
dd <- data.frame(x = rnorm(60), g = factor(rep(1:6, 10)))
dd$y <- rnorm(60, 1 + 0.5 * dd$x + rnorm(6, 0, 0.5)[dd$g], 1)
fit <- frm(bf(y ~ x + (1 | g)), family = gaussian(), data = dd)
infl <- influence(fit, groups = "g")

# Cook's distances only
plot(infl, which = 1)
}
}
\references{
Belsley, D. A., Kuh, E. and Welsch, R. E. (1980)
\emph{Regression Diagnostics}. Wiley.
}

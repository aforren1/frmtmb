% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{frmtmb_family-access}
\alias{frmtmb_family-access}
\alias{$.frmtmb_family}
\title{Family object field access}
\usage{
\method{$}{frmtmb_family}(x, name)
}
\arguments{
\item{x}{A \code{frmtmb_family}.}

\item{name}{The field name.}
}
\value{
The field.
}
\description{
A family object is a list, and \code{$} on a list PARTIAL-matches: before
frmtmb answered brms's field names, \code{student()$link} returned the
\code{links} list rather than the link name brms code expects.
}
\details{
\code{$} on a \code{frmtmb_family} answers brms's link fields, \code{link}, \code{linkfun},
\code{linkinv} and \verb{link_<dpar>} for every parameter but the mean, by
reading \code{links} at the moment of the call, so they always describe the
links the fit uses, however the object was edited and whenever it was
saved. Every other name matches exactly. A name that is only the
prefix of one field is an error naming that field, and a name that
matches nothing is \code{NULL}, as on any list.

The link fields are not elements of the list, so \code{names()}, \code{[[} and
\code{str()} do not show them. An element stored under one of those names
is refused when it is read, because \code{$} would otherwise have to choose
between it and the links the fit actually applies.
}
\examples{
fam <- beta_binomial()
fam$link
fam$link_phi
try(fam$link_p)   # a prefix of link_phi, refused
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{as.brmsprior}
\alias{as.brmsprior}
\title{Transform an object into a prior specification}
\usage{
as.brmsprior(x)
}
\arguments{
\item{x}{A data frame with a \code{prior} column, a \code{brmsprior}, a
\code{\link[=default_prior]{default_prior()}} table or a \code{frmtmb_priorlist}.}
}
\value{
A \code{frmtmb_priorlist}.
}
\description{
brms's \code{as.brmsprior()}. A data frame (or anything
\code{as.data.frame()} accepts) with a \code{prior} column becomes the object
\code{\link[=set_prior]{set_prior()}} returns, one specification per row. Missing columns
take \code{set_prior()}'s defaults (\code{class = "b"}, empty \code{coef}, \code{group},
\code{resp}, \code{dpar} and \code{nlpar}, no bounds) and columns \code{set_prior()} does
not take are dropped.
}
\details{
The name is brms's, and so is the job: turn a table into the prior
object this package fits with. That object is a \code{frmtmb_priorlist}
rather than a \code{brmsprior}, because this package parses a density
when it is written rather than when the model is compiled. A
\code{brmsprior} built by brms is translated row by row, as \code{\link[=frm]{frm()}}
translates one, and a table from \code{\link[=default_prior]{default_prior()}} or
\code{\link[=validate_prior]{validate_prior()}} is read as written.

A row whose \code{prior} is empty or \code{"(flat)"} and that carries no bound
applies nothing, and is left out: it is the flat default a
\code{\link[=default_prior]{default_prior()}} table lists for a slot nobody has set. So is a row
whose \code{source} is \code{"(vectorized)"}, which repeats the density of the
class row above it.
}
\examples{
as.brmsprior(data.frame(prior = "normal(0,1)", coef = c("a", "b")))
}

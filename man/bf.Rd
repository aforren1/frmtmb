% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bf.R
\name{bf}
\alias{bf}
\title{Set up a model formula}
\usage{
bf(formula, ..., family = NULL, nl = FALSE)
}
\arguments{
\item{formula}{The model formula for \code{mu}.}

\item{...}{Two-sided formulas for other dpars (the left-hand side names
the dpar, e.g. \code{sigma ~ z}), or named scalars fixing a dpar to a
constant on the response scale (e.g. \code{sigma = 1}).}

\item{family}{Optional family; can also be attached with \code{+} or passed
to \code{\link[=frm]{frm()}}.}

\item{nl}{Nonlinear-formula flag (not yet supported).}
}
\value{
An object of class \code{frmtmb_formula}.
}
\description{
Specifies a model with brms-compatible syntax. Distributional
parameters (dpars) can get their own formulas with the full predictor
grammar, or be fixed to constants:
\code{bf(y ~ x + (1 | g), sigma ~ z + (1 | g))}, \code{bf(y ~ x, sigma = 1)}.
Nonlinear (\code{nl = TRUE}) and multivariate formulas arrive in later
versions and signal an error for now.
}
\details{
The left-hand side accepts addition terms after \code{|}:
\code{y | weights(w) ~ ...} and \code{y | trials(n) ~ ...}.
Every linear predictor accepts lme4-style random effects \code{(1 | g)},
\code{(1 + x | g)}, \code{(x || g)}, and explicit covariance-structure wrappers
\code{us(x | g)} and \code{diag(x | g)}.

Attach a family with \code{+}, for example \code{bf(y ~ x) + gaussian()}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{frmtmb-loo-refusals}
\alias{frmtmb-loo-refusals}
\alias{loo_moment_match}
\alias{loo_moment_match.frmtmb_draws}
\alias{loo_subsample}
\alias{loo_subsample.frmtmb_draws}
\alias{reloo}
\alias{reloo.frmtmb_draws}
\alias{kfold}
\alias{kfold.frmtmb_draws}
\alias{bridge_sampler}
\alias{bridge_sampler.frmtmb_draws}
\alias{bayes_factor}
\alias{bayes_factor.frmtmb_draws}
\alias{post_prob}
\alias{post_prob.frmtmb_draws}
\title{Refusals for the refit-based and marginal-likelihood brmsfit methods}
\usage{
loo_moment_match(x, ...)

\method{loo_moment_match}{frmtmb_draws}(x, ...)

loo_subsample(x, ...)

\method{loo_subsample}{frmtmb_draws}(x, ...)

reloo(x, ...)

\method{reloo}{frmtmb_draws}(x, ...)

kfold(x, ...)

\method{kfold}{frmtmb_draws}(x, ...)

bridge_sampler(x, ...)

\method{bridge_sampler}{frmtmb_draws}(x, ...)

bayes_factor(x, ...)

\method{bayes_factor}{frmtmb_draws}(x, ...)

post_prob(x, ...)

\method{post_prob}{frmtmb_draws}(x, ...)
}
\arguments{
\item{x, ...}{Ignored; these methods always stop.}
}
\value{
These functions never return; they signal an error.
}
\description{
These \code{brmsfit} methods have frmtmb spellings that do not exist yet.
They are defined so that a ported script fails with the reason and
the alternative rather than with "could not find function", and they
are documented so the reason is findable.
}
\details{
\itemize{
\item \code{loo_moment_match()}, \code{loo_subsample()}, \code{reloo()} and \code{kfold()}
all need to refit the model on modified data. \code{frm_bootstrap()} is
the resampling machinery frmtmb does have, and \code{AIC()} on the
maximum-likelihood fits answers the comparison question directly.
\item \code{bridge_sampler()}, \code{bayes_factor()} and \code{post_prob()} are
marginal-likelihood quantities. A marginal likelihood is an
integral against the PRIOR, so it is undefined under the flat
improper priors \code{frm_sample(fit)} uses, and even with a proper
\verb{prior =} the bridge-sampling estimator needs a normalized
log-posterior evaluator that the RTMB tape does not expose.
}
}
\examples{
\donttest{
if (requireNamespace("tmbstan", quietly = TRUE) &&
    requireNamespace("rstan", quietly = TRUE)) {
  set.seed(1)
  dd <- data.frame(x = rnorm(40))
  dd$y <- rnorm(40, 1 + 0.5 * dd$x, 1)
  fit <- frm(bf(y ~ x), family = gaussian(), data = dd)
  ds <- frm_sample(fit, chains = 1, iter = 400, refresh = 0)
  # each refusal names its reason and the replacement
  try(reloo(ds))
  try(bayes_factor(ds, ds))
}
}
}

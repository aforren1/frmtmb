% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R, R/predict.R, R/structure.R
\name{response_mean}
\alias{response_mean}
\alias{as_frmtmb_family}
\alias{eval_dpars}
\alias{single_response}
\alias{fit_extras}
\alias{dpar_linpred}
\alias{frmtmb-extension-api}
\title{Accessors for a structured family written outside frmtmb}
\usage{
response_mean(fam, dpars, aterms)

as_frmtmb_family(x)

eval_dpars(fit, b = fit$estimates[["b"]])

single_response(fit, what)

fit_extras(fit)

dpar_linpred(frame, params, resp, dpar)
}
\arguments{
\item{fam}{A \code{frmtmb_family}.}

\item{dpars}{Named list of numeric dpar values, as one element of
\code{eval_dpars()}.}

\item{aterms}{Evaluated addition terms for the response,
\code{fit$frame$aterm_values[[resp]]}.}

\item{x}{A family: a \code{frmtmb_family}, a \code{stats::family()} object, a
family constructor, or a family name.}

\item{fit}{A \code{frmtmb_fit}.}

\item{b}{Random-effect vector to evaluate at, defaulting to the fit's
own estimates. \code{NULL} drops the random-effect contribution.}

\item{what}{The calling function, as it should open a refusal:
\code{"latent_probs()"}.}

\item{frame}{A \code{fit$frame}, or the frame a \code{check_fit} slot is given.}

\item{params}{A parameter list in the frame's layout: a fit's
\code{estimates}, or the starting template a \code{check_fit} slot is given.}

\item{resp, dpar}{The response name and the distributional parameter
name, as \code{eval_dpars()} returns them.}
}
\value{
\code{single_response()} a response spec; \code{eval_dpars()} a nested
list of numeric vectors; \code{fit_extras()} a named list or \code{NULL};
\code{dpar_linpred()} a numeric vector or \code{NULL}; \code{response_mean()} a
numeric vector; \code{as_frmtmb_family()} a \code{frmtmb_family}.
}
\description{
The read-only surface a \code{\link[=frmtmb_structure]{frmtmb_structure()}} slot may use. A
structure's \code{fitted_mean}, \code{fitted_var}, \code{latent_probs} and \code{sim_ctx}
run at the estimates and outside the AD tape, and they are handed a
\code{frmtmb_fit}; these are how such a slot reads what it needs out of
one without reaching into the fit's internal layout. The rest of a
family's needs are already public: \code{\link[=frm]{frm()}}, \code{\link[=bf]{bf()}}, \code{\link[=fixef]{fixef()}},
\code{\link[=set_prior]{set_prior()}}, \code{\link[=posterior_predict]{posterior_predict()}}, \code{\link[=conditional_effects]{conditional_effects()}} and
\code{\link[=hypothesis]{hypothesis()}} among them.
}
\details{
They are documented together because they are one contract with one
promise: what they return is stable, so a family in another package
can be written against them. Everything else in a fit, a frame or a
family object is internal and may be renamed without notice.

\describe{
\item{\code{single_response()}}{The one response spec of a univariate
fit, or the refusal a method owes a multivariate one. \code{what}
names the caller and opens the message. Almost every structured
family is univariate, so this is the first line of most slots.
It was named \code{uni_resp()} while it was internal.}
\item{\code{eval_dpars()}}{The distributional parameters of every
response at the estimates, on their natural (response) scale,
evaluated over the training rows: \code{out[[response]][[dpar]]} is a
numeric vector of length \code{n} or \code{1}. \code{b} supplies a different
random-effect vector, which is how a family evaluates at the
modes of a resampled fit. This is the same evaluation
\code{predict()}, \code{fitted()} and \code{residuals()} run.}
\item{\code{fit_extras()}}{The family's extra (non-dpar) parameters at
the estimates, as a named list in the order \code{extra_pars} declared
them, or \code{NULL} when the family declared none. Item profiles,
ordinal thresholds and class covariances arrive here.}
\item{\code{dpar_linpred()}}{The FIXED-effect part of one dpar's linear
predictor, on the link scale, at a parameter list in the frame's
own layout, or \code{NULL} when that response and dpar have no linear
predictor with columns. Random effects are excluded on purpose:
this exists for a \code{check_fit} slot, which runs before the inner
modes are solved and cannot see them.}
\item{\code{response_mean()}}{The expected response of a family at given
dpar values, with \code{trunc()} bounds applied when the response
carries them. A structured family composing per-state or
per-class means calls this once per state rather than reaching
into the wrapped family's \code{post} slot.}
\item{\code{as_frmtmb_family()}}{The internal representation of a family
given as a \code{frmtmb_family}, a \code{stats::family()}, a bare family
constructor or a name. A family that WRAPS another one calls this
on its argument, so that \code{hmm(2, gaussian)}, \code{hmm(2, gaussian())}
and \code{hmm(2, "gaussian")} all mean the same thing.}
}
}
\examples{
set.seed(1)
dd <- data.frame(x = rnorm(50))
dd$y <- rnorm(50, 1 + 2 * dd$x, 0.5)
fit <- frm(bf(y ~ x) + gaussian(), data = dd)

rspec <- single_response(fit, "my_family_probs()")
dp <- eval_dpars(fit)[[rspec$resp_name]]
str(dp)
head(response_mean(rspec$family, dp, list()))
fit_extras(fit)                       # NULL: no extra parameters
head(dpar_linpred(fit$frame, fit$estimates, rspec$resp_name, "mu"))
as_frmtmb_family(gaussian)
}
\seealso{
\code{\link[=frmtmb_structure]{frmtmb_structure()}} for the protocol these serve, and
\code{\link[=frmtmb_family]{frmtmb_family()}} for the family object they read
}

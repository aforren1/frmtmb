% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{habit_prep}
\alias{habit_prep}
\title{Forced-response habit data from Hardwick et al. (2019)}
\format{
A data frame with 28,940 rows and 10 columns:
\describe{
\item{participant}{Factor. Person identifier, \code{e1-NN} for experiment 1 and
\code{e2-NN} for experiment 2. Experiment 1 identifiers occur in both the
\code{minimal} and the \verb{4day} group.}
\item{group}{Factor with levels \code{minimal}, \verb{4day}, \verb{20day}. Amount of
practice on the original mapping before the remapping.}
\item{subject_code}{Integer. The participant directory in the source
deposit, which numbers the same person differently in each group.}
\item{trial}{Integer. Position within the timed-response assessment. Some
indices are absent because the corresponding trial was not recorded.}
\item{prep_time}{Numeric. Preparation time in seconds, from stimulus onset
to the required response. Values are negative when the response came
before the stimulus appeared.}
\item{remapped}{Logical. \code{TRUE} for the two stimuli whose key changed
between the original and the revised mapping, \code{FALSE} for the two that
kept their key.}
\item{stimulus}{Integer. Symbol shown. The two experiments use different
symbol sets, so the codes are not comparable between them.}
\item{target_key}{Integer in 1:4. Correct key under the revised mapping.}
\item{response_key}{Integer. Key pressed. A few trials record a value
outside 1:4, which is a hardware read failure rather than a key.}
\item{response}{Factor with levels \code{correct}, \code{habit}, \code{other}. The
response category the model is fit to: \code{correct} is the revised mapping,
\code{habit} is the original mapping, and \code{other} is either of the two
remaining keys. \code{NA} where \code{response_key} lies outside 1:4.}
}
}
\source{
Hardwick RM, Forrence AD, Krakauer JW, Haith AM (2019). Time-dependent
competition between goal-directed and habitual response preparation.
\emph{Nature Human Behaviour}, 3, 1252-1262.
\doi{10.1038/s41562-019-0725-0}

Derived from the authors' data deposit, Hardwick RM and Haith AM (2019),
"Time-Dependent Competition Between Goal-Directed and Habitual Response
Selection", \url{https://osf.io/3fjez/}. Used under the MIT License, copyright
(c) 2019 Robert Michael Hardwick and Adrian Mark Haith. See
\code{system.file("COPYRIGHTS", package = "frmtmb")}.

\code{data-raw/habit_prep.R} in the package sources rebuilds this object from the
deposit.
}
\usage{
habit_prep
}
\description{
Trial-level records from the timed-response assessment of Hardwick, Forrence,
Krakauer and Haith (2019). Participants learned a mapping from four symbols
to four keys, then learned a revised mapping in which two of the four symbols
changed key. In the assessment they had to respond at a moment fixed by the
experimenter, which sets the time available to prepare a response. The
interest is in what happens at short preparation times, when the original
mapping can win over the revised one.
}
\details{
Three groups differ in how much the original mapping was practiced before the
remapping: \code{minimal} is experiment 1 before training, \verb{4day} is the same
people after four days of practice, and \verb{20day} is a separate cohort trained
for twenty days. Because \code{minimal} and \verb{4day} are the same people measured
twice, \code{participant} repeats across those two groups.

Each trial is assigned to the remapped or the unchanged stimulus set by its
trial index.

Only the two remapped stimuli carry a habitual response option, so the model
in \code{vignette("habit")} is fit to \code{subset(habit_prep, remapped)}.
}
\examples{
str(habit_prep)
with(subset(habit_prep, remapped), table(group, response))
}
\keyword{datasets}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{log_lik}
\alias{log_lik}
\alias{log_lik.frmtmb_fit}
\title{Pointwise log-likelihood}
\usage{
log_lik(object, ...)

\method{log_lik}{frmtmb_fit}(object, ...)
}
\arguments{
\item{object}{A \code{frmtmb_fit}, or (with \code{frmtmb.sample} loaded) draws.}

\item{...}{Passed to methods.}
}
\value{
This method signals an error on a maximum-likelihood fit.
}
\description{
\code{log_lik()} is the log-likelihood of every observation at every
posterior draw, the matrix \code{\link[=loo]{loo()}} and \code{\link[=waic]{waic()}} are computed from. A
\code{frmtmb_fit} is one maximum-likelihood parameter vector rather than a
posterior, so this refuses and names the route to draws, as \code{loo()}
and \code{waic()} do; the estimator itself is in the \code{frmtmb.sample}
package, on this same generic.
}
\details{
The generic lives here rather than in \code{frmtmb.sample} so that a
script ported from brms is told to sample before it has sampled.
Without it \code{log_lik(fit)} stopped at "could not find function", which
names neither the reason nor the route.
}
\examples{
set.seed(1)
dd <- data.frame(x = rnorm(40))
dd$y <- rnorm(40, 1 + 0.5 * dd$x, 1)
fit <- frm(bf(y ~ x) + gaussian(), data = dd)

# the maximum-likelihood total is available directly
logLik(fit)
# the pointwise posterior matrix needs draws, and says so
try(log_lik(fit))
}
\seealso{
\code{\link[=loo]{loo()}} and \code{\link[=waic]{waic()}}, which average this matrix over draws;
\code{\link[=logLik]{logLik()}} for the maximum-likelihood total.
}

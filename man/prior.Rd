% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{prior}
\alias{prior}
\alias{prior_}
\alias{prior_string}
\title{Set up priors with brms's quoting spelling}
\usage{
prior(prior, ...)

prior_(prior, ...)

prior_string(prior, ...)
}
\arguments{
\item{prior}{Distribution string, e.g. \code{"normal(0, 5)"}, or a
\code{\link[=prior_normal]{prior_normal()}}/\code{\link[=prior_t]{prior_t()}}/\code{\link[=prior_lkj]{prior_lkj()}} object, or \code{""} for
bounds only.}

\item{...}{Any of \code{\link[=set_prior]{set_prior()}}'s remaining arguments: \code{class},
\code{coef}, \code{group}, \code{resp}, \code{dpar}, \code{nlpar}, \code{lb}, \code{ub}.}
}
\value{
A \code{frmtmb_priorlist}.
}
\description{
\code{prior()} is \code{\link[=set_prior]{set_prior()}} with the distribution given UNQUOTED, as
brms's \code{prior()} takes it: \code{prior(normal(5000, 1000), nlpar = "ult")}
is \code{set_prior("normal(5000, 1000)", nlpar = "ult")}. Every argument
is deparsed rather than evaluated, so \code{class = b} and \code{class = "b"}
mean the same thing, and a variable holding a distribution is
deparsed to its NAME rather than its value: use \code{\link[=prior_string]{prior_string()}} to
build a prior from strings computed at run time.
}
\details{
\code{prior_()} takes one-sided formulas, calls, names or constants
(\code{prior_(~normal(0, 10), class = ~b)}) and \code{prior_string()} takes
plain strings; both exist so that priors can be built
programmatically, and both are brms's.

A frmtmb prior and a brms prior are different objects, and with brms
attached after frmtmb its \code{prior()} masks this one. Nothing breaks:
\code{\link[=frm]{frm()}} and \code{frmtmb.sample::frm_sample()} accept a \code{brmsprior} object
and translate its rows, so \code{c(prior(...), prior(...))} copied out of a
brms script
works whichever \code{prior()} was in scope.
}
\examples{
# the brms nonlinear vignette's spelling
prior(normal(5000, 1000), nlpar = "ult")

# combine with c() or `+`, as with set_prior()
c(prior(normal(1, 2), nlpar = "omega"),
  prior(normal(45, 10), nlpar = "theta"))

# the programmatic spellings
prior_(~normal(0, 10), class = ~b)
prior_string(paste0("normal(0, ", 2 * 5, ")"), class = "b")
}

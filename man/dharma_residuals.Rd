% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{dharma_residuals}
\alias{dharma_residuals}
\title{DHARMa residual diagnostics}
\usage{
dharma_residuals(fit, nsim = 250, re.form = NULL, seed = NULL, ...)
}
\arguments{
\item{fit}{A \code{frmtmb_fit} (univariate; the family needs a simulator).}

\item{nsim}{Number of simulated response vectors.}

\item{re.form}{Passed to \code{\link[=simulate.frmtmb_fit]{simulate.frmtmb_fit()}}: \code{NULL} (default)
conditions on the estimated random effects; \code{NA} redraws them.}

\item{seed}{Optional RNG seed for the simulations.}

\item{...}{Passed to \code{\link[DHARMa:createDHARMa]{DHARMa::createDHARMa()}}.}
}
\value{
A \code{DHARMa} object.
}
\description{
Builds a DHARMa object from the fit's family simulator: scaled
quantile residuals that are uniform under a correctly specified
model. Plot the result with \code{plot()}, or run
\code{\link[DHARMa:testUniformity]{DHARMa::testUniformity()}}, \code{\link[DHARMa:testDispersion]{DHARMa::testDispersion()}},
\code{\link[DHARMa:testZeroInflation]{DHARMa::testZeroInflation()}} and friends on it.
}
\section{Ordinal responses}{

An ordinal fit is supported: the draws are the simulated categories
and the response is its own integer codes, so the rank transform runs
on the order alone (\code{integerResponse = TRUE}, which is what makes
DHARMa randomize within ties). The \code{fittedPredictedResponse} DHARMa
plots against is the expected category index \verb{sum_k k * P(y = k)},
the same scalar \code{\link[=residuals]{residuals()}} scores an ordinal fit by; it sets the
horizontal axis of the display and nothing else, since the residuals
themselves come from the ranks of the draws.

A NOMINAL response (the \code{categorical()} family) is refused. A scaled
quantile residual is the predictive CDF evaluated at the observation,
and a CDF needs an ordered support. Ordinal categories have one, so
their codes carry real information; nominal ones do not, and their
1..K codes are an arbitrary labeling - relabel the levels and every
residual moves, which is not a diagnostic. DHARMa says the same of
multinomial responses in its own vignette. Check a nominal fit with
\code{\link[=pp_check]{pp_check()}} instead (\code{type = "bars"} compares the observed category
counts with the simulated ones), or read the per-category
probabilities through \code{conditional_effects()}.
}

\examples{
if (requireNamespace("DHARMa", quietly = TRUE)) {
  set.seed(1)
  dd <- data.frame(x = rnorm(100), g = factor(rep(1:10, 10)))
  dd$y <- rpois(100, exp(0.3 + 0.4 * dd$x + rnorm(10, 0, 0.6)[dd$g]))
  fit <- frm(bf(y ~ x + (1 | g)) + poisson(), data = dd)

  # scaled quantile residuals: uniform under a correct model, which
  # is what Pearson residuals cannot give for a discrete family
  res <- dharma_residuals(fit, nsim = 100, seed = 1)
  plot(res)
  DHARMa::testUniformity(res, plot = FALSE)
  DHARMa::testDispersion(res, plot = FALSE)
}
}

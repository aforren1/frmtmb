% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{frmtmb_register_aterm}
\alias{frmtmb_register_aterm}
\title{Add an addition term from another package}
\usage{
frmtmb_register_aterm(name, arity = 1L, coerce = as.numeric)
}
\arguments{
\item{name}{The term's name, as it is written in a formula, without
parentheses: \code{"dec"} accepts \code{y | dec(response) ~ x}.}

\item{arity}{How many arguments the term takes. With \code{arity = 1} the
value arrives as \code{aterms[[name]]}; above one it arrives as
\code{aterms[[paste0(name, i)]]} for each argument, which is the
\code{vint()} convention.}

\item{coerce}{Function applied to each evaluated argument, once, at
frame assembly. It must return a numeric vector, because the value
is baked into the tape as data. The default is \code{\link[=as.numeric]{as.numeric()}}; a
term whose natural spelling is a factor gives a function that maps
the factor to the numbers the density wants.}
}
\value{
\code{NULL}, invisibly. Called for the registration.
}
\description{
Registers an addition term (a brms "aterm": the \code{trials(n)} in
\code{y | trials(n) ~ x}) that frmtmb will accept on the left-hand side of
a formula. A family that lives outside frmtmb uses this to give its
per-row data the spelling its literature uses, instead of asking
users for \code{vint()}. Register from the contributing package's
\code{.onLoad()}.
}
\details{
The registered term carries DATA and nothing else. Its evaluated,
coerced value reaches the family's \code{lpdf}, \code{lcdf}, \code{post$mean_fn} and
\code{sim} in the \code{aterms} list under \code{name} (or under \code{name1}, \code{name2},
... when \code{arity} is above one, following \code{vint()}), it is required on
\code{newdata}, and it is what \code{frmtmb_family(required_aterms =)} names.
It cannot reshape the likelihood: censoring, truncation and case
weights are core terms because the core acts on them, and a
contributed term is not a route to that.

Registering a name twice replaces the earlier entry, so that
reloading the contributing package is not an error. The eight core
terms cannot be replaced.
}
\examples{
# a decision indicator, given as a factor and delivered as 0/1
\dontrun{
frmtmb_register_aterm("dec", arity = 1, coerce = function(x) {
  as.integer(factor(x)) - 1L
})
}
}
\seealso{
\code{\link[=frmtmb_family]{frmtmb_family()}} for \code{required_aterms}, which is how a
family says the term is not optional, \code{\link[=frmtmb_register_frame_check]{frmtmb_register_frame_check()}}
for the seam that refuses a data problem, and
\link{frmtmb-extension-api} for the accessors an extension may use
}

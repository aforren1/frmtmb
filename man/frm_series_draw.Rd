% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral.R
\name{frm_series_draw}
\alias{frm_series_draw}
\title{Draw a time series from a fitted spectrum}
\usage{
frm_series_draw(
  object,
  nsim = 1,
  seed = NULL,
  newdata = NULL,
  freq = "freq",
  ...
)
}
\arguments{
\item{object}{A \code{frmtmb_fit} with a \code{\link[=whittle]{whittle()}} family.}

\item{nsim}{Number of series to draw.}

\item{seed}{Optional integer seed.}

\item{newdata}{Rows to take the spectrum from, for a fit that pools
several series. Defaults to the fitted rows.}

\item{freq}{The frequencies, either as a column name in the data or
as the numbers themselves. A model written in terms of a
transformed frequency keeps no \code{freq} column, so passing
\code{pg$freq} is the general route.}

\item{...}{Passed to \code{\link[=predict]{predict()}}, for example \code{re.form}.}
}
\value{
A \code{ts} matrix with \code{2 * nf + 1} rows and \code{nsim} columns, at
the sampling rate the grid implies.
}
\description{
The inverse of the transform a spectral fit is built on, and the verb
to reach for when \code{simulate()} gave ordinates where a series was
wanted. It draws a stationary Gaussian series whose spectral density
is the one the fit estimated, by giving each Fourier frequency an
independent complex Gaussian amplitude with the fitted variance and
transforming back.
}
\details{
A simulated PERIODOGRAM cannot be inverted: the phases are not in it.
What is well defined is a draw from the fitted MODEL, which is what
this is, and it is a different object - two calls with the same
fitted spectrum give two unrelated series.

The length is fixed by the grid at \code{2 * nf + 1}, an odd number, so
that the drawn series has no Nyquist ordinate to invent and exactly
zero mean. The sampling rate follows from the grid spacing, and the
result carries it as a \code{ts} frequency, so
\code{frm_periodogram(frm_series_draw(fit))} lands back on the same grid.
}
\examples{
set.seed(1)
y <- as.numeric(arima.sim(list(ar = 0.6), 512))
pg <- frm_periodogram(y, fs = 128)
fit <- frm(bf(pgram ~ s(freq, k = 8)), family = whittle(), data = pg)

# a new series with the same estimated spectrum, not the same series
z <- frm_series_draw(fit, nsim = 2, seed = 1)
dim(z)
stats::frequency(z)
}
\seealso{
\code{\link[=whittle]{whittle()}}, \code{\link[=frm_periodogram]{frm_periodogram()}}.
}

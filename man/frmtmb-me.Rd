% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/me.R
\name{frmtmb-me}
\alias{frmtmb-me}
\alias{me}
\title{Noise-free predictors with known measurement error}
\description{
\code{me(x, sdx)} in a formula says that the column \code{x} is a noisy
measurement of a latent, noise-free value, with known measurement
standard deviation \code{sdx}, and that the model uses the latent value.
It is brms's \code{me()} term with brms's meaning.
}
\details{
The model is
\deqn{x_i \sim N(\tilde{x}_i, sdx_i), \qquad
  \tilde{x}_i \sim N(meanme, sdme)}

where \code{meanme} and \code{sdme} are estimated. The coefficient of the term
is \verb{bsp_me<x><sdx>} in brms's names, for example \code{bsp_mexsx} for
\code{me(x, sx)}.

\code{me()} is not a function. It is a formula special that the parser
reads, like \code{mo()} and \code{mi()}.
}
\section{Arguments of \code{me()}}{

\itemize{
\item \code{x}: the noisy variable, a numeric column or an expression of
columns, such as \code{log(x)}.
\item \code{sdx}: the known measurement SD, a column, an expression or a
positive constant.
\item \code{gr}: optional. The name of a grouping column. There is then one
latent value per level of \code{gr}, and \code{x} and \code{sdx} must be constant
within each level.
}
}

\section{Estimation}{

The latent values are integrated out by the Laplace approximation,
together with the random effects. For a gaussian response with a
linear \code{me()} term the marginal likelihood is multivariate normal,
and the approximation is exact. An interaction of two \code{me()} terms,
or a non-gaussian response, makes it approximate.

Several \code{me()} terms that share a grouping have correlated latent
values by default, as in brms. Use \code{\link[=set_mecor]{set_mecor()}} to make them
independent.

\code{me()} can appear on its own, in a \code{:} or \code{*} interaction with
numeric variables or with other \code{me()} terms, and in the formula of
any distributional or nonlinear parameter. It cannot appear in a
group-level term, in a nonlinear formula body, inside another
function call, or in the same interaction as \code{mo()} or \code{mi()}.
}

\section{Parameters and methods}{

The hyperparameters are the template components \code{meanme}, \code{logsdme}
(the log of \code{sdme}) and \code{thetame} (the correlation parameters). The
Noise-free Terms section of \code{\link[=summary]{summary()}} reports them on brms's scale
as \verb{meanme_<coef>}, \verb{sdme_<coef>} and \verb{corme__<coef1>__<coef2>}.
Priors on brms's classes \code{meanme}, \code{sdme} and \code{corme} are refused;
class \code{"b"} addresses the \code{me()} coefficients by their names, for
example \code{coef = "mexsx"}.

\code{\link[=anova]{anova()}} compares only fits with the same \code{me()} calls, because each
call puts its noisy variable's measurements into the likelihood.
\code{\link[=AIC]{AIC()}} does not check this, so compare by AIC only across fits that
share their \code{me()} calls.

\code{\link[=fitted]{fitted()}}, \code{\link[=predict]{predict()}}, \code{\link[=residuals]{residuals()}} and \code{\link[=simulate]{simulate()}} on the fitted
data use the estimated latent values (the conditional modes). On new
data the observed value of the noisy variable stands in for the latent
value. brms draws that value from \code{N(x, sdx)}, so the two agree on
the mean of a linear term, and frmtmb does not add the measurement
noise to a prediction interval.
}

\examples{
set.seed(1)
n <- 200
tx <- rnorm(n, 1, 0.8)
d <- data.frame(x = tx + rnorm(n, 0, 0.4), sx = 0.4)
d$y <- 2 + 0.7 * tx + rnorm(n, 0, 0.5)

fit <- frm(bf(y ~ me(x, sx)) + gaussian(), data = d)
fixef(fit)

# the naive slope is attenuated by the measurement error
coef(lm(y ~ x, data = d))[["x"]]
}
\seealso{
\code{\link[=set_mecor]{set_mecor()}}; \code{mi()} for a measurement model that has
covariates of its own, \code{bf(x | mi(sdx) ~ z)}.
}

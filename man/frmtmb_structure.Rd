% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure.R
\name{frmtmb_structure}
\alias{frmtmb_structure}
\title{Declare a non-rowwise likelihood to the core}
\usage{
frmtmb_structure(
  frame_vars = NULL,
  keep_na = FALSE,
  check_spec = NULL,
  frame_block = NULL,
  check_frame = NULL,
  check_fit = NULL,
  loglik = NULL,
  unit = NULL,
  fitted_mean = NULL,
  fitted_var = NULL,
  latent_probs = NULL,
  sim_ctx = NULL,
  supports = list(),
  refusals = list()
)
}
\arguments{
\item{frame_vars}{\verb{function(fam)} returning a list of language
objects whose variables must be in the model frame but belong to no
linear predictor: a grouping column, a time column, a sequence id.
It runs before any data is seen, so it may read only the family.}

\item{keep_na}{\code{TRUE} means an \code{NA} in the response is data the
family reads, so the row survives \code{na.action}. \code{NA}s in every other
variable still drop the row. A family that keeps them must handle
them in \code{frame_block} (a mask, a placeholder) or in \code{loglik}.}

\item{check_spec}{\verb{function(resp, spec, av)} run before the generic
addition-term guards, so a structured family refuses a term in its
own words rather than through a missing CDF further down. It sees
the response spec, the whole spec (for the univariate check) and
the evaluated addition terms. It returns nothing and stops to
refuse.}

\item{frame_block}{\verb{function(resp, spec, av, mf, y, n)} run once at
frame assembly, after \code{y} is coerced and before the random-effect
blocks are built. It returns the block; see the Block section.}

\item{check_frame}{\verb{function(spec, frame)} run after the predictors
and random-effect blocks exist, for a refusal that depends on the
design rather than on the response. It sees the assembled frame
without the parameter template.}

\item{check_fit}{\verb{function(resp, frame, template)} run once the
starting values are resolved and before the optimizer, for a
refusal or a warning that depends on WHERE the fit starts rather
than on the model. \code{template} is the starting parameter list. The
capability flags cover the fitting options a structure cannot
answer at all; this covers a start the structure can answer only
badly, such as a start sitting on a label-symmetry axis.}

\item{loglik}{\verb{function(y, dpars, aterms, weights, block, extra)}
returning the taped log-likelihood of the WHOLE response as one AD
scalar: \code{sum(log-likelihood)}, not a negative. \code{y} is the response
after any \code{block[["y"]]} replacement; \code{dpars} are the evaluated
distributional parameters on the natural scale, one AD vector of
length \code{n} or \code{1} each; \code{weights} are the effective row weights
with cluster weights folded in, or \code{1}; \code{extra} are the family's
extra parameters as an AD list, in the order \code{extra_pars} declared
them. The family decides what a row weight means for a likelihood
that is not rowwise, and may have refused weights in \code{check_spec}.
It must not call \code{RTMB::OBS()}.}

\item{unit}{One noun phrase naming the smallest independent unit of
\code{loglik}, as it should read in the middle of a sentence: "a
hidden-Markov sequence", "a group-level mixture". The core quotes
it where it must explain that a per-OBSERVATION quantity does not
exist, which today is \code{\link[=loo]{loo()}} and \code{\link[=waic]{waic()}} refusing a pointwise
log-likelihood matrix. Ignored when \code{loglik} is \code{NULL}.}

\item{fitted_mean, fitted_var}{\verb{function(fit, block)} giving the
conditional mean and variance of each row GIVEN the whole observed
response, for \code{\link[=fitted]{fitted()}}, \code{predict(type = "response")} on the
training data, and pearson residuals. \code{NULL} means "use the rowwise
family mean", which is what a group-level mixture wants. A family
with no mean supplies a function that stops. Both run at the
estimates, outside the tape.}

\item{latent_probs}{\verb{function(fit, block)} returning one matrix of
posterior latent-state probabilities with column names, \code{n} rows or
one row per group.}

\item{sim_ctx}{\verb{function(ctx)} drawing the whole response, the
structured-simulator contract of \code{\link[=frmtmb_family]{frmtmb_family()}} with
\code{ctx[["block"]]} carrying the block. One implementation serves
\code{\link[=simulate]{simulate()}}, \code{\link[=posterior_predict]{posterior_predict()}} and \code{\link[=frm_simulate]{frm_simulate()}}.}

\item{supports}{Named logical vector or list of capability flags; see
Capability flags. Unnamed entries and unknown names are refused.}

\item{refusals}{Named list of one message per \code{FALSE} flag.}
}
\value{
An object of class \code{frmtmb_structure}.
}
\description{
A \code{frmtmb_structure()} is what a family carries when its likelihood
does not factorize over the rows of the data: a group-level
\code{\link[=mixture]{mixture()}}, a hidden Markov chain, a latent class measurement model.
It is one object with one contract, so the core needs no branch per
family and a structured family can live in another package. Attach it
with \code{frmtmb_family(structure = )}.
}
\details{
Every slot defaults to the rowwise behavior, which is what a family
that changes the likelihood and nothing else needs: give \code{loglik} and
leave the rest alone.

\code{loglik = NULL} keeps the family's own rowwise \code{lpdf} and makes the
structure a CAPABILITY DECLARATION instead. That is what a family
whose likelihood does factorize per row but which still has to refuse
things in its own words wants: \code{\link[=lca]{lca()}} is rowwise per subject and
still refuses \code{REML}, \code{residuals(type = "osa")} and random effects in
its gating predictor, and each of those refusals is a property of the
family rather than of the method that meets it.
}
\section{The block}{

\code{frame_block()} returns the \strong{block}: a plain list of DATA, stored
at \code{fit$frame$blocks[[response]]} and handed back to \code{loglik()},
\code{fitted_mean()}, \code{fitted_var()}, \code{latent_probs()} and \code{sim_ctx()}. It
is saved inside the fit and rebuilt by \code{refit()}, so it must hold no
AD values and no closure that captures the model frame. Read and
write it with \verb{[[ ]]} only: \code{$} partial matching is how a \code{mix} read
once returned \code{mix_g}.

Three names in it are reserved, because the core reads them:
\describe{
\item{\code{y}}{If present, replaces the response vector for every later
stage, which is how a family fills a placeholder in for an \code{NA}
it keeps. Otherwise the response is unchanged.}
\item{\code{miss}}{A logical \code{n}-vector. Residuals are \code{NA} at these
rows. Optional.}
\item{\code{mask}}{A numeric 0/1 \code{n}-vector the family multiplies into
its own density. The core does not read it; the name is reserved
so that every structured family spells it the same way.}
}
Everything else in the block belongs to the family.

[ ]: R:\%20
}

\section{Capability flags}{

\code{supports} is a named logical vector or list. Every name defaults to
\code{FALSE}, so a structured family starts fully refused and opts in:
\describe{
\item{\code{reml}}{\code{REML = TRUE}.}
\item{\code{quadrature}}{\verb{quadrature =} other than the Laplace default.}
\item{\code{profile}}{\code{frmtmb_control(profile = TRUE)}.}
\item{\code{newdata_response}}{\code{predict(newdata =, type = "response")}.}
\item{\code{se_fit_response}}{\code{predict(se.fit = TRUE, type = "response")}.}
\item{\code{re_form}}{\verb{re.form =} in \code{predict()} and \code{simulate()}.}
\item{\code{conditional_effects}}{\code{\link[=conditional_effects]{conditional_effects()}}.}
\item{\code{osa}}{\code{residuals(type = "osa")}.}
\item{\code{deviance}}{\code{residuals(type = "deviance")}.}
\item{\code{multivariate}}{\code{\link[=mvbf]{mvbf()}} and \code{rescor = TRUE}.}
\item{\code{cens_trunc}}{\code{cens()} and \code{trunc()}.}
\item{\code{mi}}{\code{mi()} on the same response.}
\item{\code{cluster_robust}}{\code{\link[=vcov_cluster]{vcov_cluster()}}.}
}
\code{FALSE} refuses the capability outright. \code{TRUE} means only that the
structure does not stand in the way: the core's ordinary rules still
apply, so a mixture that sets \code{deviance = TRUE} is still refused a
deviance residual by the family's missing \code{dev_fn}, with the message
that refusal has always carried.

Prediction on the link scale with \verb{dpar =} is always available and is
not a flag: the linear predictors are rowwise and belong to the core.

\code{reml}, \code{quadrature} and \code{profile} are read at fit time; the rest by
the method each one names. \code{multivariate}, \code{cens_trunc} and \code{mi}
describe model shapes a family will usually want to refuse from
\code{check_spec} instead, where it can say which addition term was the
problem.

\code{refusals[[flag]]} is the WHOLE message a user sees when that
capability is asked for, so a family explains its own refusal in its
own words instead of through a generic sentence. A flag with no
string of its own gets a generic one naming the family. A name may
carry a context suffix, \code{"re_form.simulate"}, when one flag is
refused for two different reasons in two places; the core falls back
to the bare flag name.
}

\examples{
# a structure whose likelihood is the rowwise one, written out: the
# smallest thing the protocol accepts
st <- frmtmb_structure(
  loglik = function(y, dpars, aterms, weights, block, extra) {
    sum(weights * RTMB::dnorm(y, dpars$mu, dpars$sigma, log = TRUE))
  },
  supports = list(conditional_effects = TRUE)
)
st
}
\seealso{
\link{frmtmb-extension-api} for the accessors a slot may use to
read a fit, \code{\link[=latent_probs]{latent_probs()}} for the generic \code{latent_probs}
answers, \code{\link[=frmtmb_family]{frmtmb_family()}}, \code{\link[=mixture]{mixture()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{mixture_probs}
\alias{mixture_probs}
\title{Posterior class probabilities of a mixture fit}
\usage{
mixture_probs(fit)
}
\arguments{
\item{fit}{A \code{frmtmb_fit} with a mixture family.}
}
\value{
A matrix of class probabilities (rows sum to one).
}
\description{
For an ordinary \code{\link[=mixture]{mixture()}} or \code{\link[=mixture_mvn]{mixture_mvn()}} fit, one row per
observation; for a group-level mixture (\code{groups = ~g}), one row per
group.
}
\examples{
set.seed(3)
dd <- data.frame(y = c(rnorm(80, 0, 1), rnorm(80, 5, 1)))
fit <- frm(bf(y ~ 1) + mixture(gaussian(), gaussian()), data = dd)

p <- mixture_probs(fit)
head(p)
rowSums(p)[1:3]                    # rows sum to one

# the hard assignment, and how well it recovers the truth
cl <- max.col(p)
table(cl, truth = rep(1:2, each = 80))
}

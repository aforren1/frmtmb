% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{validate_prior}
\alias{validate_prior}
\title{Check a prior against a model}
\usage{
validate_prior(
  prior,
  formula,
  data,
  family = NULL,
  data2 = list(),
  route = c("fit", "sample")
)
}
\arguments{
\item{prior}{A \code{frmtmb_priorlist} from \code{\link[=set_prior]{set_prior()}}, a \code{brmsprior},
or \code{NULL} for none.}

\item{formula}{A \code{bf()} formula (with family) or a plain formula.}

\item{data}{A data frame of model data (ignored when \code{object} is a
fit).}

\item{family}{Family, when \code{object} does not carry one.}

\item{data2}{Structural objects, as in \code{\link[=frm]{frm()}} (ignored when
\code{object} is a fit, which carries its own).}

\item{route}{Which route's defaults the \code{prior} column reports.
\code{"fit"} (the default) reports the defaults \code{\link[=frm]{frm()}} applies, which
are flat in every slot; it consults no registry, so its answer
does not depend on which packages are attached. \code{"sample"} reports
the defaults \code{frmtmb.sample::frm_sample()} applies, and refuses
when no package has registered any. The returned object records
the route and \code{print()} names it on its first line.}
}
\value{
A \code{frmtmb_prior_rows} table, as \code{\link[=default_prior]{default_prior()}} returns.
}
\description{
brms's \code{validate_prior()}: resolve a prior specification against the
model it is meant for, refuse it if any part of it addresses nothing
the model has, and return the whole \code{\link[=default_prior]{default_prior()}} table with the
prior filled in. Fitting runs the same check, so a prior this
function accepts is one \code{\link[=frm]{frm()}} accepts, and a prior it refuses is
refused here with the message the fit would give, before any fitting
work.
}
\details{
A row the prior sets reads \code{source = "user"}. A row that takes its
density from a class row above it, the coefficients under a class
\code{"b"} prior or the blocks under a class \code{"sd"} prior, reads
\code{source = "(vectorized)"} and repeats that density, as brms prints
it. Every other row keeps its default.

Two specifications for the same slot are refused, as \code{\link[=frm]{frm()}} and
brms refuse them. Where specifications for different slots reach one
parameter, the table shows the more specific one, which is the one
\code{\link[=frm]{frm()}} applies; see \code{\link[=set_prior]{set_prior()}}.

The table can be passed back as \verb{prior =}: \code{\link[=frm]{frm()}} reads a
\code{frmtmb_prior_rows} table the way \code{\link[=as.brmsprior]{as.brmsprior()}} does, applying the
rows with a density or a bound and skipping the flat and
vectorized ones.
}
\examples{
dd <- data.frame(y = rnorm(60), x = rnorm(60), z = rnorm(60),
                 g = factor(rep(1:6, 10)))
validate_prior(prior(normal(0, 10), class = b) +
                 prior(cauchy(0, 2), class = sd),
               y ~ x + z + (1 | g), data = dd)
# a prior on a coefficient the model does not have is refused
try(validate_prior(prior(normal(0, 1), coef = w), y ~ x, data = dd))
}

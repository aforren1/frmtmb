% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-fit.R
\name{expose_functions}
\alias{expose_functions}
\alias{expose_functions.frmtmb_fit}
\title{Expose a model's compiled functions}
\usage{
expose_functions(x, ...)

\method{expose_functions}{frmtmb_fit}(x, ...)
}
\arguments{
\item{x}{A \code{frmtmb_fit}.}

\item{...}{Ignored; this method always stops.}
}
\value{
This function never returns; it signals an error.
}
\description{
brms compiles Stan functions and \code{expose_functions()} makes them
callable from R. frmtmb compiles nothing: the model is an R closure
built by \code{build_objective()} and differentiated by RTMB, and a
custom family's density is the plain R function handed to
\code{\link[=custom_family]{custom_family()}}. The method is defined so that a ported brms
script gets that reason rather than "could not find function".
}
\examples{
set.seed(1)
dd <- data.frame(x = rnorm(40))
dd$y <- rnorm(40, 1 + 0.5 * dd$x, 1)
fit <- frm(bf(y ~ x) + gaussian(), data = dd)
try(expose_functions(fit))
}

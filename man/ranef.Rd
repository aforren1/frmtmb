% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-fit.R
\name{ranef}
\alias{ranef}
\alias{ranef.frmtmb_fit}
\title{Extract random-effect modes}
\usage{
ranef(object, ...)

\method{ranef}{frmtmb_fit}(object, condVar = FALSE, ...)
}
\arguments{
\item{object}{A \code{frmtmb_fit}.}

\item{...}{Unused.}

\item{condVar}{If \code{TRUE}, attach the conditional SDs of the modes
(from the Laplace posterior) as a \code{"condSD"} attribute on each
matrix, in matching layout.}
}
\value{
A named list of levels-by-coefficients matrices, one per
random-effect term, KEYED BY THE GROUPING FACTOR as brms and lme4
key it (so \code{ranef(fit)$g} and \code{coef(fit)$g} name the same group).
Two terms on one factor give two entries under one name, so index
by position when a model has them; each matrix carries the block
label that tells them apart in its \code{"term"} attribute, which is
also the key \code{\link[=VarCorr]{VarCorr()}} uses and the \code{grp} column of
\code{as.data.frame()}. That long form (with a \code{condsd} column when
\code{condVar = TRUE} was used) is what broom.mixed-style code reads.
}
\description{
Extract random-effect modes
}
\examples{
set.seed(1)
dd <- data.frame(x = rnorm(100), g = factor(rep(1:10, 10)))
dd$y <- rnorm(100, 1 + 0.5 * dd$x + rnorm(10, 0, 0.8)[dd$g], 1)
fit <- frm(bf(y ~ x + (1 | g)) + gaussian(), data = dd)

# one matrix per random-effect term, levels by coefficients
ranef(fit)
ranef(fit)$g[1:3, ]                 # keyed by the grouping factor
attr(ranef(fit)$g, "term")          # the block it came from

# condVar adds the conditional SDs a caterpillar plot needs
re <- as.data.frame(ranef(fit, condVar = TRUE))
head(re)
with(re[order(re$condval), ],
     plot(condval, seq_along(condval), pch = 16,
          xlim = range(condval - 2 * condsd, condval + 2 * condsd),
          xlab = "conditional mode", ylab = "group"))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure.R
\name{frmtmb_register_frame_check}
\alias{frmtmb_register_frame_check}
\title{Check an assembled model frame from another package}
\usage{
frmtmb_register_frame_check(fn)
}
\arguments{
\item{fn}{A function of two arguments, the model specification and
the assembled frame.}
}
\value{
\code{NULL}, invisibly. Called for the registration.
}
\description{
Registers a function that frmtmb calls on every model frame it
assembles, after the response and the design matrices are built and
before the objective is taped. A package that adds model syntax of
its own uses this to refuse a data problem that only the assembled
frame can show. Register from the contributing package's \code{.onLoad()}.
}
\details{
The check is called as \code{fn(spec, frame)} and its return value is
discarded. To refuse a model, call \code{stop()} with a message that says
what is wrong and what the user can do instead. A check with nothing
to say returns without error. The frame is a read-only view: a check
must not try to change the model.

\code{frame} is a named list. These elements are the contract:

\describe{
\item{\code{spec}}{The parsed model specification, the same object the
first argument carries.}
\item{\code{n_obs}}{Number of observations.}
\item{\code{y}}{The response, already coerced.}
\item{\code{y_levels}}{Response levels for a categorical family, else
\code{NULL}.}
\item{\code{aterm_values}}{Values of the addition terms, by response.}
\item{\code{data_frame}}{The model frame, one row per observation.}
\item{\code{linpred}}{\verb{function(resp_name, dpar)} giving the linear
predictor of one distributional or nonlinear parameter, or \code{NULL}
when there is none. Elements \code{X} and \code{Z} are its fixed and random
design matrices. Use this accessor. The key format of the
\code{linpreds} list itself is internal and can change.}
}

Other elements of \code{frame} are internal assembly structures. They are
present, but they are not part of the contract.
}
\examples{
# the shape of a check: refuse what only the assembled frame shows
check_not_constant <- function(spec, frame) {
  if (length(unique(frame$y)) == 1L) {
    stop("The response takes one value, so nothing can be estimated.",
         call. = FALSE)
  }
}

# a registration lasts for the session, so it belongs in the
# contributing package's .onLoad(), not in a script
\dontrun{
frmtmb_register_frame_check(check_not_constant)
}
}
\seealso{
\code{\link[=frmtmb_structure]{frmtmb_structure()}} for the family-side protocol,
\code{\link[=frmtmb_register_aterm]{frmtmb_register_aterm()}} and \code{\link[=frmtmb_register_compat]{frmtmb_register_compat()}} for the
other two registries, and \link{frmtmb-extension-api} for the
accessors an extension may use
}

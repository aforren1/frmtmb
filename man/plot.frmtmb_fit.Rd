% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional-effects.R
\name{plot.frmtmb_fit}
\alias{plot.frmtmb_fit}
\title{Diagnostic plots for a fit}
\usage{
\method{plot}{frmtmb_fit}(x, which = 1:2, ask = NULL, ...)
}
\arguments{
\item{x}{A \code{frmtmb_fit}.}

\item{which}{Subset of \code{1:2}.}

\item{ask}{Whether to prompt between plots; defaults to the usual
interactive-device rule.}

\item{...}{Unused.}
}
\value{
\code{x}, invisibly. Called for the plots it draws.
}
\description{
Panel 1: Pearson residuals against fitted values with a lowess
trend. Panel 2: normal QQ plot of the Pearson residuals. For
simulation-based residuals that are exact for discrete families, use
\code{\link[=dharma_residuals]{dharma_residuals()}} or \code{residuals(type = "osa")}.
}
\details{
On an ordinal fit \code{\link[=fitted]{fitted()}} is a matrix of category probabilities,
so panel 1 uses the expected category index \verb{sum_k k * P(y = k)} -
the same scalar the Pearson residual is taken against - and labels
the axis accordingly.
}
\examples{
set.seed(1)
dd <- data.frame(x = rnorm(100), g = factor(rep(1:10, 10)))
dd$y <- rnorm(100, 1 + 0.5 * dd$x + rnorm(10, 0, 0.8)[dd$g], 1)
fit <- frm(bf(y ~ x + (1 | g)) + gaussian(), data = dd)

# both panels, side by side
op <- par(mfrow = c(1, 2))
plot(fit, ask = FALSE)
par(op)

# just the QQ panel
plot(fit, which = 2)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-brms.R
\name{predict.frmtmb_fit}
\alias{predict.frmtmb_fit}
\title{Predictions from a frmtmb fit}
\usage{
\method{predict}{frmtmb_fit}(
  object,
  newdata = NULL,
  re_formula = NULL,
  transform = NULL,
  resp = NULL,
  negative_rt = FALSE,
  ndraws = NULL,
  draw_ids = NULL,
  sort = FALSE,
  ntrys = 100L,
  cores = NULL,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975),
  ...,
  allow_new_levels = FALSE,
  sample_new_levels = NULL,
  param_uncertainty = TRUE
)
}
\arguments{
\item{object}{A \code{frmtmb_fit}.}

\item{newdata}{Optional data frame to predict on. Defaults to the
training data.}

\item{re_formula}{\code{NULL} (default) keeps the random effects, so the
draw is conditional on the modes; \code{NA} or \code{~0} draws at the
population level.}

\item{transform}{A function applied to the draws before they are
summarized, as in brms.}

\item{resp}{For multivariate fits: which response, or \code{NULL}
(default) for all of them, which is brms's \verb{nrow x 4 x nresp}
array with the responses named on the third dimension.}

\item{negative_rt}{brms's sign convention for its \code{wiener} family.
Refused, with the reason: this package's evidence-accumulation
families return the response and the time as they declare them.}

\item{ndraws}{Number of simulated replicates. Defaults to 1000.
brms thins its posterior with this; here it sets how many draws
are taken, so a larger number narrows the Monte Carlo error of the
quantile columns and costs proportionally more.}

\item{draw_ids, sort, cores}{Refused by name. There are no stored
draws to index, rows always come back in the order of the data,
and the simulation is not parallelized.}

\item{ntrys}{Rejection-sampling attempts per row for a truncated
response, brms's spelling of the family simulator's \code{max_iter}.}

\item{summary}{If \code{FALSE}, the \verb{ndraws x nrow} matrix of simulated
draws instead of the summary.}

\item{robust}{If \code{TRUE}, the median and the median absolute
deviation of the draws instead of the mean and the standard
deviation. brms's argument, and it is answerable here because these
draws exist.}

\item{probs}{Probabilities of the quantile columns.}

\item{...}{Refused. An argument this method does not have is an
error naming it, and \code{type}, \code{se.fit}, \code{dpar} and \code{scale} are
refused with the name of the function that took over each one.}

\item{allow_new_levels}{Predict rows whose grouping-factor level the
fit never saw, instead of erroring. Each replicate draws that
level's effect from the block's estimated covariance.}

\item{sample_new_levels}{brms's argument. \code{"gaussian"}, which is
what happens, or \code{NULL}. \code{"uncertainty"} and \code{"old_levels"}
resample the posterior draws of the levels that were seen, and a
maximum-likelihood fit has none, so both are refused by name.}

\item{param_uncertainty}{If \code{FALSE}, simulate at the estimates alone
(the plug-in predictive distribution) instead of drawing the
parameters first.}
}
\value{
With \code{summary = TRUE} (the default) an \verb{nrow x 4} matrix with
the columns \code{Estimate}, \code{Est.Error} and one per entry of \code{probs};
for an ordinal or categorical response an \verb{nrow x K} matrix of
simulated category proportions; for a multivariate fit an
\verb{nrow x 4 x nresp} array with the responses named. With
\code{summary = FALSE} the \verb{ndraws x nrow} matrix of draws, or
\verb{ndraws x nrow x nresp} for a multivariate fit.
}
\description{
brms's \code{predict()}: a summary of the PREDICTIVE distribution of the
response, with observation noise included, in the columns \code{Estimate},
\code{Est.Error}, \code{Q2.5} and \code{Q97.5}. It is a much wider interval than
\code{\link[=fitted]{fitted()}}'s, which summarizes the expected response.

A maximum-likelihood fit has no posterior, so the draws are
simulated. Each replicate draws the outer parameter vector from its
asymptotic normal law, \code{N(theta_hat, vcov(object, full = TRUE))},
evaluates every distributional parameter there, and draws a response
from the family's own simulator, the one \code{\link[=simulate]{simulate()}} uses. The
interval is the empirical quantile of those draws, so it carries the
family's skew and its discreteness: a poisson predictive interval is
on the counts.
}
\section{Which uncertainty is in the interval}{

Two of the three sources.
\itemize{
\item Observation noise, from the family's simulator. This is almost
always the largest term.
\item Uncertainty in the estimates, from the asymptotic covariance of
the outer parameter vector. \code{param_uncertainty = FALSE} drops it
and simulates at the estimates alone.
\item Uncertainty in the random-effect modes of a level the fit SAW is
NOT included. The draw is conditional on them, the convention
every other method here follows. On a design with few
observations per grouping level that term is not small, and the
interval is then narrower than a fully marginal one.
\code{dev/shapes-findings.md} reports the measured coverage for a
design with and without a grouping factor.
}

A level the fit did NOT see is different: there is no mode to
condition on, so \code{allow_new_levels = TRUE} draws its effect from the
block's own estimated covariance, once per replicate, at that
replicate's parameters. This is brms's \code{sample_new_levels = "gaussian"} and it is the whole between-group variance, so the
interval at an unseen level is wider than at a known one.

A fit whose covariance could not be recovered from the Hessian has no
second term to draw: the simulation falls back to the estimates and
warns, rather than drawing from a matrix of \code{NaN}.

Under \code{REML = TRUE} (or \code{frmtmb_control(profile = TRUE)}) the fixed
effects are integrated out of the outer problem, so they are not in
\code{vcov(object, full = TRUE)} and are not drawn either. The interval
there carries the covariance parameters' uncertainty and the
observation noise, and holds the coefficients fixed.
}

\section{Ordinal and categorical responses}{

There is no mean to summarize, so \code{predict()} returns the simulated
proportion of each category, one column per category named
\code{P(Y = k)}, which is brms's shape. \code{\link[=fitted]{fitted()}} returns the MODELLED
category probabilities with their standard errors instead, which is
the smoother quantity.
}

\examples{
set.seed(1)
dd <- data.frame(x = rnorm(100))
dd$y <- rpois(100, exp(0.3 + 0.4 * dd$x))
fit <- frm(bf(y ~ x) + poisson(), data = dd)

# the predictive summary: observation noise is in it
head(predict(fit, ndraws = 200))
# against the expected response, which is much tighter
head(fitted(fit))

# the draws themselves
dim(predict(fit, summary = FALSE, ndraws = 50))

# the linear predictor is frm_linpred() now
head(frm_linpred(fit, type = "link"))
}
\seealso{
\code{\link[=fitted.frmtmb_fit]{fitted.frmtmb_fit()}} for the expected response,
\code{\link[=frm_linpred]{frm_linpred()}} for the linear predictor and the glmmTMB scale
vocabulary, \code{\link[=simulate.frmtmb_fit]{simulate.frmtmb_fit()}} for the draws themselves, and
\link{frmtmb-scales}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autocor.R
\name{autocor_matrix}
\alias{autocor_matrix}
\title{Estimated within-group residual correlation matrix}
\usage{
autocor_matrix(fit, resp = NULL)
}
\arguments{
\item{fit}{A \code{frmtmb_fit}.}

\item{resp}{Response name, for a multivariate fit.}
}
\value{
A correlation matrix with the time levels as dimnames, or
\code{NULL} when the fit has no residual correlation term.
}
\description{
The correlation matrix \code{R} of the \code{ar()}, \code{ma()}, \code{arma()}, \code{cosy()}
or \code{unstr()} term of a fit, over the model's time levels. The
residual covariance of a group is \verb{D R D} restricted to the time
points that group has, with \code{D} the diagonal matrix of that group's
\code{sigma} values, so \code{R} plus \code{\link[=sigma]{sigma()}} (or \code{predict(dpar = "sigma")})
describes the whole residual structure.
}
\examples{
set.seed(1)
d <- expand.grid(week = 1:5, subj = factor(1:25))
e <- as.vector(apply(matrix(rnorm(125), 5, 25), 2, function(z) {
  as.vector(stats::filter(z, 0.6, "recursive"))
}))
d$x <- rnorm(125)
d$y <- 1 + 0.5 * d$x + e
fit <- frm(bf(y ~ x + ar(week, subj, cov = TRUE)) + gaussian(),
           data = d)
autocor_matrix(fit)
}
\seealso{
\code{\link[=rescor_matrix]{rescor_matrix()}} for the ACROSS-response residual
correlation of a \code{rescor = TRUE} fit, which is a different
structure and cannot be combined with this one.
}

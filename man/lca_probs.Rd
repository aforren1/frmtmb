% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lca.R
\name{lca_probs}
\alias{lca_probs}
\title{Posterior class membership of an \code{lca()} fit}
\usage{
lca_probs(fit)
}
\arguments{
\item{fit}{A \code{frmtmb_fit} with an \code{\link[=lca]{lca()}} family.}
}
\value{
A \verb{n x K} matrix of posterior class probabilities with an
\code{entropy} attribute.
}
\description{
One row per subject, one column per latent class, rows summing to
one: the probability that a subject belongs to each class given its
observed item responses and its gating covariates. This is poLCA's
\verb{$posterior}.
}
\details{
The relative entropy of the classification is attached as the
\code{entropy} attribute: \verb{1 - sum(-p log p) / (n log K)}, which is 1
for a partition with no ambiguity and 0 when every subject is
equally likely to be in any class. Values above about 0.8 are the
usual rule of thumb for classes worth naming.

This is \code{\link[=mixture_probs]{mixture_probs()}} under an LCA-specific name and check; the
two return the same matrix for an \code{\link[=lca]{lca()}} fit.
}
\examples{
set.seed(3)
n <- 200
cl <- rbinom(n, 1, 0.5) + 1
pr <- rbind(c(0.9, 0.85, 0.8, 0.9), c(0.1, 0.15, 0.2, 0.1))
Y <- matrix(0L, n, 4)
for (j in 1:4) Y[, j] <- 1L + rbinom(n, 1, pr[cl, j])
dd <- data.frame(row = seq_len(n))
dd$Y <- Y
fit <- frm(bf(Y ~ 1), family = lca(K = 2), data = dd)

p <- lca_probs(fit)
head(p)
attr(p, "entropy")            # classification quality, 0 to 1
table(max.col(p), truth = cl) # the modal assignment, up to relabeling
}
\seealso{
\code{\link[=lca]{lca()}}, \code{\link[=lca_profiles]{lca_profiles()}}, \code{\link[=mixture_probs]{mixture_probs()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-fit.R
\name{fixef_by_dpar}
\alias{fixef_by_dpar}
\title{Fixed effects per linear predictor}
\usage{
fixef_by_dpar(object)
}
\arguments{
\item{object}{A \code{frmtmb_fit}.}
}
\value{
A named list of coefficient vectors, one per linear
predictor.
}
\description{
The coefficients of each linear predictor, as a named list keyed by
distributional parameter (and by response on a multivariate fit),
each entry named by DESIGN COLUMN: \code{fixef_by_dpar(fit)$sigma},
\code{fixef_by_dpar(fit)$mu[["(Intercept)"]]}.
}
\details{
This is the shape \code{\link[=fixef]{fixef()}} returned before frmtmb took brms's
summary matrix. It is kept because nothing else has it: \code{fixef()}
names its rows as brms does and flattens every predictor into one
table, and \code{fixef(flatten = TRUE)} is one vector in the \code{\link[=confint]{confint()}}
spelling. A model with several linear predictors (a mixture, a
multivariate response, a nonlinear body) is the case this reads
cleanly.

It carries ESTIMATES only. For a standard error or an interval use
\code{\link[=fixef]{fixef()}}, whose rows are the same coefficients under brms's names.
}
\examples{
set.seed(1)
dd <- data.frame(x = rnorm(100))
dd$y <- rnorm(100, 1 + 0.5 * dd$x, exp(0.2 + 0.1 * dd$x))
fit <- frm(bf(y ~ x, sigma ~ x) + gaussian(), data = dd)

# one entry per distributional parameter, named by design column
fixef_by_dpar(fit)
exp(fixef_by_dpar(fit)$sigma[["(Intercept)"]])

# the same coefficients under brms's names, with their errors
fixef(fit)
}
\seealso{
\code{\link[=fixef]{fixef()}}, \code{\link[=coef.frmtmb_fit]{coef.frmtmb_fit()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat.R
\name{frm_compat}
\alias{frm_compat}
\title{Query the feature compatibility registry}
\usage{
frm_compat(feature_a = NULL, feature_b = NULL, status = NULL)
}
\arguments{
\item{feature_a, feature_b}{Feature names, as given by
\code{\link[=frm_compat_features]{frm_compat_features()}}. Supply both for one pair, one for every
pair involving that feature, or neither for the whole table. Both
accept a vector, which gives every pair in the cross of the two
sides; an empty vector is an error rather than an empty answer.}

\item{status}{Optional character vector; keep only these statuses.}
}
\value{
A data frame with columns \code{feature_a}, \code{kind_a},
\code{feature_b}, \code{kind_b}, \code{status}, and \code{note}. Family pairs are
omitted, because a model carries one family. When the session
holds an unresolved rule side, the frame also carries an
\code{unresolved} attribute and the class \code{frmtmb_compat}, whose print
method names them; see the section below.
}
\description{
Gives the declared status of a pair of package features. The status
is one of:
}
\details{
\describe{
\item{\code{works}}{The combination is supported and exercised.}
\item{\code{conditional}}{Supported, but the note states a condition
that the combination must meet.}
\item{\code{refused}}{\code{\link[=frm]{frm()}} or the post-fit method stops with an
error. The refusal is deliberate.}
\item{\code{broken}}{The combination is accepted but the result is
wrong, or it fails with an error that does not explain itself.
Avoid the pair. The note gives the evidence.}
\item{\code{untested}}{Nothing checks this pair. It may work. Treat a
silent success as unverified, not as support.}
}

The last status is the point of the registry. A guard that does not
exist looks exactly like a guard that passed, so the absence of an
error was never evidence of support.
}
\section{Rule sides this session cannot resolve}{

A rule may name a feature another package owns, declared through
\code{frmtmb_register_compat(expects =)}. Until that package is loaded
there is no such feature and so no pair, and the rule matches
nothing. Such a side cannot honestly be a row here, because a row
would put a feature in the table that nothing in this session
implements; it is reported instead. The returned table carries the
names in an \code{unresolved} attribute and the print method names them
under the rows. Both appear only when the session holds one, so a
session with no forward reference gets the plain data frame it
always got. Load the package that owns the feature, or read the
names as the list of rules that are waiting for one.
}

\examples{
# one pair
frm_compat("rescor", "cens()")

# everything known about truncation
frm_compat("trunc()", status = c("refused", "broken"))

# the pairs to avoid
frm_compat(status = "broken")[, 1:5]
}
\seealso{
\code{\link[=frm_compat_rules]{frm_compat_rules()}}, \code{\link[=frm_compat_features]{frm_compat_features()}}
}

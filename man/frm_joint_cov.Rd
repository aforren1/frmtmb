% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{frm_joint_cov}
\alias{frm_joint_cov}
\title{The joint covariance of the fixed and random coefficients}
\usage{
frm_joint_cov(object)
}
\arguments{
\item{object}{A fitted \code{frmtmb_fit}.}
}
\value{
A list with
\describe{
\item{\code{V}}{the \verb{p x p} joint covariance.}
\item{\code{names}}{length \code{p}; the PARAMETER COMPONENT each row belongs
to (\code{"beta"}, \code{"betad"}, \code{"b"}, \code{"theta"}, ...), which is what
\code{\link[=frm_lp_basis]{frm_lp_basis()}}\verb{$coef_pos} indexes.}
\item{\code{labels}}{length \code{p}; one label per row,
\verb{beta.<coefficient>} for a fixed effect and \verb{b.<block>.<level>}
for a random one.}
}
A fit with no random effects has no joint precision, and \code{V} is then
the fixed-effect covariance \code{sdreport()$cov.fixed}; \code{names} says so.
}
\description{
The covariance of everything the fit estimates, \code{beta}, \code{betad},
\code{theta} and the random-effect coefficients \code{b} together, in one
matrix. It is what a delta method over a fitted CURVE needs and what
\code{\link[=vcov]{vcov()}} cannot return: a penalized smooth's wiggly part is a
random-effect block even when the smooth is a population term, so a
covariance that stops at the fixed effects covers none of it.
}
\details{
\code{vcov(full = TRUE)} returns the OUTER parameter vector's covariance
and its row names are documented to be exactly \code{\link[=confint]{confint()}}'s, so \code{b}
is not in it and cannot be added. \code{frm_lp_basis()} is the accessor
for a design at \code{newdata}; this is the accessor for the covariance
those coefficients have.

The result is memoized on the fit, so the joint-precision solve is
paid once however many curves are drawn from it. It is also the ONLY
route to the covariance of an AUTOSCALED fit
(\code{frmtmb_control(autoscale = TRUE)}): a fresh
\code{RTMB::sdreport(getJointPrecision = TRUE)} goes round the
reparameterization and returns a covariance built on the unscaled
Hessian, which is a different matrix and is not marked as one.
}
\examples{
set.seed(1)
dd <- data.frame(x = rnorm(120), g = factor(rep(1:12, each = 10)))
dd$y <- rnorm(120, 1 + 2 * dd$x + rnorm(12, 0, 0.5)[dd$g], 0.4)
fit <- frm(bf(y ~ x + (1 | g)), data = dd)
jc <- frm_joint_cov(fit)
dim(jc$V)
table(jc$names)
head(jc$labels)
}
\seealso{
\code{\link[=frm_lp_basis]{frm_lp_basis()}}, \code{\link[=vcov]{vcov()}}, \link{frmtmb-extension-api}
}

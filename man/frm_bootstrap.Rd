% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{frm_bootstrap}
\alias{frm_bootstrap}
\title{Parametric bootstrap}
\usage{
frm_bootstrap(
  fit,
  FUN = function(f) unlist(fixef(f)),
  nsim = 500,
  seed = NULL,
  re.form = NA
)
}
\arguments{
\item{fit}{A \code{frmtmb_fit} for a univariate model.}

\item{FUN}{Function of a \code{frmtmb_fit} returning a numeric vector.
Default: the flattened fixed effects.}

\item{nsim}{Number of bootstrap draws.}

\item{seed}{Optional seed.}

\item{re.form}{Passed to \code{\link[=simulate]{simulate()}}; the default \code{NA} simulates
marginally (new random effects), which is the standard parametric
bootstrap for mixed models.}
}
\value{
A \code{frmtmb_boot} object: \code{t0} (FUN at the original fit), \code{t}
(\code{nsim} x \code{length(t0)} matrix), and \code{converged}. \code{confint()} gives
percentile intervals.
}
\description{
Simulates \code{nsim} response vectors from the fitted model (by default
with new random effects each draw), refits the model to each through
\code{\link[=refit]{refit()}} (warm-started, no re-parsing), and collects \code{FUN} of every
refit. Draws whose refit fails are kept as \code{NA} rows; draws whose
optimizer does not report convergence are kept but flagged.
}
\details{
There is no standard \code{bootstrap} generic to implement
(\code{boot::boot} and \code{lme4::bootMer} are plain functions), hence the
\code{frm_} prefix.
}
\examples{
set.seed(3)
dd <- data.frame(x = rnorm(80), g = factor(rep(1:8, 10)))
dd$y <- rnorm(80, 1 + 0.5 * dd$x + rnorm(8, 0, 0.5)[dd$g], 1)
fit <- frm(bf(y ~ x + (1 | g)) + gaussian(), data = dd)
bs <- frm_bootstrap(fit, nsim = 20, seed = 1)
bs
confint(bs)
}

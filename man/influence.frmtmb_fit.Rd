% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/influence.R
\name{influence.frmtmb_fit}
\alias{influence.frmtmb_fit}
\title{Influence measures by case deletion}
\usage{
\method{influence}{frmtmb_fit}(model, groups = NULL, data = NULL, force = FALSE, ...)
}
\arguments{
\item{model}{A \code{frmtmb_fit}.}

\item{groups}{Name of a random-effect grouping factor (see
\code{\link[=ngrps]{ngrps()}}) to delete level-wise; \code{NULL} deletes single
observations (refuses for large n unless \code{force = TRUE}).}

\item{data}{The original model data; defaults to the stored model
frame, which works unless the formula uses variables that are not
stored raw (e.g. inside \code{poly()}).}

\item{force}{Allow observation-wise deletion for n > 500.}

\item{...}{Unused.}
}
\value{
A \code{frmtmb_influence} object: \code{fixed} and \code{theta} matrices
(one row per deleted unit) plus the full-data reference.
}
\description{
Refits the model with one group (or observation) left out at a time,
warm-started at the full-data estimates, and collects the
fixed-effect and covariance-parameter changes. \code{\link[=cooks.distance]{cooks.distance()}} on
the result gives the scaled fixed-effect displacement.
}
\examples{
\donttest{
set.seed(7)
dd <- data.frame(x = rnorm(60), g = factor(rep(1:6, 10)))
dd$y <- rnorm(60, 1 + 0.5 * dd$x + rnorm(6, 0, 0.5)[dd$g], 1)
fit <- frm(bf(y ~ x + (1 | g)) + gaussian(), data = dd)
infl <- influence(fit, groups = "g")
cooks.distance(infl)
}
}

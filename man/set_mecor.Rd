% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/me.R
\name{set_mecor}
\alias{set_mecor}
\title{Control the correlation of noise-free latent variables}
\usage{
set_mecor(mecor = TRUE)
}
\arguments{
\item{mecor}{\code{TRUE} (the default) to estimate the correlation between
the latent values of the \code{me()} terms that share a grouping, \code{FALSE}
to fix it at zero.}
}
\value{
An object of class \code{frmtmb_mecor}, which \code{+} adds to a
formula built with \code{\link[=bf]{bf()}} or \code{\link[=mvbf]{mvbf()}}.
}
\description{
Several \link[=frmtmb-me]{me()} terms model their latent (noise-free)
values as correlated by default, which is brms's default too. Add
\code{set_mecor(FALSE)} to a formula to model them as independent.
}
\examples{
set.seed(3)
n <- 120
tx <- rnorm(n)
tz <- 0.6 * tx + rnorm(n, 0, 0.8)
d <- data.frame(x = tx + rnorm(n, 0, 0.3), z = tz + rnorm(n, 0, 0.3),
                sx = 0.3, sz = 0.3)
d$y <- 1 + 0.5 * tx - 0.4 * tz + rnorm(n, 0, 0.5)

# independent latent values
f0 <- bf(y ~ me(x, sx) + me(z, sz)) + set_mecor(FALSE)
fit0 <- frm(f0, data = d)
}
\seealso{
\link{frmtmb-me} for the model that \code{me()} specifies.
}

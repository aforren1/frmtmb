% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat.R
\name{frm_compat_features}
\alias{frm_compat_features}
\title{Feature metadata for the compatibility registry}
\usage{
frm_compat_features()
}
\value{
A data frame with columns \code{name}, \code{key}, and \code{kind}.
}
\description{
The vocabulary the compatibility registry talks about: every family,
addition term, covariance structure, predictor special, estimation
mode, model structure, and post-fit method that has a declared
compatibility status.
}
\details{
\code{name} is how the feature is written in a formula or a call. \code{key}
is the identifier the package uses internally, which is what lets
the tests check the registry against \code{\link[=frm]{frm()}}'s real vocabulary.
Three specials share a name with a covariance structure, so they
carry the display names \code{mi_pred()}, \code{gp_pred()}, and \code{cs_pred()}.
}
\examples{
head(frm_compat_features())
table(frm_compat_features()$kind)
}
\seealso{
\code{\link[=frm_compat]{frm_compat()}}, \code{\link[=frm_compat_rules]{frm_compat_rules()}}
}

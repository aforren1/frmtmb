# Estimated residual correlation matrix (rescor fits), else NULL

Estimated residual correlation matrix (rescor fits), else NULL

## Usage

``` r
rescor_matrix(fit)
```

## Arguments

- fit:

  A `frmtmb_fit`.

## Value

A correlation matrix or `NULL`.

# Extract fixed effects

Extract fixed effects

## Usage

``` r
fixef(object, ...)

# S3 method for class 'frmtmb_fit'
fixef(object, ...)
```

## Arguments

- object:

  A `frmtmb_fit`.

- ...:

  Unused.

## Value

A named list of coefficient vectors, one per dpar.

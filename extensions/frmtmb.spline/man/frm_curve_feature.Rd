% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve-feature.R
\name{frm_curve_feature}
\alias{frm_curve_feature}
\title{Features of a fitted curve: a peak, a trough, or a level crossing}
\usage{
frm_curve_feature(
  object,
  var,
  type = c("maximum", "minimum", "extremum", "crossing"),
  at = 0,
  newdata = NULL,
  dpar = NULL,
  resp = NULL,
  re.form = NA,
  level = 0.95,
  eps = NULL,
  maxit = 50L,
  tol = 1e-06
)
}
\arguments{
\item{object}{A \code{frmtmb_fit}, or a \code{frmtmb_curve} from \code{\link[=frm_curve]{frm_curve()}}.}

\item{var}{Name of the covariate the feature is located along.}

\item{type}{\code{"maximum"}, \code{"minimum"} or \code{"extremum"} for a stationary
point of the given kind; \code{"crossing"} for the points where the curve
passes \code{at}.}

\item{at}{The level to cross, for \code{type = "crossing"}.}

\item{newdata}{The grid the search scans, and the values every other
covariate is held at. Required when \code{object} is a fit.}

\item{dpar}{Distributional parameter to read the curve off. \code{NULL},
the default, is the location parameter \code{mu}.}

\item{resp}{Response name, for a multivariate fit.}

\item{re.form}{\code{NA} (the default) evaluates the population curve, the
convention \code{mgcv} and \code{gratia} plot. \code{NULL} keeps every random
effect, so the grid must carry the grouping columns and the curve is
that group's own.}

\item{level}{Coverage of both intervals.}

\item{eps}{Step size for the differences. \code{NULL} is the measured
default of \code{\link[=frm_curve_deriv]{frm_curve_deriv()}}.}

\item{maxit}{Newton iterations allowed per root.}

\item{tol}{Largest relative disagreement with \code{predict(se.fit = TRUE)}
the assembled covariance may show before the call refuses.}
}
\value{
A data frame with one row per root: \code{.feature}, \code{.var},
\code{.estimate} (the located position), \code{.se}, \code{.lower_ci}, \code{.upper_ci},
\code{.value} (the curve there) and \code{.value_se}. The \code{"check"} attribute
carries the covariance agreement, as \code{\link[=frm_curve]{frm_curve()}}'s does.
}
\description{
Locates a stationary point or a level crossing of a fitted curve and
gives its position a standard error, by the implicit-function delta
method.
}
\details{
The search is a scan of the grid for a sign change, then Newton
refinement on the fitted curve, then one design pass at the located
root for the variance. Every root the grid brackets is returned, so a
curve with two peaks gives two rows; a curve with none gives a
zero-row result rather than an error, because "this curve does not
peak in this window" is an answer.

At a stationary point the curve's own value has a delta-method
simplification worth knowing: the derivative of \verb{f(t*)} with respect
to the coefficients is \verb{df/dc + f'(t*) dt*/dc}, and \verb{f'(t*)} is zero
there, so the standard error of the PEAK HEIGHT is just the pointwise
standard error of the curve at \verb{t*}. It is reported as \code{.value_se},
and it is not inflated by the uncertainty in the peak's location.
}
\examples{
set.seed(1)
dd <- data.frame(x = sort(runif(300)))
dd$y <- 2 * sin(pi * dd$x) + rnorm(300, 0, 0.3)
fit <- frmtmb::frm(frmtmb::bf(y ~ s(x, k = 8)),
                   family = stats::gaussian(), data = dd)
# 2 sin(pi x) peaks at x = 0.5
frm_curve_feature(fit, var = "x", type = "maximum",
                  newdata = data.frame(x = seq(0.05, 0.95, length.out = 41)))
}
\seealso{
\code{\link[=frm_curve]{frm_curve()}}, \code{\link[=frm_curve_deriv]{frm_curve_deriv()}}
}

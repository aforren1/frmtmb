% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-draws.R
\name{sample-posterior_summary}
\alias{sample-posterior_summary}
\alias{posterior_summary.frmtmb_draws}
\alias{posterior_interval}
\alias{posterior_interval.frmtmb_draws}
\alias{predictive_interval}
\alias{predictive_interval.frmtmb_draws}
\alias{predictive_error}
\alias{predictive_error.frmtmb_draws}
\title{Summaries and intervals of draws}
\usage{
\method{posterior_summary}{frmtmb_draws}(
  x,
  probs = c(0.025, 0.975),
  robust = FALSE,
  variable = NULL,
  ...
)

posterior_interval(object, ...)

\method{posterior_interval}{frmtmb_draws}(
  object,
  pars = NA,
  variable = NULL,
  prob = 0.95,
  regex = FALSE,
  fixed = FALSE,
  ...
)

predictive_interval(object, ...)

\method{predictive_interval}{frmtmb_draws}(
  object,
  prob = 0.9,
  newdata = NULL,
  resp = NULL,
  re_formula = arg_unset(),
  re.form = arg_unset(),
  ndraws = NULL,
  ...
)

predictive_error(object, ...)

\method{predictive_error}{frmtmb_draws}(
  object,
  newdata = NULL,
  re_formula = arg_unset(),
  re.form = arg_unset(),
  method = "posterior_predict",
  resp = NULL,
  ndraws = NULL,
  draw_ids = NULL,
  ...
)
}
\arguments{
\item{x}{The same, for \code{posterior_summary()}, whose generic
is brms's and names its first argument \code{x}.}

\item{probs}{Quantiles for \code{posterior_summary()}.}

\item{robust}{If \code{TRUE}, median and MAD instead of mean and SD.}

\item{variable}{Optional subset of variables, by name.}

\item{...}{Unused.}

\item{object}{A \code{frmtmb_draws}, or a matrix of draws
(variables in columns).}

\item{pars}{brms's alias of \code{variable}, in brms's own second
position on \code{posterior_interval()}: \code{NA} (the default) for every
variable, otherwise a character vector matched as a regular
expression unless \code{fixed = TRUE}. brms refuses a \code{pars} that is
neither \code{NA} nor character, and so does this, which is why
\code{posterior_interval(x, 0.9)} is a refusal and not an interval.}

\item{prob}{Central interval width for \code{posterior_interval()} and
\code{predictive_interval()}.}

\item{regex}{If \code{TRUE}, \code{variable} is a regular expression.}

\item{fixed}{If \code{TRUE}, \code{pars} is matched by exact name.}

\item{re_formula, re.form}{Passed to \code{\link[=posterior_predict]{posterior_predict()}}, which
takes brms's \code{re_formula} and accepts lme4's \code{re.form} as an alias
of it. Pass one or the other; see the \emph{Argument spellings} section
of \code{\link[=posterior_epred]{posterior_epred()}}.}

\item{ndraws, draw_ids, newdata, resp}{Passed to
\code{\link[=posterior_predict]{posterior_predict()}}. \code{predictive_error(newdata =)} re-evaluates
the response term on \code{newdata}, so \code{newdata} must carry the
response.}

\item{method}{For \code{predictive_error()}, which predictive draws the
error is taken against: \code{"posterior_predict"} (the default) or
\code{"posterior_epred"}.}
}
\value{
A matrix with one row per variable (or per observation, for
the predictive functions), except \code{predictive_error()}, which
returns a draws-by-observations matrix.
}
\description{
\code{posterior_summary()} reduces draws to estimate, error and quantiles
in brms's column layout (\code{Estimate}, \code{Est.Error}, \code{Q2.5}, \code{Q97.5});
\code{posterior_interval()} gives the central interval alone, in
rstantools' layout. Both work on a \code{frmtmb_draws} object and on any
matrix of draws, which is what makes
\code{posterior_summary(bayes_R2(ds, summary = FALSE))} work.
}
\details{
\code{predictive_interval()} is the same central interval of
\code{\link[=posterior_predict]{posterior_predict()}} draws, and \code{predictive_error()} is the matrix
of predictive residuals \code{y - yrep}, one row per draw.
}
\examples{
\donttest{
if (requireNamespace("tmbstan", quietly = TRUE) &&
    requireNamespace("rstan", quietly = TRUE) &&
    !frmtmb.sample:::tmbstan_build_broken()) {
  set.seed(9)
  dd <- data.frame(x = rnorm(60), g = factor(rep(1:6, 10)))
  dd$y <- rnorm(60, 1 + 0.5 * dd$x + rnorm(6, 0, 0.5)[dd$g], 1)
  ds <- frm_sample(bf(y ~ x + (1 | g)), family = gaussian(),
                   data = dd, chains = 1, iter = 500, refresh = 0)
  posterior_summary(ds, variable = c("Intercept", "x"))
  posterior_interval(ds, prob = 0.9, variable = "x")
  head(predictive_interval(ds))
}
}
}

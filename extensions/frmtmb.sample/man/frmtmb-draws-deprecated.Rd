% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-draws.R
\name{frmtmb-draws-deprecated}
\alias{frmtmb-draws-deprecated}
\alias{posterior_samples}
\alias{posterior_samples.frmtmb_draws}
\alias{nsamples}
\alias{nsamples.frmtmb_draws}
\alias{parnames}
\alias{parnames.frmtmb_draws}
\title{Deprecated brms draws accessors}
\usage{
posterior_samples(x, pars = NA, ...)

\method{posterior_samples}{frmtmb_draws}(
  x,
  pars = NA,
  fixed = FALSE,
  add_chain = FALSE,
  subset = NULL,
  as.matrix = FALSE,
  as.array = FALSE,
  ...
)

nsamples(object, ...)

\method{nsamples}{frmtmb_draws}(object, subset = NULL, incl_warmup = FALSE, ...)

parnames(x, ...)

\method{parnames}{frmtmb_draws}(x, ...)
}
\arguments{
\item{x, object}{A \code{frmtmb_draws}.}

\item{pars}{Variable names. A regular expression unless
\code{fixed = TRUE}; \code{NA} (the default) takes all of them.}

\item{...}{Refused: an argument the method does not have is an error
naming it.}

\item{fixed}{If \code{TRUE}, \code{pars} names variables exactly.}

\item{add_chain}{If \code{TRUE}, add the \code{chain} and \code{iter} columns brms
adds.}

\item{subset}{Draw indices to keep.}

\item{as.matrix, as.array}{Return a matrix or a
draws-by-chains-by-variables array instead of a data frame.}

\item{incl_warmup}{Refused: \code{frm_sample()} discards the warmup, so
there is no warmup draw to count.}
}
\value{
A data frame (or matrix, or array) of draws for
\code{posterior_samples()}, one integer for \code{nsamples()}, and a
character vector for \code{parnames()}.
}
\description{
\code{posterior_samples()}, \code{nsamples()} and \code{parnames()} are brms
spellings that brms itself deprecated and still answers. Each warns
and then does what brms's does, so a ported script runs.
\itemize{
\item \code{posterior_samples(x)} is \code{as.data.frame(x)}; \code{pars} is a regular
expression unless \code{fixed = TRUE}, as in brms.
\item \code{nsamples(x)} is \code{\link[=ndraws]{ndraws()}}.
\item \code{parnames(x)} is \code{\link[=variables]{variables()}}.
}

Use \code{as_draws(x)} for a posterior draws object, \code{as.data.frame(x)},
\code{\link[=ndraws]{ndraws()}} and \code{\link[=variables]{variables()}} in new code.
}

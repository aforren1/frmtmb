% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-draws.R
\name{fixef.frmtmb_draws}
\alias{fixef.frmtmb_draws}
\alias{VarCorr.frmtmb_draws}
\alias{ranef.frmtmb_draws}
\alias{draws-structure}
\alias{nobs.frmtmb_draws}
\alias{formula.frmtmb_draws}
\alias{family.frmtmb_draws}
\alias{getCall.frmtmb_draws}
\alias{coef.frmtmb_draws}
\title{Model structure behind a set of draws}
\usage{
\method{fixef}{frmtmb_draws}(
  object,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975),
  pars = NULL,
  ...
)

\method{VarCorr}{frmtmb_draws}(
  x,
  sigma = 1,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975),
  ...
)

\method{ranef}{frmtmb_draws}(
  object,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975),
  pars = NULL,
  groups = NULL,
  ...
)

\method{nobs}{frmtmb_draws}(object, ...)

\method{formula}{frmtmb_draws}(x, ...)

\method{family}{frmtmb_draws}(object, ...)

\method{getCall}{frmtmb_draws}(x, ...)

\method{coef}{frmtmb_draws}(object, summary = TRUE, robust = FALSE, probs = c(0.025, 0.975), ...)
}
\arguments{
\item{object, x}{A \code{frmtmb_draws} from \code{\link[=frm_sample]{frm_sample()}}.}

\item{summary}{If \code{TRUE} (brms's default), summaries; otherwise the
draws, as above.}

\item{robust}{If \code{TRUE}, median and MAD instead of mean and SD.}

\item{probs}{The quantiles to report.}

\item{pars}{For \code{fixef()} and \code{ranef()}, the coefficients to keep, by
name without the \code{b_} prefix, as in brms.}

\item{...}{Refused: an argument the method does not have is an
error naming it, rather than silently changing nothing.}

\item{sigma}{Ignored, as in brms.}

\item{groups}{For \code{ranef()}, the grouping factors to keep.}
}
\value{
As for the corresponding \code{frmtmb_fit} method.
}
\description{
\code{nobs()}, \code{formula()}, \code{family()}, \code{getCall()} and \code{ngrps()} on a
\code{frmtmb_draws} report the model the sampler ran, by delegating to the
fit stored inside it. They read structure only, so they work on draws
from the formula route, which has no maximum-likelihood estimate.
}
\details{
\code{coef()}, \code{fixef()}, \code{ranef()} and \code{VarCorr()} are posterior
quantities, not structural ones, and they are brms's methods on these
draws, compared \code{identical()} against brms's own installed methods in
\code{dev/brmsnames-findings.md}. \code{fixef()} is a coefficients x statistics
matrix; \code{ranef()} and \code{coef()} are a list keyed by grouping factor of
\verb{levels x statistics x coefficients} arrays, and \code{coef()} broadcasts
every population-level coefficient over the levels, as brms's does;
\code{VarCorr()} is a list keyed by grouping factor with \code{sd}, and \code{cor} and
\code{cov} when the group has correlations, then \code{residual__}. With
\code{summary = FALSE} each returns brms's raw draws instead: a draws x
coefficients matrix, a \verb{draws x levels x coefficients} array, or a
\verb{draws x coefficients x coefficients} array.

\code{VarCorr()}'s standard deviations and correlations are computed per
draw from the sampled covariance parameters, because the sampler
stores \code{theta} and not brms's \code{sd_} and \code{cor_} draws.
}
\examples{
\donttest{
if (requireNamespace("tmbstan", quietly = TRUE) &&
    requireNamespace("rstan", quietly = TRUE) &&
    !frmtmb.sample:::tmbstan_build_broken()) {
  set.seed(9)
  dd <- data.frame(x = rnorm(60), g = factor(rep(1:6, 10)))
  dd$y <- rnorm(60, 1 + 0.5 * dd$x + rnorm(6, 0, 0.5)[dd$g], 1)
  ds <- frm_sample(bf(y ~ x + (1 | g)), family = gaussian(),
                   data = dd, chains = 1, iter = 500, refresh = 0)
  nobs(ds)
  ngrps(ds)
  coef(ds)$g[1:3, , "Intercept"]
  dim(ranef(ds, summary = FALSE)$g)
  VarCorr(ds)$g$sd
}
}
}

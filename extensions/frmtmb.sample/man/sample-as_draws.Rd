% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-draws.R, R/sample.R
\name{sample-as_draws}
\alias{sample-as_draws}
\alias{as_draws.frmtmb_draws}
\alias{as.data.frame.frmtmb_draws}
\alias{as.array.frmtmb_draws}
\alias{as_draws_matrix.frmtmb_draws}
\alias{as_draws_array.frmtmb_draws}
\alias{as_draws_df.frmtmb_draws}
\alias{as_draws_list.frmtmb_draws}
\alias{as_draws_rvars.frmtmb_draws}
\alias{as.mcmc}
\alias{as.mcmc.frmtmb_draws}
\alias{as.matrix.frmtmb_draws}
\title{Convert draws to a posterior draws object}
\usage{
\method{as_draws}{frmtmb_draws}(x, variable = NULL, regex = FALSE, inc_warmup = FALSE, ...)

\method{as.data.frame}{frmtmb_draws}(
  x,
  row.names = NULL,
  optional = TRUE,
  pars = NA,
  variable = NULL,
  draw = NULL,
  subset = NULL,
  ...
)

\method{as.array}{frmtmb_draws}(x, pars = NA, variable = NULL, draw = NULL, subset = NULL, ...)

\method{as_draws_matrix}{frmtmb_draws}(x, variable = NULL, regex = FALSE, inc_warmup = FALSE, ...)

\method{as_draws_array}{frmtmb_draws}(x, variable = NULL, regex = FALSE, inc_warmup = FALSE, ...)

\method{as_draws_df}{frmtmb_draws}(x, variable = NULL, regex = FALSE, inc_warmup = FALSE, ...)

\method{as_draws_list}{frmtmb_draws}(x, variable = NULL, regex = FALSE, inc_warmup = FALSE, ...)

\method{as_draws_rvars}{frmtmb_draws}(x, variable = NULL, regex = FALSE, inc_warmup = FALSE, ...)

as.mcmc(x, ...)

\method{as.mcmc}{frmtmb_draws}(
  x,
  pars = NA,
  fixed = FALSE,
  combine_chains = FALSE,
  inc_warmup = FALSE,
  ...
)

\method{as.matrix}{frmtmb_draws}(x, pars = NA, variable = NULL, draw = NULL, subset = NULL, ...)
}
\arguments{
\item{x}{A \code{frmtmb_draws} object.}

\item{variable}{Variables to keep, by exact name unless \code{regex}.}

\item{regex}{If \code{TRUE}, \code{variable} is a regular expression.}

\item{inc_warmup}{Accepted for brms's signature and only \code{FALSE} is
supported: a \code{frmtmb_draws} keeps the post-warmup draws alone.}

\item{...}{For \code{as.matrix()}, \code{as.array()} and \code{as.data.frame()}, the
\code{regex}, \code{fixed} and \code{inc_warmup} brms passes on; anything else is
refused by name, rather than silently changing nothing.}

\item{row.names, optional}{Accepted for the generic and unused, as in
brms.}

\item{pars}{Variables to keep, in brms's spelling: \code{NA} (the
default) for all of them, otherwise a character vector matched as
a regular expression unless \code{fixed = TRUE}. The argument sits in
brms's own second position, so \code{as.mcmc(x, TRUE)} is refused here
exactly as brms refuses it.}

\item{draw}{Draws to keep, by index.}

\item{subset}{brms's deprecated alias of \code{draw}; it warns.}

\item{fixed}{If \code{TRUE}, \code{pars} is matched by exact name.}

\item{combine_chains}{If \code{TRUE}, one \code{mcmc} object over the pooled
draws; otherwise an \code{mcmc.list} with one component per chain, which
is what coda's diagnostics (\code{gelman.diag()}) need.}
}
\value{
A \code{posterior::draws_matrix}: one column per sampled variable
and one row per draw.
}
\description{
brms's converters, with brms's arguments and brms's output. The
\verb{as_draws_*()} family takes \code{variable} (exact names unless \code{regex})
and returns a posterior draws object; \code{as_draws()} is brms's
\code{as_draws_list()}. \code{as.matrix()}, \code{as.array()} and \code{as.data.frame()}
return brms's unclassed objects, with brms's deprecated \code{pars} and
\code{subset} accepted under brms's own warning.
}
\examples{
\donttest{
if (requireNamespace("posterior", quietly = TRUE) &&
    requireNamespace("tmbstan", quietly = TRUE) &&
    requireNamespace("rstan", quietly = TRUE) &&
    !frmtmb.sample:::tmbstan_build_broken()) {
  set.seed(9)
  dd <- data.frame(x = rnorm(80), g = factor(rep(1:8, 10)))
  dd$y <- rnorm(80, 1 + 0.5 * dd$x + rnorm(8, 0, 0.5)[dd$g], 1)
  fit <- frm(bf(y ~ x + (1 | g)) + gaussian(), data = dd)
  ds <- frm_sample(fit, chains = 1, iter = 500, refresh = 0)

  # hands the draws to the posterior package, keeping the frmtmb
  # parameter names
  dm <- as_draws(ds)
  posterior::summarise_draws(dm)
  # which is what variables() lists
  head(variables(ds))
}
}
}

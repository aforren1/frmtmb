% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-draws.R
\name{frmtmb-draws-summaries}
\alias{frmtmb-draws-summaries}
\alias{fitted.frmtmb_draws}
\alias{predict.frmtmb_draws}
\alias{residuals.frmtmb_draws}
\title{Summaries of the posterior predictive quantities}
\usage{
\method{fitted}{frmtmb_draws}(
  object,
  newdata = NULL,
  re_formula = arg_unset(),
  scale = c("response", "linear"),
  resp = NULL,
  dpar = NULL,
  nlpar = NULL,
  ndraws = NULL,
  draw_ids = NULL,
  sort = FALSE,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975),
  ...
)

\method{predict}{frmtmb_draws}(
  object,
  newdata = NULL,
  re_formula = arg_unset(),
  transform = NULL,
  resp = NULL,
  negative_rt = FALSE,
  ndraws = NULL,
  draw_ids = NULL,
  sort = FALSE,
  ntrys = NULL,
  cores = NULL,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975),
  ...
)

\method{residuals}{frmtmb_draws}(
  object,
  newdata = NULL,
  re_formula = arg_unset(),
  method = "posterior_predict",
  type = c("ordinary", "pearson"),
  resp = NULL,
  ndraws = NULL,
  draw_ids = NULL,
  sort = FALSE,
  summary = TRUE,
  robust = FALSE,
  probs = c(0.025, 0.975),
  ...
)
}
\arguments{
\item{object}{A \code{frmtmb_draws} from \code{frm_sample()}.}

\item{newdata, re_formula, resp, dpar, nlpar, ndraws, draw_ids}{Passed to
the draws method, where they are documented.}

\item{scale}{\code{"response"} for the expected response, \code{"linear"} for
the linear predictor.}

\item{summary}{If \code{FALSE}, the draws instead of their summary.}

\item{robust}{If \code{TRUE}, the median and MAD instead of the mean and
standard deviation.}

\item{probs}{Probabilities of the quantile columns.}

\item{...}{Passed to the draws method, which is where brms's
\code{point_estimate} and \code{ndraws_point_estimate} are answered.}

\item{transform}{Applied to the draws before they are summarized.}

\item{negative_rt, ntrys, cores, sort}{brms's remaining arguments,
carried so that a positional brms call asks the same question; each
is passed on or refused by the method it belongs to.}

\item{method}{For \code{residuals()}, which predictive distribution the
error is taken against.}

\item{type}{For \code{residuals()}, \code{"ordinary"} (brms's name for the raw
error) or \code{"pearson"}.}
}
\value{
With \code{summary = TRUE} an observations-by-four matrix, or an
observations-by-four-by-K array for a category distribution. With
\code{summary = FALSE} the draws.
}
\description{
brms's three summarizing methods, each a summary of the draws method
beside it:
\itemize{
\item \code{fitted()} summarizes \code{\link[=posterior_epred]{posterior_epred()}} (or
\code{\link[=posterior_linpred]{posterior_linpred()}} under \code{scale = "linear"});
\item \code{predict()} summarizes \code{\link[=posterior_predict]{posterior_predict()}};
\item \code{residuals()} summarizes \code{\link[=predictive_error]{predictive_error()}}.
}

\code{summary = FALSE} returns the draws themselves, which is what the
method it wraps returns.
}

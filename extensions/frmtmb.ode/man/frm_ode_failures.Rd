% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ode.R
\name{frm_ode_failures}
\alias{frm_ode_failures}
\title{Groups whose ODE solve failed in the last \code{frm_ode()} call}
\usage{
frm_ode_failures()
}
\value{
\code{NULL} when the last call solved every group. Otherwise a
list with \code{groups} (the labels that failed), \code{n_groups} (how many
groups the call had), \code{penalty} (the value written into their
rows), and \code{when}.
}
\description{
Reports the groups that \code{\link[=frm_ode]{frm_ode()}} could not solve on its most
recent call, and whose rows therefore hold the \code{penalty} value rather
than a solution. \code{frm_ode()} also warns when that happens; this
function is for reading the record afterwards, for example after a
\code{predict()} whose warnings were suppressed.
}
\details{
The record covers the last call only, from anywhere: a fit, a
\code{predict()}, or a direct call. It is reset by every \code{frm_ode()} call,
so a clean call clears it. See the "Failed solves" section of
\code{\link[=frm_ode]{frm_ode()}} for why a failure during fitting usually cannot be
recorded at all.
}
\examples{
# NULL until a solve fails
frm_ode_failures()
}
\seealso{
\code{\link[=frm_ode]{frm_ode()}}
}

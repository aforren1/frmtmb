% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gddm.R
\name{gddm_simulate}
\alias{gddm_simulate}
\title{Simulate from a generalized drift-diffusion model}
\usage{
gddm_simulate(
  n,
  ...,
  coh = 0,
  drift = gddm_drift_constant(),
  bound = gddm_bound_constant(),
  start = gddm_start_point(),
  lapse = c("none", "uniform"),
  control = gddm_control()
)
}
\arguments{
\item{n}{Number of trials.}

\item{...}{Parameter values by name: \code{mu}, \code{bs}, \code{ndt} and whatever else
the chosen components need (\code{alpha}, \code{leak}, \code{tau}, \code{kappa}, \code{bias},
\code{sz}, \code{lapse}). Anything not given takes the component's own starting
value.}

\item{coh}{Coherence covariate, recycled to length \code{n}. Only meaningful
with \code{\link[=gddm_drift_coherence]{gddm_drift_coherence()}}.}

\item{drift, bound, start, lapse, control}{As in \code{\link[=gddm]{gddm()}}.}
}
\value{
A data frame with \code{rt}, \code{upper}, \code{cond} and, when a coherence
drift is used, \code{coh}.
}
\description{
Draws choices and response times by solving the model's own
Fokker-Planck equation and sampling the resulting defective densities,
so the draws come from the density \code{\link[=gddm]{gddm()}} fits rather than from a
discretized forward simulation, whose first passages are late by however
much the step misses excursions between monitoring times.
}
\examples{
set.seed(2)
head(gddm_simulate(20, mu = 1.5, bs = 2, ndt = 0.2,
                   control = gddm_control(t_max = 2, dt = 0.02,
                                          ny = 101)))
}
\seealso{
\code{\link[=gddm]{gddm()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdm.R
\name{rdm_simulate}
\alias{rdm_simulate}
\title{Simulate from a racing diffusion model}
\usage{
rdm_simulate(n, v, A = 0.5, k = 0.5, ndt = 0.2)
}
\arguments{
\item{n}{Number of trials.}

\item{v}{Drift rates, one per accumulator, all positive. Its length
sets the number of accumulators. May be a matrix with one row per
trial, for a drift that varies with a covariate.}

\item{A}{Upper end of the start-point range.}

\item{k}{Distance from the top of the start-point range to the
threshold, so the threshold is \code{A + k}.}

\item{ndt}{Non-decision time.}
}
\value{
A data frame with \code{choice} (the accumulator that reached the
threshold, from 1) and \code{rt}.
}
\description{
Draws from the generative process directly: a uniform start point per
accumulator, then that accumulator's inverse-Gaussian first-passage
time, and whichever arrives first. This is the joint draw of choice
and time, which is what the model produces and what a fit needs;
\code{simulate()} on a fitted object instead redraws only the time, holding
each row's observed choice.
}
\examples{
set.seed(1)
dat <- rdm_simulate(500, v = c(3.0, 2.0, 1.2), A = 0.5, k = 0.5,
                    ndt = 0.2)
table(dat$choice)
tapply(dat$rt, dat$choice, mean)

}

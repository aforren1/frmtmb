% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gddm.R
\name{gddm-start}
\alias{gddm-start}
\alias{gddm_start_point}
\alias{gddm_start_uniform}
\title{Starting-point specifications for \code{\link[=gddm]{gddm()}}}
\usage{
gddm_start_point()

gddm_start_uniform()
}
\value{
A \code{gddm_component}.
}
\description{
Where the accumulator starts, as a distribution over the rescaled state
at time zero. \code{bias} is the relative start point in \verb{(0, 1)}, the
fraction of the boundary separation above the lower boundary, exactly as
in \code{\link[=wiener]{wiener()}}; 0.5 is unbiased.
}
\details{
\describe{
\item{\code{gddm_start_point()}}{All mass at \code{bias}. Free parameter \code{bias},
logit link.}
\item{\code{gddm_start_uniform()}}{Uniform on an interval of half-width
\code{sz} around \code{bias}, with \code{sz} measured as a fraction of the half
separation. Free parameters \code{bias} and \code{sz}, both logit link. This
is the tape-safe counterpart of the Ratcliff start-point
variability, which the analytic family can only reach by
quadrature.}
}

A point start is a delta function, which no fixed grid can hold. Both
components spread the initial mass with the same cubic B-spline used for
the non-decision-time shift, so the starting distribution is smooth in
\code{bias} and no grid index depends on a parameter. Both are then
normalized to unit mass on the grid, which also keeps the family honest
when \code{bias} and \code{sz} together push mass past a boundary.
}
\section{Writing your own}{

A start term is the value of \code{\link[=gddm_start_term]{gddm_start_term()}}. Its \code{fn} is called as
\code{fn(y, h, p)}, where \code{y} are the rescaled grid nodes on \verb{(-1, 1)} and
\code{h} is the node spacing, and it returns a density at those nodes.
}

\seealso{
\code{\link[=gddm]{gddm()}}, \link{gddm-drift}, \link{gddm-bound}
}

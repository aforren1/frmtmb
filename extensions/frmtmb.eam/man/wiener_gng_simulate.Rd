% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wiener-gng.R
\name{wiener_gng_simulate}
\alias{wiener_gng_simulate}
\title{Simulate from a go/no-go diffusion model}
\usage{
wiener_gng_simulate(
  n,
  mu = 1,
  bs = 1.4,
  ndt = 0.25,
  bias = 0.5,
  deadline = 1.5,
  sv = 0,
  sz = 0,
  st = 0
)
}
\arguments{
\item{n}{Number of trials.}

\item{mu}{Drift rate.}

\item{bs}{Boundary separation.}

\item{ndt}{Non-decision time.}

\item{bias}{Relative start point in \verb{(0, 1)}.}

\item{deadline}{The response deadline. One number, or one per trial.}

\item{sv, sz, st}{Across-trial variability in the drift rate, the
relative start point and the non-decision time, as \code{\link[=ddm_simulate]{ddm_simulate()}}
takes them. Zero, the default, is the plain model.}
}
\value{
A data frame with \code{responded} (1 for a go trial), \code{rt} (the
response time on a go trial, and the deadline on a no-go trial,
which is the placeholder \code{\link[=wiener_gng]{wiener_gng()}} documents) and \code{deadline}.
}
\description{
Draws from the generative process: run the two-boundary diffusion,
and record a response only if it reached the upper boundary before
the deadline. Nothing here evaluates the go density or the no-go
probability, so the simulator is an independent statement of the same
model and can be used to check the likelihood rather than merely to
agree with it.
}
\examples{
set.seed(1)
dat <- wiener_gng_simulate(1000, mu = 1.0, bs = 1.4, ndt = 0.25,
                           deadline = 1.5)
mean(dat$responded)
summary(dat$rt[dat$responded == 1])

}

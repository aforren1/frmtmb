% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gddm.R
\name{gddm-drift}
\alias{gddm-drift}
\alias{gddm_drift_constant}
\alias{gddm_drift_coherence}
\alias{gddm_drift_leak}
\title{Drift specifications for \code{\link[=gddm]{gddm()}}}
\usage{
gddm_drift_constant()

gddm_drift_coherence(cmax = 1, cov = 1L)

gddm_drift_leak()
}
\arguments{
\item{cmax}{Scale the coherence covariate is divided by, so that the
nonlinearity is anchored at a coherence a subject sees. The paper uses
the largest coherence in the design.}

\item{cov}{Which \code{vreal()} value carries the coherence, counting from 1.}
}
\value{
A \code{gddm_component}.
}
\description{
The drift is the mean rate of evidence accumulation, \code{a(x, t)}. A GDDM
lets it depend on the current state \code{x}, on time, and on a covariate.
Terms are summed: pass one, or a list of several.
}
\details{
\describe{
\item{\code{gddm_drift_constant()}}{\code{a = mu}. The textbook drift-diffusion
drift. Free parameter \code{mu}, identity link, so it is signed.}
\item{\code{gddm_drift_coherence()}}{\verb{a = sign(C) mu (|C| / cmax)^alpha},
the coherence nonlinearity of Shinn et al. (2020). The covariate \code{C}
is supplied through \code{vreal()} and \code{cmax} scales it. Free parameters
\code{mu} (identity link) and \code{alpha} (log link).}
\item{\code{gddm_drift_leak()}}{\verb{a = -leak x}, added to whichever base term
is used. Positive \code{leak} is leaky integration, which pulls the
accumulator back toward zero; negative \code{leak} is unstable
integration, which pushes it away. Identity link. Note the sign is
the paper's \code{l} and the negative of PyDDM's \code{leak}.}
}

Exactly one base term, \code{gddm_drift_constant()} or
\code{gddm_drift_coherence()}, must be present and must come first, because
it supplies \code{mu}, the parameter that receives the model formula.
}
\section{Writing your own}{

A drift term is the value of \code{\link[=gddm_drift_term]{gddm_drift_term()}}. Its \code{fn} is called on
the tape as \code{fn(x, t, p, cov)} where \code{x} is the accumulator state at the
grid nodes in the original coordinate, \code{t} is the time as a plain
number, \code{p} is a named list holding one value of each free parameter for
the condition being solved, and \code{cov} is that condition's \code{vreal()}
values. It returns the drift at each node, and must contain no
comparison against a parameter.
}

\seealso{
\code{\link[=gddm]{gddm()}}, \code{\link[=gddm_bound_constant]{gddm_bound_constant()}}, \code{\link[=gddm_start_point]{gddm_start_point()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndt-seam.R
\name{ndt_bound}
\alias{ndt_bound}
\title{The bound a non-decision time is measured against}
\usage{
ndt_bound(y, aterms = list(), max_ndt = NULL, what = "ndt_bound()")
}
\arguments{
\item{y}{The response, a numeric vector of times.}

\item{aterms}{The addition-term values for this response, as a
density receives them. Only \code{ndt_group} is read.}

\item{max_ndt}{An absolute bound in the response's own units, or
\code{NULL} to take the fastest response. Above the fastest response it
is refused, and it cannot be combined with \code{ndt_group()}.}

\item{what}{The family's name, used in the refusals so that the
sentence a user reads names the family they wrote.}
}
\value{
An object of class \code{"frmtmb_eam_ndt_bound"}: a list with
\code{ub}, the scalar bound; \code{floors}, the per-group bounds or \code{NULL};
\code{sizes}, the trial count behind each one; \code{pending}, always \code{FALSE}
here; and \code{what}.
}
\description{
Derives the upper bound \code{\link[=wiener]{wiener()}} and its siblings put on the
non-decision time, so that a family in another package gets the same
bound, the same \code{ndt_group()} behavior and the same refusals without
writing them again. Pair it with \code{\link[=ndt_bound_attach]{ndt_bound_attach()}}, which puts the
result on a family object.
}
\details{
The bound is one of two things.
\itemize{
\item With no \code{ndt_group()} in \code{aterms} it is ONE number: \code{max_ndt} when
that is given, and the fastest response otherwise. \code{ndt} is then a
TIME, the link carries the bound, and a consumer's density reads
\code{dpars$ndt} in the response's own units.
\item With \code{ndt_group()} it is one number PER GROUP, that group's own
fastest response. \code{ndt} is then a FRACTION of the row's own bound,
on a plain logit, and the density multiplies it back out by the
per-row vector that arrives in the addition-term values under the
reserved name \code{ndt_floor}.
}

\code{max_ndt} and \code{ndt_group()} together are refused: they set the same
bound to different things.
}
\section{Why the group's own floor and not the data set's}{

The information about one subject's non-decision time is that
subject's own fastest response. At 30 subjects by 400 trials with a
between-subject spread of 26 ms on a mean of 250 ms, 20 of the 30
subjects have a true non-decision time above the GLOBAL minimum while
none is above its own, so a single bound runs the fit to the wall.
\code{\link[=wiener]{wiener()}} and \code{dev/ndt-findings.md} carry the measurements.
}

\examples{
rt <- c(0.31, 0.42, 0.55, 0.61, 0.78)
g <- c("a", "a", "a", "b", "b")
# `[[` and not `$`: the record has five names and `bd$s` would
# partial-match `sizes`
ndt_bound(rt)[["ub"]]
ndt_bound(rt, list(ndt_group = ndt_bound_key(g)))[["floors"]]
}
\seealso{
\code{\link[=ndt_bound_attach]{ndt_bound_attach()}} to put it on a family,
\code{\link[=ndt_bound_of]{ndt_bound_of()}} to read one back, \code{\link[=ndt_time]{ndt_time()}} to report a fitted
non-decision time in the response's own units.
}

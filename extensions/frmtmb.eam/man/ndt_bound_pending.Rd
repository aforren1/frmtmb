% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndt-seam.R
\name{ndt_bound_pending}
\alias{ndt_bound_pending}
\title{The bound a family carries before it has seen a response}
\usage{
ndt_bound_pending(max_ndt = NULL, what = "ndt_bound()")
}
\arguments{
\item{max_ndt}{An absolute bound in the response's own units, or
\code{NULL} for the pending state.}

\item{what}{The family's name, for the refusals.}
}
\value{
An object of class \code{"frmtmb_eam_ndt_bound"}.
}
\description{
A family object built outside \code{\link[frmtmb:frm]{frmtmb::frm()}} has no data, so it has
no fastest response and no bound. That state is not neutral, and this
is the seam entry that settles it, so that a family which reaches
\code{\link[=ndt_bound_attach]{ndt_bound_attach()}} at CONSTRUCTION is in one of two honest states
rather than carrying whatever link its constructor happened to
declare.
}
\details{
\itemize{
\item \code{max_ndt = NULL} gives a PENDING bound. The \code{ndt} link then refuses
by name until \code{\link[frmtmb:frm]{frmtmb::frm()}} settles it. That refusal is
load-bearing rather than decorative: \code{\link[frmtmb:mixture]{frmtmb::mixture()}} never
finalizes its components, so a component with an unsettled bound
would read \code{ndt} on a scale nothing had set; and \code{bf(ndt = )} runs
\code{linkfun()} at PARSE time, where the range check is against
whichever link the family is carrying, so a constant outside the
SETTLED bound passes a check against a placeholder and reaches the
objective as \code{NaN}.
\item \verb{max_ndt = <a number>} gives a SETTLED bound, and the family scores
outside \code{frm()}: \code{ndt} is a time measured against that bound,
\code{predict(dpar = "ndt", type = "response")} reports seconds, and a
pinned constant means what it says.
}

The second is what a package reaching into another family's density
slot needs, and it is what \code{\link[=wiener]{wiener()}}, \code{\link[=lba]{lba()}}, \code{\link[=rdm]{rdm()}} and
\code{\link[=wiener_gng]{wiener_gng()}} do for themselves through their own \code{max_ndt}
argument.
}
\examples{
ndt_bound_pending()[["pending"]]
ndt_bound_pending(0.4)[["ub"]]
}
\seealso{
\code{\link[=ndt_bound]{ndt_bound()}} for the bound derived once the response is in
hand, \code{\link[=ndt_bound_attach]{ndt_bound_attach()}} to put either on a family.
}

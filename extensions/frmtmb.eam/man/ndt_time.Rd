% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ddm-shared.R
\name{ndt_time}
\alias{ndt_time}
\title{The non-decision time, in the response's own units}
\usage{
ndt_time(object, newdata = NULL, ...)
}
\arguments{
\item{object}{A fitted \code{\link[=wiener]{wiener()}}, \code{\link[=lba]{lba()}}, \code{\link[=rdm]{rdm()}} or \code{\link[=wiener_gng]{wiener_gng()}}
model, or a fit from another package whose family carries a bound
from \code{\link[=ndt_bound_attach]{ndt_bound_attach()}}.}

\item{newdata}{Optional data frame. It must carry the model's
\code{ndt_group()} column when the model has one.}

\item{...}{Passed to \code{\link[stats:predict]{stats::predict()}}; \code{re.form} and
\code{allow_new_levels} are the useful ones.}
}
\value{
A numeric vector, one non-decision time per row.
}
\description{
Without \code{ndt_group()} this is \code{predict(fit, dpar = "ndt", type = "response")}, which already reports a time. With \code{ndt_group()} the
\code{ndt} link is a plain logit on a FRACTION of the row's own bound, so
\code{predict()} reports that fraction and this multiplies it back out.
Written so that a caller need not know which of the two a fit is.
}
\details{
The bound is the fastest response of the row's \code{ndt_group()}, or of
the whole data set when the model has no \code{ndt_group()}, or the
\code{max_ndt} the family was given. Whichever it is, it is a property of
the data the model was FITTED to, so a prediction on new rows is
scaled by the same bound the fit used.

It reads the bound record rather than the family's name, so a fit
from ANOTHER package whose family carries a bound through
\code{\link[=ndt_bound_attach]{ndt_bound_attach()}} is reported here too, and
\code{frmtmb.learn::rlddm()} is the first of those.
}
\examples{
set.seed(1)
d <- ddm_simulate(300, mu = 1.2, bs = 1.5, ndt = 0.25)
fit <- frm(bf(rt | dec(upper) ~ 1, bs ~ 1, ndt ~ 1, bias = 0.5),
           family = wiener(), data = d)
# with no ndt_group() the two agree: `ndt` is already a time
head(predict(fit, dpar = "ndt", type = "response"), 3)
head(ndt_time(fit), 3)
}
\seealso{
\code{\link[=wiener]{wiener()}} for what the two parameterizations are and why
there are two, \code{\link[=ndt_bound]{ndt_bound()}} for the seam another package's family
reaches them through.
}

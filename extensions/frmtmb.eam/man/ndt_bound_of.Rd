% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndt-seam.R
\name{ndt_bound_of}
\alias{ndt_bound_of}
\title{The non-decision-time bound a family already carries}
\usage{
ndt_bound_of(fam)
}
\arguments{
\item{fam}{A \code{frmtmb_family} object, fitted or not.}
}
\value{
An object of class \code{"frmtmb_eam_ndt_bound"}, or \code{NULL}.
}
\description{
\code{NULL} when the family has none, and \code{NULL} when the one it has is
still PENDING, which is the state a family constructed outside
\code{\link[frmtmb:frm]{frmtmb::frm()}} is in before the response has been seen.
}
\details{
A settled bound is KEPT rather than re-derived, and that is what this
is for. \code{\link[frmtmb:frm]{frmtmb::frm()}} runs \code{family_finalize} again on new rows for
\code{influence()}, \code{frm_simulate()} and the prior-predictive path, so a
bound re-derived from fewer rows would make a leave-one-out refit a
refit of a DIFFERENT model. A \code{family_finalize} written against this
seam therefore asks first and returns the family untouched when the
answer is not \code{NULL}.
}
\examples{
# a family built outside frm() has no settled bound yet
ndt_bound_of(wiener())
ndt_bound_of(wiener(max_ndt = 0.4))[["ub"]]
}
\seealso{
\code{\link[=ndt_bound]{ndt_bound()}}, \code{\link[=ndt_bound_attach]{ndt_bound_attach()}}.
}

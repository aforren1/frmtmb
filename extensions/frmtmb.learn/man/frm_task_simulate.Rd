% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task.R
\name{frm_task_simulate}
\alias{frm_task_simulate}
\title{Draw datasets from a learning family's generative process}
\usage{
frm_task_simulate(
  family,
  data,
  pars,
  nsim = 1L,
  response = "choice",
  seed = NULL
)
}
\arguments{
\item{family}{A family from this package, such as
\code{\link[=bandit2arm_delta]{bandit2arm_delta()}}.}

\item{data}{A design from \code{\link[=frm_task_design]{frm_task_design()}}, or any data frame with
the same canonical columns.}

\item{pars}{Named list of parameters on their NATURAL scales (a
learning rate in \verb{(0, 1)}, not its logit). Each element is one
value, recycled everywhere; or one value per subject, in the order
\code{levels(factor(data$id))} gives; or one value per ROW. The per-row
form is what a covariate that varies WITHIN a subject needs, which
is what a reversal is: see the example.}

\item{nsim}{Datasets to draw.}

\item{response}{Name of the column the drawn choice goes into.}

\item{seed}{Passed to \code{\link[=set.seed]{set.seed()}} when not \code{NULL}.}
}
\value{
A list of \code{nsim} data frames, each \code{data} with the drawn
columns replaced.
}
\description{
Walks each subject forward through the design, drawing a choice from
the current value store and learning from the payoff that choice
earns. It is the same recursion the likelihood tapes, at
\code{mode = "simulate"}, so a draw and the density that scores it cannot
drift apart.
}
\details{
This route takes parameters directly, per subject or per row, rather
than through a formula and a random-effect covariance. That is what a
recovery study wants, and it makes the simulator independent of the
fitting machinery, so agreement between a fit and its truth is
evidence about both. \code{\link[frmtmb:frm_simulate]{frmtmb::frm_simulate()}} is the other route and
goes through the fitted grammar; the two agree by construction for
every family except \code{\link[=ts_par7]{ts_par7()}}, which \code{\link[frmtmb:frm_simulate]{frmtmb::frm_simulate()}}
refuses because one trial's draw is three numbers.
}
\examples{
d <- frm_task_design("bandit2arm", n_subject = 5, n_trial = 30,
                     seed = 1)
sims <- frm_task_simulate(bandit2arm_delta(subject = id, trial = trial),
                          d, pars = list(alpha = 0.4, tau = 3),
                          nsim = 2, seed = 1)
table(sims[[1]]$choice)

# a learning rate that changes within a subject, one value per row
r <- frm_task_design("reversal", n_subject = 5, n_trial = 30, seed = 2)
a <- ifelse(r$after_reversal == "after", 0.6, 0.2)
rs <- frm_task_simulate(bandit2arm_delta(subject = id, trial = trial),
                        r, pars = list(alpha = a, tau = 3), seed = 2)
table(rs[[1]]$choice)
}
\seealso{
\code{\link[=frm_task_design]{frm_task_design()}}, \code{\link[frmtmb:frm_simulate]{frmtmb::frm_simulate()}}
}

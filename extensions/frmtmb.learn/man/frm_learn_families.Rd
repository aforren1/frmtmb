% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace.R
\name{frm_learn_families}
\alias{frm_learn_families}
\title{The families this package supplies, and what each one is}
\usage{
frm_learn_families()
}
\value{
A data frame, one row per family.
}
\description{
A reference table rather than a computation. It carries the name each
family has here, the name hBayesDM gives the same model where it has
one, the task, the options per trial, the addition terms and the
parameters with their links.
}
\details{
The parameter names are this package's, not hBayesDM's, and the
\code{hbayesdm_pars} column is the map, in the same order as \code{pars}. A
cell that is a name is a RENAME; a cell that is an expression is a
TRANSFORM, and three of the six rows carry one. Two of those three
involve an estimated parameter (\code{-tau * alpha} for
\code{\link[=prl_fictitious]{prl_fictitious()}}'s \code{bias}, \code{pi / tau1} for \code{\link[=ts_par7]{ts_par7()}}'s \code{pers}),
so no constant relates them and a value carried across unchanged
fits a different model without complaint. \code{?prl_fictitious} and
\code{?ts_par7} derive both. One
convention across six families is worth more than six inherited ones:
\code{alpha} is always a learning rate on \verb{(0, 1)} and \code{tau} is always the
softmax sensitivity on \verb{(0, Inf)}, so what a formula does to \code{alpha}
means the same thing whichever family it is written against.
hBayesDM spells the two-armed bandit's learning rate \code{A}, the
reversal models' sensitivity \code{beta}, and the Iowa gambling task's
UTILITY EXPONENT \code{alpha}.

ONE ROW HAS NO COUNTERPART. \code{\link[=rlddm]{rlddm()}} is a delta rule feeding a
drift-diffusion choice rule, and hBayesDM ships the two halves
separately (\code{bandit2arm_delta} and \code{choiceRT_ddm}) rather than the
join. Its \code{hbayesdm} cell says \code{none} rather than naming a model it
is not, and \code{?rlddm} says which of that package's parameters map onto
which of its own. \code{\link[=igt_orl]{igt_orl()}}'s cell is a rename with one transform:
hBayesDM bounds \code{K} to \verb{(0, 5)} through a probit where this family
puts it on \verb{(0, Inf)} with a log link, and the two are the same
number on the same scale wherever hBayesDM's bound is not binding.

Two cautions the map does not remove. Core refuses a distributional
parameter name containing a dot or an underscore, which is why the
dual-rate family spells its rates \code{Arew} and \code{Apun} rather than
anything more readable. And \code{lambda} is used by the literature for
three unrelated quantities: the arm decay in the Kalman filter, the
eligibility trace in the two-step model, and loss aversion in the
Iowa gambling task. Each family's help says which.
}
\examples{
frm_learn_families()[, c("family", "hbayesdm", "pars")]
}

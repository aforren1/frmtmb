% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task.R
\name{frm_task_design}
\alias{frm_task_design}
\title{Build a trial-level design for a learning task}
\usage{
frm_task_design(
  task = c("bandit2arm", "reversal", "bandit4arm_restless", "igt", "twostep"),
  n_subject = 30L,
  n_trial = 100L,
  p = c(0.7, 0.3),
  seed = NULL,
  ...
)
}
\arguments{
\item{task}{One of \code{"bandit2arm"} (a stationary two-armed bandit),
\code{"reversal"} (the same with the contingency reversed at the
halfway point, and an \code{after_reversal} factor to model it with),
\code{"bandit4arm_restless"} (four arms whose payoffs follow the
decaying Gaussian random walk of Daw et al. 2006), \code{"igt"} (the
Iowa gambling task's four decks) or \code{"twostep"} (the two-stage
Markov task of Daw et al. 2011).}

\item{n_subject, n_trial}{Subjects, and trials per subject.}

\item{p}{Two payoff probabilities, for the two-armed tasks.}

\item{seed}{Passed to \code{\link[=set.seed]{set.seed()}} when not \code{NULL}.}

\item{...}{Task-specific settings: \code{decay} and \code{center} and \code{sd_walk}
and \code{sd_obs} for the restless bandit, \code{p_reward} for the two-step
task's stage-two options.}
}
\value{
A data frame, ready for \code{\link[=frm_task_simulate]{frm_task_simulate()}}.
}
\description{
One row per subject and trial, with the payoff schedule of EVERY
option fixed in advance and a placeholder response. Fixing the whole
schedule ahead of the choices is what makes a draw from these models
coherent: a simulated subject that takes the arm the real one did not
still needs to be paid.
}
\details{
The column names are canonical, because \code{\link[=frm_task_simulate]{frm_task_simulate()}} reads
them: \code{id}, \code{trial}, \code{choice}, and \code{pay1} to \code{pay2} or \code{pay4}. A fit
to your own data needs none of this; the family reads whatever
columns your formula names.
}
\examples{
d <- frm_task_design("reversal", n_subject = 4, n_trial = 20, seed = 1)
head(d)
table(d$after_reversal)
}
\seealso{
\code{\link[=frm_task_simulate]{frm_task_simulate()}}, \code{\link[=bandit2arm_delta]{bandit2arm_delta()}}
}

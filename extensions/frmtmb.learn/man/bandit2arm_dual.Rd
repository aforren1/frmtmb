% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bandit2arm-dual.R
\name{bandit2arm_dual}
\alias{bandit2arm_dual}
\title{Two-armed delta learning with separate rates for gains and losses}
\usage{
bandit2arm_dual(subject, trial = NULL, split = c("pe", "outcome"))
}
\arguments{
\item{subject}{The column separating one learner's trial sequence
from the next, given unquoted. It is carried by the family rather
than by the formula, the way \code{frmtmb.latent::hmm()} carries its
sequence grouping, because it orders the likelihood rather than
entering a linear predictor.}

\item{trial}{The column giving trial order within a subject, given
unquoted. \code{NULL} uses the order the rows appear in.}

\item{split}{\code{"pe"} splits on the sign of the prediction error,
\code{"outcome"} on the sign of the outcome itself.}
}
\value{
A \code{frmtmb_family} object, for \code{frm(family = )}.
}
\description{
\code{\link[=bandit2arm_delta]{bandit2arm_delta()}} with one learning rate replaced by two, so that
good news and bad news are absorbed at different speeds. This
asymmetry is the parameter most of the clinical literature is about,
and it is the model hBayesDM calls \code{prl_rp} when the split is on the
sign of the OUTCOME.
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{pe  <- reward - Q[chosen]
Q[chosen] <- Q[chosen] + (pe > 0 ? Arew : Apun) * pe
}\if{html}{\out{</div>}}
}
\section{Which sign, and why it matters here more than elsewhere}{

Two models are in circulation and they are not the same model. One
splits on the sign of the PREDICTION ERROR: an outcome better than
expected is a gain even when it is a small reward. The other splits
on the sign of the OUTCOME, which is what \code{prl_rp} does. \code{split}
chooses, and the default is \code{"pe"}.

They also cost different things to tape. The outcome's sign is DATA,
so \code{split = "outcome"} selects the rate with a column of zeros and
ones computed once and adds nothing to the tape. A prediction error
is an AD quantity, and RTMB refuses a comparison on one outright, so
\code{split = "pe"} selects with \code{sign()}. That is exact rather than
smoothed, and the kink it introduces at \code{pe == 0} is the model's own:
the update is zero there under both rates, so the two branches agree
at the crossing and the derivative from each side is the right one.

With \code{split = "outcome"}, an outcome of exactly zero counts as
punishment, which is what \code{prl_rp} does and what the usual \code{0}/\code{1}
payoff coding needs. Code punishment as \code{-1} if you want the
symmetric reading.

One consequence of the kink is worth knowing before fitting \code{"pe"}
hierarchically on GRADED payoffs. The joint log density is then
non-smooth in the random effects wherever a prediction error crosses
zero, and the Laplace inner solve stops short of the conditional
mode: measured on the identity fixture, the largest gradient on the
subject effects is 4.0e-02 under \code{"pe"} against 1.4e-15 under
\code{"outcome"} on the same data. The fit and its log likelihood are
unaffected to machine precision, but a convergence warning on a
\code{"pe"} fit with graded payoffs is expected rather than alarming. On
binary payoffs neither split has a live kink, and the two are the
same model anyway.
}

\section{The Laplace caveat}{

frmtmb integrates the subject effects out with a Laplace
approximation, which is exact only when the conditional log-density
is quadratic. For binary choices it is not, and the fewer trials a
subject has the less quadratic it is. Measured on this family, the
fixed effects survive short sessions and the variance components do
not: see the Laplace section of \code{vignette("learning")} and
\code{?frmtmb.learn} for the numbers.

\code{frm(importance =)}, the usual way to price that error, now WORKS for
every family here. It was refused in the first release because the
correction needs one log-likelihood value per subject and the
structured protocol had no slot to put them in; the slots landed and
the families declare them. What the correction is worth depends on
the design and not on the family: at 40 subjects by 100 trials it
moves a well-identified subject-level standard deviation up by about
0.1 to 0.2 log units, toward the truth, with good diagnostics, and at
20 trials it has nothing to correct because the Laplace fit has
usually collapsed the component to zero already. \code{?frmtmb.learn}
carries the per-dataset table.
}

\examples{
d <- frm_task_design("bandit2arm", n_subject = 6, n_trial = 40, seed = 2)
d$choice <- frm_task_simulate(
  bandit2arm_dual(subject = id, trial = trial), d,
  pars = list(Arew = 0.5, Apun = 0.15, tau = 3),
  seed = 2)[[1]]$choice
fit <- frmtmb::frm(
  frmtmb::bf(choice | reward(pay1, pay2) ~ 1, Apun ~ 1, tau ~ 1),
  family = bandit2arm_dual(subject = id, trial = trial), data = d)
frmtmb::fixef(fit)
}
\references{
Ahn, W.-Y., Haines, N. and Zhang, L. (2017). Revealing
neurocomputational mechanisms of reinforcement learning and
decision-making with the hBayesDM package. \emph{Computational
Psychiatry} 1, 24-57.
}
\seealso{
\code{\link[=bandit2arm_delta]{bandit2arm_delta()}}, \code{\link[=frm_learn_families]{frm_learn_families()}}
}

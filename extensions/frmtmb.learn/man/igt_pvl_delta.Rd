% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igt-pvl-delta.R
\name{igt_pvl_delta}
\alias{igt_pvl_delta}
\title{Prospect-valence learning with a delta rule, for the Iowa gambling
task}
\usage{
igt_pvl_delta(subject, trial = NULL)
}
\arguments{
\item{subject}{The column separating one learner's trial sequence
from the next, given unquoted. It is carried by the family rather
than by the formula, the way \code{frmtmb.latent::hmm()} carries its
sequence grouping, because it orders the likelihood rather than
entering a linear predictor.}

\item{trial}{The column giving trial order within a subject, given
unquoted. \code{NULL} uses the order the rows appear in.}
}
\value{
A \code{frmtmb_family} object, for \code{frm(family = )}.
}
\description{
Four decks, each paying a gain on every trial and an occasional
larger loss. What makes this model different from the bandits is that
the OUTCOME is transformed before it is learned from: money is
converted to subjective value by a prospect-theory utility with a
compressive exponent and a loss-aversion multiplier, and the delta
rule then runs on that.
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{u <- if (x >= 0)  abs(x)^shape
     else        -lambda * abs(x)^shape
Q[chosen] <- Q[chosen] + alpha * (u - Q[chosen])
P(deck k) <- softmax(tau * Q)
}\if{html}{\out{</div>}}
}
\section{Names, and one trap}{

hBayesDM calls this \code{igt_pvl_delta} and spells the parameters \code{A},
\code{alpha}, \code{lambda} and \code{cons}. The map is\tabular{lll}{
   this family \tab hBayesDM \tab what it is \cr
   \code{alpha} \tab \code{A} \tab the learning rate, on (0, 1) \cr
   \code{shape} \tab \code{alpha} \tab the utility exponent, on (0, 1); see below \cr
   \code{lambda} \tab \code{lambda} \tab loss aversion, positive \cr
   \code{tau} \tab \code{3^cons - 1} \tab softmax sensitivity, positive \cr
}


\strong{\code{alpha} means different things in the two packages.} It is the
learning rate here, as it is in every other family in this package,
and it is the utility exponent in hBayesDM. Reading an hBayesDM
\code{alpha} into this family's \code{alpha} fits a different model that
converges quietly.

\strong{\code{shape} reaches a NARROWER range than hBayesDM's \code{alpha}.} The
logit link confines it to (0, 1); hBayesDM bounds the same quantity
on (0, 2), through \code{Phi_approx(...) * 2}. An hBayesDM fit whose
utility exponent came out above 1, which is a convex rather than a
compressive utility, is not representable here. Every other bound
difference between the two runs the other way, this package being the
more permissive, so this is the one to check before porting.

\code{tau} is the fourth difference and it is a reparameterization rather
than a rename: hBayesDM estimates a consistency parameter \code{cons} on
\verb{(0, 5)} and uses \code{3^cons - 1} as the sensitivity, while this family
estimates the sensitivity itself on a log link. The two are the same
model at \code{tau = 3^cons - 1}, and \code{tau} is then the same quantity, on
the same scale, that it is in every other family here.
}

\section{Why PVL-delta and not ORL}{

ORL (Haines, Vassileva and Ahn 2018) is the other Iowa gambling model
worth having, and it is not here. PVL-delta was chosen because its
four parameters recover at a realistic scale, which is the standard
this package holds a family to, and because its outcome transform
exercises a part of the engine nothing else does: a nonlinear
function of DATA raised to an estimated exponent. ORL carries two
further value stores and two frequency-weighting parameters that are
known to be weakly identified, so shipping it would mean shipping a
recovery table that says so. The engine takes it in about forty
lines when someone wants it.
}

\section{Scale}{

The exponent makes this family sensitive to the units of the payoffs.
\code{frm_task_design("igt")} returns them in units of 100, which is the
scaling the PVL literature uses; passing raw currency changes what
\code{shape} and \code{tau} mean and will usually make the fit worse
conditioned.
}

\section{The Laplace caveat}{

frmtmb integrates the subject effects out with a Laplace
approximation, which is exact only when the conditional log-density
is quadratic. For binary choices it is not, and the fewer trials a
subject has the less quadratic it is. Measured on this family, the
fixed effects survive short sessions and the variance components do
not: see the Laplace section of \code{vignette("learning")} and
\code{?frmtmb.learn} for the numbers. \code{frm(importance =)}, which is the
usual way to price that error, is REFUSED for every family here; the
refusal names the seam.
}

\examples{
d <- frm_task_design("igt", n_subject = 6, n_trial = 50, seed = 7)
d$choice <- frm_task_simulate(
  igt_pvl_delta(subject = id, trial = trial), d,
  pars = list(alpha = 0.3, shape = 0.4, lambda = 1.5, tau = 1),
  seed = 7)[[1]]$choice
# the good decks are 3 and 4
table(d$choice)
}
\references{
Ahn, W.-Y., Busemeyer, J. R., Wagenmakers, E.-J. and Stout, J. C.
(2008). Comparison of decision learning models using the generalization
criterion method. \emph{Cognitive Science} 32, 1376-1402.
}
\seealso{
\code{\link[=bandit4arm2_kalman_filter]{bandit4arm2_kalman_filter()}}, \code{\link[=frm_task_design]{frm_task_design()}}
}

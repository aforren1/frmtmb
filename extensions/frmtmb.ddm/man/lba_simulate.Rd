% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lba.R
\name{lba_simulate}
\alias{lba_simulate}
\title{Simulate from a linear ballistic accumulator}
\usage{
lba_simulate(n, v, A = 0.5, k = 0.4, ndt = 0.2, sd_v = 1, posdrift = TRUE)
}
\arguments{
\item{n}{Number of trials.}

\item{v}{Drift means, one per accumulator. Its length sets the number
of accumulators. May be a matrix with one row per trial, for a
drift that varies with a covariate.}

\item{A}{Upper end of the start-point range.}

\item{k}{Distance from the top of the start-point range to the
threshold, so the threshold is \code{A + k}.}

\item{ndt}{Non-decision time.}

\item{sd_v}{Drift standard deviation, fixed. One number or one per
accumulator.}

\item{posdrift}{Truncate the drift distribution at zero? Matches the
\code{\link[=lba]{lba()}} argument of the same name.}
}
\value{
A data frame with \code{choice} (the accumulator that responded,
from 1) and \code{rt}.
}
\description{
Draws from the generative process directly: a uniform start point and
a normal drift rate per accumulator, and whichever reaches the
threshold first. This is the joint draw of choice and time, which is
what the model produces and what a fit needs; \code{simulate()} on a
fitted object instead redraws only the time, holding each row's
observed choice.
}
\examples{
set.seed(1)
dat <- lba_simulate(500, v = c(2.5, 1.5, 1.0), A = 0.5, k = 0.4,
                    ndt = 0.2)
table(dat$choice)
tapply(dat$rt, dat$choice, mean)

}

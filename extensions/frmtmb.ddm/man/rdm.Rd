% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdm.R
\name{rdm}
\alias{rdm}
\title{The racing diffusion model}
\usage{
rdm(n, max_ndt = NULL)
}
\arguments{
\item{n}{Number of accumulators, so the number of response
alternatives. At least 2.}

\item{max_ndt}{Upper bound for the non-decision time, in the units of
the response. \code{NULL}, the default, takes it from the data.}
}
\value{
A \code{frmtmb_family}.
}
\description{
A race between \code{n} independent diffusions, for choices with more than
two alternatives. Each accumulator is a Wiener process with its own
positive drift rate and unit diffusion, starting from a point drawn
uniformly on \verb{(0, A)} and running to a common threshold \code{b}. The
first accumulator to reach the threshold is the response, and the
observed time is its arrival time plus a non-decision time.
}
\details{
The model is \code{\link[=lba]{lba()}}'s geometry with the ballistic assumption
removed: same start-point range, same threshold, but each accumulator
is noisy WITHIN a trial rather than drawing one rate at the start of
it. One accumulator's arrival time is therefore an inverse Gaussian
rather than the reciprocal of a normal, and the whole race stays
closed form.

Use it rather than \code{\link[=wiener]{wiener()}} when a design has more than two
response alternatives, and rather than \code{\link[=lba]{lba()}} when the story you
want for trial-to-trial spread is within-trial noise rather than a
rate drawn afresh each trial.
}
\section{The model}{

Write the decision time as \eqn{t = rt - ndt}. One accumulator with
drift \eqn{v} travels a distance \eqn{b - z} from its start point
\eqn{z}, and \eqn{z} is uniform on \eqn{(0, A)}, so the DISTANCE is
uniform on \eqn{(k, k + A)} with \eqn{k = b - A}. For a fixed
distance \eqn{c} the first-passage time is inverse Gaussian with
density \eqn{c\,\phi((c - vt)/\sqrt{t})\,t^{-3/2}}, and averaging that
over the distance integrates in closed form. Put

\deqn{x_1 = \frac{k - v t}{\sqrt t}, \qquad
      x_2 = \frac{k + A - v t}{\sqrt t},}

the standardized positions of the two ends of the distance range at
time \eqn{t}. Then that accumulator's defective density and survival
are

\deqn{f(t) = \frac{v\,(\Phi(x_2) - \Phi(x_1)) -
                   (\phi(x_2) - \phi(x_1))/\sqrt t}{A},}
\deqn{S(t) = \frac{\sqrt t\,(x_2\,\Delta\Phi + \Delta\phi) +
                   A\,\Phi(x_1) -
                   (\Delta E + \Delta\Phi)/(2 v)}{A},}

where \eqn{\Delta} is the change of a quantity across the distance
range and \eqn{E(c) = e^{2 v c}\,\Phi(-(v t + c)/\sqrt t)} is the
reflected term of the inverse-Gaussian distribution function. A trial
on which accumulator \eqn{j} responded at time \eqn{rt} contributes

\deqn{\log f_j(t) + \sum_{i \neq j} \log S_i(t),}

the same race \code{\link[=lba]{lba()}} runs, over a different single-accumulator law.
}

\section{Parameters}{

\describe{
\item{\code{v1}, ..., \code{vn}}{Drift rates, one per accumulator. LOG link,
which is where this family parts company with \code{\link[=lba]{lba()}}: an LBA
drift is the MEAN of a normal drawn once per trial and may be
negative, but a racing-diffusion drift is the rate itself, and an
accumulator with a rate of zero or less never reaches the
threshold at all. These are the primary parameters, so the main
formula goes to all of them and each gets its own coefficients.
Give one its own formula to move it alone.}
\item{\code{A}}{Upper end of the start-point range, so start points are
uniform on \verb{(0, A)}. Log link.}
\item{\code{k}}{Distance from the top of the start-point range to the
threshold, so that \code{b = A + k}. Log link.}
\item{\code{ndt}}{Non-decision time. Bounded link; see below.}
}

The threshold is \code{A + k} rather than a free \code{b}, for the reason
\code{\link[=lba]{lba()}} gives: a log link on \code{k} makes \code{b > A} structural, so a
threshold inside the start-point range, where a fraction of trials
would begin already finished, is not a state the optimizer can reach.

\code{A} divides the density, so it cannot be zero. The log link keeps it
positive at every value of its linear predictor, and a race with no
start-point variability is a limit this family approaches rather than
a model it fits.
}

\section{Mapping to other software and to the paper}{

The same model is written three ways. This family's spelling is
\code{\link[=lba]{lba()}}'s, because the geometry is the same and moving between the
two families should cost one word.\tabular{lll}{
   this family \tab EMC2 \tab Tillman et al. (2020) \cr
   \code{v1}..\code{vn} \tab \code{v} \tab drift rate \cr
   \code{A} \tab \code{A} \tab start-point range \cr
   \code{k} \tab \code{B} \tab threshold less the range \cr
   \code{A + k} \tab \code{B + A} \tab threshold \cr
   \code{ndt} \tab \code{t0} \tab non-decision time \cr
   fixed at 1 \tab \code{s} \tab diffusion coefficient \cr
}


EMC2's \code{B} is this family's \code{k} and NOT the threshold: its own
sampler draws the distance to travel as \code{B + runif(1, 0, A)}, so \code{B}
is the gap above the start-point range, exactly as \code{k} is here.
Reading it as the threshold moves every threshold by \code{A}.

The diffusion coefficient is fixed at one, which is what identifies
the scale: multiplying \code{A}, \code{k} and every drift by one constant, and
the diffusion coefficient with them, leaves the distribution of
\verb{(choice, rt)} unchanged. EMC2 divides \code{A}, \code{B} and \code{v} by its \code{s}
and so fixes the same quantity in the same place.
}

\section{Non-decision time}{

The density is zero at and below \code{ndt}, so the likelihood has a hard
edge at \code{ndt = min(rt)}. As \code{\link[=wiener]{wiener()}} and \code{\link[=lba]{lba()}} do, \code{ndt} gets a
logit scaled onto \verb{(0, max_ndt)} rather than a log link, which makes
the constraint structural. \code{max_ndt} defaults to the smallest
observed response time, taken when the model frame is assembled. Pass
it explicitly to pin the bound, which matters if you will \code{predict()}
on new data whose minimum differs.
}

\section{The response, and why not \code{dec()}}{

A trial is a \verb{(choice, time)} pair. The time is the response and the
choice is per-row data, which reaches the family through \code{vint()}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{frm(bf(rt | vint(choice) ~ cond), family = rdm(3), data = dat)
}\if{html}{\out{</div>}}

\code{vint1} is the ACCUMULATOR INDEX: a whole number in \verb{1..n} naming
which accumulator reached the threshold, counting from one. This is
\code{\link[=lba]{lba()}}'s rule, including that a factor is not accepted, so recode it
with \code{as.integer(factor(choice))} and check that the level order
matches the accumulator numbering.

\code{dec()} does not apply, for \code{\link[=lba]{lba()}}'s reason: that term carries a 0/1
indicator naming one of two boundaries, and a race of \code{n}
accumulators needs \verb{1..n}.
}

\section{Accuracy}{

Every piece is written in the form that keeps its digits rather than
the form the paper prints, and the reasons are \code{\link[=lba]{lba()}}'s reasons.

The normal difference \eqn{\Phi(x_2) - \Phi(x_1)} goes through the
same log-space helper the linear ballistic accumulator uses, because
the subtractive spelling returns exactly zero once the lower tail
rounds to one. The density difference \eqn{\phi(x_2) - \phi(x_1)} and
the boundary-term difference \eqn{\Delta E} both go through
\code{ddm_expdiff()}, which anchors at the larger of the two logs:
written directly, the first underflows one term to zero while the
other is still finite, and the second overflows.

The survival is written out rather than as \code{1 - F}. Measured against
a Gauss-Legendre reference that never subtracts, on a grid of 648
points, the form here holds 1.3e-12 relative down to a survival of
1e-3, 6.4e-11 down to 1e-15 and 4.1e-8 down to 1e-250, and returns no
exact zeros. \code{1 - EMC2:::pWald()} returns exactly zero on 35 of the
315 points of a comparable grid, the first at a survival near 1e-13,
which would send a loser's log contribution to \code{-Inf} on an ordinary
row.

The density agrees with \code{EMC2:::dWald()} to 2.1e-4 relative and with
the same non-subtracting reference to 1.6e-13. The gap is the
subtractive normal difference in EMC2's form, not a disagreement
about the model.

Below all of this the density underflows in double precision; the log
density is floored rather than returning \code{-Inf}, so the optimizer
sees a finite wall instead of a hole. Decision times at or below
zero, which \code{predict()} on faster new data can reach, are floored the
same way.
}

\examples{
set.seed(1)
dat <- rdm_simulate(400, v = c(3.0, 2.0, 1.2), A = 0.5, k = 0.5,
                    ndt = 0.2)
fit <- frm(bf(rt | vint(choice) ~ 1), family = rdm(3), data = dat)
fixef(fit)

}
\references{
Tillman, G., Van Zandt, T. and Logan, G. D. (2020). Sequential
sampling models without random between-trial variability: The racing
diffusion model of speeded decision making. \emph{Psychonomic Bulletin and
Review}, 27(5), 911-936.
}
\seealso{
\code{\link[=rdm_simulate]{rdm_simulate()}} to generate from the model, \code{\link[=lba]{lba()}} for the
ballistic race with the same geometry, and \code{\link[=wiener]{wiener()}} for the
two-choice diffusion.
}

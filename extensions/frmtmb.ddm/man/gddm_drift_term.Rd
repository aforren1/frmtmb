% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gddm.R
\name{gddm_drift_term}
\alias{gddm_drift_term}
\title{Build a drift term}
\usage{
gddm_drift_term(label, dpars, fn, aterms = character(0), base = FALSE)
}
\arguments{
\item{label}{One word naming the term, used when the family prints.}

\item{dpars}{Named list, one entry per free parameter, each a list with
\code{link} (a link name or a link object) and \code{init}, a
\verb{function(y, aterms)} returning a starting value on the natural scale.}

\item{fn}{\verb{function(x, t, p, cov)} returning the drift at each node.}

\item{aterms}{Character vector of addition-term values \code{fn} reads, named
as they reach the density: \code{"vreal1"}.}

\item{base}{\code{TRUE} if the term supplies \code{mu} and can stand alone.}
}
\value{
A \code{gddm_component}.
}
\description{
The extension point behind \code{\link[=gddm_drift_constant]{gddm_drift_constant()}} and its siblings.
}
\seealso{
\link{gddm-drift}
}

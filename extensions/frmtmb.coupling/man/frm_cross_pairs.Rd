% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairs.R
\name{frm_cross_pairs}
\alias{frm_cross_pairs}
\title{The cross-spectra of several channel pairs, stacked with a \code{pair} factor}
\usage{
frm_cross_pairs(X, pairs = NULL, ...)
}
\arguments{
\item{X}{The recording: a numeric matrix or a data frame of numeric
columns whose COLUMNS ARE CHANNELS and whose rows are samples, or a
list of such matrices, one per epoch, each with the same channels.
Channel names come from the column names, or are \code{ch1}, \code{ch2}, and
so on when there are none.}

\item{pairs}{Which pairs to form. \code{NULL}, the default, forms every
pair of two different channels, in column order. Otherwise a
two-column matrix or data frame, one row per pair, naming each
channel by name or by column number.}

\item{...}{Passed to \code{\link[=frm_cross_spectrum]{frm_cross_spectrum()}}: \code{sfreq}, \code{segments},
\code{tapers}, \code{smooth}, \code{window}, \code{frange} and, for a list of epochs,
\code{group}.}
}
\value{
The rows of \code{\link[=frm_cross_spectrum]{frm_cross_spectrum()}} for every pair, with the
factors \code{pair} (\code{"a-b"} for channels \code{a} and \code{b}), \code{ch1} and \code{ch2}
in front. \code{ch1} is the signal \code{frm_cross_spectrum()} takes as \code{x},
so the fitted phase is the lead of \code{ch1} over \code{ch2}.
}
\description{
\code{\link[=cross_wishart]{cross_wishart()}} models ONE channel pair. A recording with more
channels has a pair for every two of them, and the questions asked of
it are about all of those pairs at once. \code{frm_cross_pairs()} runs
\code{\link[=frm_cross_spectrum]{frm_cross_spectrum()}} on each pair and stacks the results, one block
per pair, with a \code{pair} factor that a model can use.
}
\section{Many pairs in one model}{

Fitting each pair on its own and correcting the p-values afterwards
treats the pairs as unrelated. One model over the stacked frame lets
them share information instead:
\itemize{
\item \code{coh ~ 1 + (1 | pair)} gives each pair its own coherence, shrunk
toward the pairs' common level by as much as the data say the pairs
differ. A pair that stands out after shrinkage stands out against
the others, which is the comparison a multiple-comparison correction
tries to make after the fact.
\item \code{coh ~ s(freq, by = pair)} with \code{pair} also a fixed effect gives
each pair its own coherence spectrum, and \code{\link[=frm_coherence]{frm_coherence()}} reads
each one off with a band.
\item \code{coh ~ 0 + pair}, with \code{mu}, \code{pow2} and \code{phase} also by \code{pair}, is
the separate fits in one call: the likelihood is then a sum of one
term per pair, so each pair's estimate is the one its own fit gives.
}

The power of a channel appears in every pair that includes it, so the
blocks share channels. Each block is still a correct likelihood for
its own pair; what the stacked likelihood does not model is the
dependence between two blocks that share a channel. Read the
standard errors of a stacked fit with that in mind.
}

\examples{
set.seed(2)
src <- rnorm(2048)
X <- cbind(Fz = src + rnorm(2048), Cz = 0.8 * src + rnorm(2048),
           Pz = rnorm(2048))
xp <- frm_cross_pairs(X, sfreq = 256, segments = 8)
table(xp$pair)
}
\seealso{
\code{\link[=frm_cross_spectrum]{frm_cross_spectrum()}}, \code{\link[=cross_wishart]{cross_wishart()}}.
}

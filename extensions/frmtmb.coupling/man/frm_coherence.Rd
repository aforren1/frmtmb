% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling.R
\name{frm_coherence}
\alias{frm_coherence}
\alias{frm_phase}
\title{Coherence and phase from a fitted coupling model, with intervals}
\usage{
frm_coherence(
  fit,
  newdata = NULL,
  re.form = NULL,
  level = 0.95,
  allow_new_levels = FALSE
)

frm_phase(
  fit,
  newdata = NULL,
  re.form = NULL,
  level = 0.95,
  allow_new_levels = FALSE
)
}
\arguments{
\item{fit}{A \code{\link[frmtmb:frm]{frmtmb::frm()}} fit whose family is \code{\link[=cross_wishart]{cross_wishart()}}.}

\item{newdata}{Optional data frame of predictor values. Defaults to
the data the model was fitted to.}

\item{re.form}{\code{NULL} keeps the random effects, so the answer is per
group; \code{NA} drops them, so the answer is the population one. Passed
through to \code{\link[stats:predict]{stats::predict()}}.}

\item{level}{Confidence level.}

\item{allow_new_levels}{Passed through to \code{\link[stats:predict]{stats::predict()}}.}
}
\value{
A data frame with one row per row of \code{newdata} and columns
\code{.estimate}, \code{.se}, \code{.lower} and \code{.upper}. \code{frm_coherence()} adds
\code{.eta}, the value on the logit scale the interval was built on.
}
\description{
Reads the \code{coh} and \code{phase} distributional parameters off a
\code{\link[=cross_wishart]{cross_wishart()}} fit and returns them with confidence intervals.
}
\section{Why the interval is built on the link scale}{

Coherence lives on \verb{(0, 1)} and a Wald interval on that scale walks
off the end whenever the estimate is near a boundary, which is
exactly where few segments put it. The interval here is a Wald
interval on the logit scale pushed through \code{plogis()}, so it cannot
leave \verb{(0, 1)} and it is asymmetric in the direction the sampling
distribution actually is.

Measured coverage of that interval, 40 subjects with a coherence
random effect, 150 replicates: 0.912 at 4 segments per subject and
0.893 at 16, when every distributional parameter carries a random
effect. With the two channel powers left as free per-subject
parameters instead, coverage falls to 0.622 and 0.820, because two
free numbers per subject that gain no information as subjects are
added starve the variance component. \code{dev/xspec-findings.md} in the
repository has the table. \strong{Put \code{(1 | id)} on every dpar, not only on
\code{coh}.}
}

\section{Phase is an angle}{

\code{frm_phase()} returns radians and does no wrapping: the linear
predictor is unbounded and a phase of \code{-3.1} and one of \code{3.18} are
the same angle reached from two sides. When a group's phase is near
the wrap point its interval is wide for a reason that is a
parameterization artifact rather than data, and the fit is worth
re-centering with an offset. Nothing here detects that for you.
}

\examples{
set.seed(5)
src <- rnorm(4096)
xs <- frm_cross_spectrum(src + rnorm(4096), 0.9 * src + rnorm(4096),
                         segments = 16)
xs <- xs[xs$freq < 0.1, ]
fit <- frmtmb::frm(
  frmtmb::bf(w11 | vreal(w22, w12r, w12i) + vint(n) ~ 1,
             pow2 ~ 1, coh ~ 1, phase ~ 1),
  family = cross_wishart(), data = xs)
frm_coherence(fit, newdata = xs[1, ])
frm_phase(fit, newdata = xs[1, ])
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coupling.R
\name{frm_cross_simulate}
\alias{frm_cross_simulate}
\title{Draw whole cross-spectral matrices from a fitted coupling model}
\usage{
frm_cross_simulate(fit, nsim = 1L, seed = NULL, newdata = NULL, re.form = NULL)
}
\arguments{
\item{fit}{A \code{\link[=cross_wishart]{cross_wishart()}} fit.}

\item{nsim}{Number of replicate data frames.}

\item{seed}{Optional seed, set with \code{\link[=set.seed]{set.seed()}} before drawing.}

\item{newdata}{Optional data frame of predictor values.}

\item{re.form}{Passed to \code{\link[stats:predict]{stats::predict()}}; \code{NULL} keeps the random
effects.}
}
\value{
A list of \code{nsim} data frames, each with the columns
\code{\link[=frm_cross_spectrum]{frm_cross_spectrum()}} produces: \code{w11}, \code{w22}, \code{w12r}, \code{w12i}, \code{n}.
}
\description{
\code{\link[stats:simulate]{stats::simulate()}} refuses on a \code{\link[=cross_wishart]{cross_wishart()}} fit, because a
draw is a Hermitian matrix and the response slot carries one of its
four numbers. This returns all four, so a simulated data frame can be
refitted or compared against the observed one.
}
\section{How a draw is made}{

Each row's fitted spectral matrix is factored as \verb{L L^H} and \code{n}
independent complex normal vectors are drawn through \code{L}; the
returned matrix is their outer-product sum. That is the definition of
the complex Wishart rather than a transformation of it, so a
simulated row and the density are one statement of the model. Checked
against the density's own first moment: over 20000 draws the mean of
the simulated matrix divided by \code{n} matches the fitted matrix to
0.0011 at \code{n = 16}.
}

\examples{
set.seed(6)
src <- rnorm(2048)
xs <- frm_cross_spectrum(src + rnorm(2048), 0.9 * src + rnorm(2048),
                         segments = 16)
xs <- xs[xs$freq < 0.1, ]
fit <- frmtmb::frm(
  frmtmb::bf(w11 | vreal(w22, w12r, w12i) + vint(n) ~ 1,
             pow2 ~ 1, coh ~ 1, phase ~ 1),
  family = cross_wishart(), data = xs)
str(frm_cross_simulate(fit, nsim = 2, seed = 1)[[1]])
}

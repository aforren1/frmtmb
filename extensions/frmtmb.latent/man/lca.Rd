% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lca.R
\name{lca}
\alias{lca}
\title{Latent class analysis}
\usage{
lca(K, ncat = NULL, na.rm = TRUE)
}
\arguments{
\item{K}{Number of latent classes (at least 2).}

\item{ncat}{Number of categories per item: a vector of length \code{J}, a
single value for equally-sized items, or \code{NULL} (the default) to
infer each item's count as its largest observed code, which is
what poLCA does.}

\item{na.rm}{If \code{TRUE} (default) subjects with any missing item are
dropped by \code{na.action}; if \code{FALSE} they are kept and each missing
item's term is masked out of that subject's likelihood.}
}
\value{
A \code{frmtmb_family}.
}
\description{
\code{lca(K)} fits the classic latent class measurement model, the one
'poLCA' fits (Linzer and Lewis 2011): \code{J} polytomous indicator items
per subject, one latent class \code{c} in \verb{1..K} per subject, and the
items conditionally independent given the class,
}
\details{
\deqn{P(y_i) = \sum_c w_c \prod_j \pi_{j,c,y_{ij}}.}

The response is a MATRIX, one row per subject and one column per
item, holding integer category codes \verb{1..C_j}. Write it as
\code{cbind(item1, item2, ...)} on the left of the formula, or attach a
matrix column to the data (\code{dd$Y <- data.matrix(dd[items])}) and
name it. Items may have different numbers of categories.

The class-membership weights are the \verb{theta1 ... theta\{K-1\}} dpars,
multinomial logit against class \code{K}, and the main model formula
applies to every one of them. So \code{bf(cbind(a, b, c) ~ 1)} is the
plain measurement model and \code{bf(cbind(a, b, c) ~ age + educ)} is
poLCA's latent class regression: covariates on class membership come
from the ordinary linear-predictor machinery, with the usual
\code{fixef()}, \code{confint()} and \code{hypothesis()} on top. Individual gating
predictors are overridable as \code{bf(Y ~ x, theta2 ~ 1)} (all but
\code{theta1}, which the main formula owns).

The item profiles are family extra parameters, held as
reference-category logits, one vector \verb{pi<j>} per item (item \code{j}'s
\code{K * (C_j - 1)} free logits, class-major), and reported
on the probability scale by \code{\link[=lca_profiles]{lca_profiles()}}. They take no linear
predictor - a covariate acts on class membership, never on an item's
conditional response probability, which is what makes the classes
interpretable as measurement.
}
\section{Labeling and starting values}{

A latent class likelihood is invariant to relabeling the classes and
is genuinely multimodal, so the answer depends on where the
optimizer starts. poLCA uses random restarts (\code{nrep}). The starting
values here are deterministic instead: subjects are scored by the
mean of their item codes rescaled to \verb{[0, 1]}, cut into \code{K}
equal-count slices by that score, and each slice's smoothed
empirical category proportions become one class's starting profile.
Class 1 is therefore the low-score end and class \code{K} the high-score
end, and re-running the same data gives the same labeling.

That fixes reproducibility, not multimodality. Do what
\code{poLCA(..., nrep = 10)} does and compare starts before reading a
solution: perturb the item parameters and refit,

\if{html}{\out{<div class="sourceCode">}}\preformatted{p0 <- fit$frame$par_template[fit$frame$extra_names]
refits <- replicate(10, simplify = FALSE,
  frm(bf(Y ~ 1), family = lca(K = 3), data = dd,
      start = lapply(p0, function(v) v + rnorm(length(v)))))
sapply(refits, logLik)
}\if{html}{\out{</div>}}

and keep the best. \code{\link[frmtmb:frm_allfit]{frm_allfit()}} is a different check: it re-runs
the four optimizers from the SAME start, so it tests the optimizer,
not the surface. Ordering the gating intercepts through \code{lower} and
\code{upper} is the way to pin the labeling itself.

Asking for more classes than the data hold drives item
probabilities to 0 and 1. The optimizer then reports singular
convergence and the standard errors come back \code{NaN}, which is the
boundary showing through rather than a fault; poLCA lands on the
same solutions. Compare \code{AIC()} and \code{BIC()} across \code{K} and read
\code{\link[=lca_probs]{lca_probs()}}'s entropy before committing to a \code{K}.
}

\section{Missing item responses}{

With \code{na.rm = TRUE} (the default, poLCA's default) a subject with
any missing item is dropped by the usual \code{na.action}, and the
message naming the dropped rows is the ordinary one. With
\code{na.rm = FALSE} the subject is kept and only the missing item's
factor leaves that subject's likelihood, which is poLCA's
\code{na.rm = FALSE} behavior; the mask is data, so the tape never sees a
branch. A subject missing EVERY item contributes a constant and is
better dropped.
}

\section{What is and is not supported}{

Post-fit, \code{\link[=lca_probs]{lca_probs()}} gives posterior class-membership
probabilities per subject (with the relative-entropy classification
diagnostic attached), \code{\link[=lca_profiles]{lca_profiles()}} gives the item profile table,
and \code{simulate()} draws a class per subject and then its items.
\code{fitted()}, \code{predict(type = "response")} and \code{residuals()} are
refused: the response is a matrix of nominal codes, so an "expected
item code" would be an average of arbitrary labels. Read
\code{\link[=lca_probs]{lca_probs()}} and \code{\link[=lca_profiles]{lca_profiles()}} instead. \code{predict()} itself
returns the gating linear predictor (\code{theta1} by default, any
\code{theta} with \verb{dpar =}), on \code{newdata} as well.

The gating coefficients are ordinary fixed effects, so \code{fixef()},
\code{confint()} (Wald, profile and uniroot), \code{hypothesis()},
\code{set_prior()}, \code{lower}/\code{upper} bounds and
\code{frmtmb.sample::frm_sample()} all work on
them; \code{anova()} compares nested gating formulas at one \code{K}.

Refused in this version: random effects and smooths anywhere in the
model (latent classes plus continuous random effects is the
growth-mixture shape - use \code{mixture(..., groups = ~g)} for that),
\code{REML} and \code{frmtmb_control(profile = TRUE)} (the classes are
exchangeable, so there is no single inner mode), \code{quadrature}, every
addition term (\code{weights()}, \code{cens()}, \code{trunc()}, \code{se()}, \code{mi()},
\code{trials()}), \code{\link[frmtmb:mvbf]{mvbf()}}, and \code{residuals(type = "osa")}.
}

\examples{
set.seed(1)
# four binary items measuring two well-separated classes
n <- 300
cl <- rbinom(n, 1, 0.4) + 1
pr <- rbind(c(0.85, 0.80, 0.75, 0.90), c(0.15, 0.20, 0.25, 0.10))
Y <- matrix(0L, n, 4)
for (j in 1:4) Y[, j] <- 1L + rbinom(n, 1, pr[cl, j])
dd <- data.frame(x = rnorm(n))
dd$Y <- Y

fit <- frm(bf(Y ~ 1), family = lca(K = 2), data = dd)
lca_profiles(fit)
head(lca_probs(fit))

# latent class regression: covariates gate class membership
frm(bf(Y ~ x), family = lca(K = 2), data = dd)
}
\references{
Linzer, D. A. and Lewis, J. B. (2011). poLCA: An R
Package for Polytomous Variable Latent Class Analysis. \emph{Journal of
Statistical Software}, 42(10), 1-29.
}
\seealso{
\code{\link[=lca_probs]{lca_probs()}}, \code{\link[=lca_profiles]{lca_profiles()}}, \code{\link[frmtmb:mixture]{mixture()}} for a mixture
of continuous responses, \code{\link[frmtmb:mixture_mvn]{mixture_mvn()}} for model-based
clustering of a numeric matrix.
}

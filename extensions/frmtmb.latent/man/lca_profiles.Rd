% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lca.R
\name{lca_profiles}
\alias{lca_profiles}
\title{The fitted item profiles of an \code{lca()} fit}
\usage{
lca_profiles(fit)
}
\arguments{
\item{fit}{A \code{frmtmb_fit} with an \code{\link[=lca]{lca()}} family.}
}
\value{
A named list with one \verb{K x C_j} probability matrix per item,
of class \code{frmtmb_lca_profiles}, carrying the estimated class sizes
(the mean prior class probabilities, poLCA's \verb{$P}) in the
\code{class_sizes} attribute.
}
\description{
The class-conditional item-response probabilities \code{pi[j, c, k]},
which are the main output of a latent class analysis: for each item,
a \verb{K x C_j} table of the probability of each category given each
class. This is poLCA's \verb{$probs}.
}
\examples{
set.seed(2)
n <- 200
cl <- rbinom(n, 1, 0.5) + 1
pr <- rbind(c(0.9, 0.85, 0.8), c(0.1, 0.15, 0.2))
Y <- matrix(0L, n, 3)
for (j in 1:3) Y[, j] <- 1L + rbinom(n, 1, pr[cl, j])
dd <- data.frame(row = seq_len(n))
dd$Y <- Y
fit <- frm(bf(Y ~ 1), family = lca(K = 2), data = dd)

pf <- lca_profiles(fit)
pf
pf[[1]]                       # item 1's K x C table
attr(pf, "class_sizes")
}
\seealso{
\code{\link[=lca]{lca()}}, \code{\link[=lca_probs]{lca_probs()}}
}

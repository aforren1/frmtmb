# GENERATED by dev/famlink-gen-links.R from brms 2.23.0. Do not edit by
# hand: rerun the script, which reads brms:::.family_<name>()$links.
#
# The links brms allows for each family's MEAN, keyed by the frmtmb
# family name. The first entry is brms's default. nbinom1, tweedie and
# huber have no brms family and take the set of the analog named in
# `brms_mu_link_source`.

brms_mu_link_source <- c(
  gaussian = "gaussian",
  poisson = "poisson",
  binomial = "binomial",
  Gamma = "gamma",
  lognormal = "lognormal",
  student = "student",
  negbinomial = "negbinomial",
  nbinom1 = "negbinomial",
  beta = "beta",
  tweedie = "gamma",
  compois = "com_poisson",
  zero_inflated_poisson = "zero_inflated_poisson",
  zero_inflated_negbinomial = "zero_inflated_negbinomial",
  hurdle_poisson = "hurdle_poisson",
  multinomial = "multinomial",
  cumulative = "cumulative",
  beta_binomial = "beta_binomial",
  skew_normal = "skew_normal",
  inverse.gaussian = "inverse.gaussian",
  exgaussian = "exgaussian",
  bernoulli = "bernoulli",
  geometric = "geometric",
  exponential = "exponential",
  weibull = "weibull",
  shifted_lognormal = "shifted_lognormal",
  hurdle_gamma = "hurdle_gamma",
  hurdle_lognormal = "hurdle_lognormal",
  zero_inflated_binomial = "zero_inflated_binomial",
  zero_inflated_beta = "zero_inflated_beta",
  asym_laplace = "asym_laplace",
  zero_inflated_asym_laplace = "zero_inflated_asym_laplace",
  huber = "gaussian",
  sratio = "sratio",
  cratio = "cratio",
  acat = "acat",
  von_mises = "von_mises",
  cox = "cox",
  categorical = "categorical"
)

brms_mu_link_analogs <- c("nbinom1", "tweedie", "huber")

brms_mu_links <- list(
  gaussian = c("identity", "log", "inverse", "softplus", "squareplus",
      "logit", "probit", "probit_approx", "cloglog", "cauchit", "softit"),
  poisson = c("log", "identity", "sqrt", "softplus", "squareplus"),
  binomial = c("logit", "probit", "probit_approx", "cloglog", "cauchit",
      "softit", "identity", "log"),
  Gamma = c("log", "identity", "inverse", "softplus", "squareplus"),
  lognormal = c("identity", "inverse"),
  student = c("identity", "log", "inverse", "softplus", "squareplus",
      "logit", "probit", "probit_approx", "cloglog", "cauchit", "softit"),
  negbinomial = c("log", "identity", "sqrt", "softplus", "squareplus"),
  nbinom1 = c("log", "identity", "sqrt", "softplus", "squareplus"),
  beta = c("logit", "probit", "probit_approx", "cloglog", "cauchit",
      "softit", "identity", "log"),
  tweedie = c("log", "identity", "inverse", "softplus", "squareplus"),
  compois = c("log", "identity", "sqrt", "softplus", "squareplus"),
  zero_inflated_poisson = c("log", "identity", "sqrt", "softplus",
      "squareplus"),
  zero_inflated_negbinomial = c("log", "identity", "sqrt", "softplus",
      "squareplus"),
  hurdle_poisson = c("log", "identity", "sqrt", "softplus", "squareplus"),
  multinomial = c("logit"),
  cumulative = c("logit", "probit", "probit_approx", "cloglog",
      "cauchit", "softit"),
  beta_binomial = c("logit", "probit", "probit_approx", "cloglog",
      "cauchit", "softit", "identity"),
  skew_normal = c("identity", "log", "inverse", "softplus", "squareplus",
      "logit", "probit", "probit_approx", "cloglog", "cauchit", "softit"),
  inverse.gaussian = c("1/mu^2", "inverse", "identity", "log",
      "softplus", "squareplus"),
  exgaussian = c("identity", "log", "inverse", "softplus", "squareplus"),
  bernoulli = c("logit", "probit", "probit_approx", "cloglog", "cauchit",
      "softit", "identity", "log"),
  geometric = c("log", "identity", "sqrt", "softplus", "squareplus"),
  exponential = c("log", "identity", "inverse", "softplus", "squareplus"),
  weibull = c("log", "identity", "inverse", "softplus", "squareplus"),
  shifted_lognormal = c("identity", "inverse"),
  hurdle_gamma = c("log", "identity", "inverse", "softplus",
      "squareplus"),
  hurdle_lognormal = c("identity", "inverse"),
  zero_inflated_binomial = c("logit", "probit", "probit_approx",
      "cloglog", "cauchit", "softit", "identity", "log"),
  zero_inflated_beta = c("logit", "probit", "probit_approx", "cloglog",
      "cauchit", "softit", "identity", "log"),
  asym_laplace = c("identity", "log", "inverse", "softplus",
      "squareplus"),
  zero_inflated_asym_laplace = c("identity", "log", "inverse",
      "softplus", "squareplus"),
  huber = c("identity", "log", "inverse", "softplus", "squareplus",
      "logit", "probit", "probit_approx", "cloglog", "cauchit", "softit"),
  sratio = c("logit", "probit", "probit_approx", "cloglog", "cauchit"),
  cratio = c("logit", "probit", "probit_approx", "cloglog", "cauchit"),
  acat = c("logit", "probit", "probit_approx", "cloglog", "cauchit",
      "softit"),
  von_mises = c("tan_half", "identity"),
  cox = c("log", "identity", "softplus", "squareplus"),
  categorical = c("logit")
)

# The families brms:::no_mixture() bars as mixture components, by
# frmtmb family name. The analogs above take no verdict.
brms_no_mixture <- c("multinomial", "hurdle_gamma", "hurdle_lognormal",
  "zero_inflated_beta", "categorical")
